{
  "$defs": {
    "AffineConfig": {
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "affine",
          "default": "affine",
          "title": "Type",
          "type": "string"
        },
        "gain": {
          "title": "Gain",
          "type": "number"
        },
        "bias": {
          "default": 0.0,
          "title": "Bias",
          "type": "number"
        },
        "rate_gain": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Rate Gain"
        }
      },
      "required": [
        "gain"
      ],
      "title": "AffineConfig",
      "type": "object"
    },
    "AuthConfig": {
      "additionalProperties": false,
      "description": "The `runner.auth` section: who may reach a *bare* runner, one with no front.\n\nA *token* is the one credential: machines send it as a bearer header, and a person\ntrades it (or the one-time link the runner prints at start) for a session cookie. What a\ncaller with neither may do is `anonymous`: nothing, or read. With no token the runner is\nopen, and served on loopback only unless the run itself says otherwise (see\n[settle_exposure][flyball.runtime.config.settle_exposure]); that switch is never a key\nhere, so no file -- nor anything it `extends` -- can open a runner.\n\nA runner the front started (`--front-dir`) ignores this section: the front decides who\ngets in. `password`, `session` and `secret` are removed: parsed, so an old file still\nstarts, and ignored with a warning.",
      "properties": {
        "token": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Bearer token for the CLI, MCP clients, scripts and the UI.",
          "title": "Token"
        },
        "anonymous": {
          "default": "none",
          "description": "What a caller with no session and no token may do: nothing, or read (every GET and every stream).",
          "enum": [
            "none",
            "read"
          ],
          "title": "Anonymous",
          "type": "string"
        },
        "password": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "deprecated": true,
          "description": "Removed: ignored with a warning. The bare runner has no password login; use `token`, or run it under `flyball run` (`runner.front`).",
          "title": "Password"
        },
        "session": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "deprecated": true,
          "description": "Removed: ignored with a warning. A token-link session lasts 12 h.",
          "title": "Session"
        },
        "secret": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "deprecated": true,
          "description": "Removed: ignored with a warning. Sessions are no longer signed.",
          "title": "Secret"
        }
      },
      "title": "AuthConfig",
      "type": "object"
    },
    "Bounds": {
      "maxItems": 2,
      "minItems": 2,
      "prefixItems": [
        {
          "type": "number"
        },
        {
          "type": "number"
        }
      ],
      "type": "array"
    },
    "ClockEntry": {
      "additionalProperties": false,
      "description": "How the rig's time runs. Only a rig with nothing real on it may run off wall time.",
      "properties": {
        "speed": {
          "default": 1.0,
          "description": "Rig seconds per wall second.",
          "exclusiveMinimum": 0,
          "title": "Speed",
          "type": "number"
        },
        "stepped": {
          "default": false,
          "description": "Time moves only when stepped; for a batch run or a test.",
          "title": "Stepped",
          "type": "boolean"
        }
      },
      "title": "ClockEntry",
      "type": "object"
    },
    "ControllerEntry": {
      "additionalProperties": false,
      "description": "A controller and how it regulates its output; keyed by the output's address in the file.",
      "properties": {
        "measured": {
          "description": "The measured signal's address (a P signal).",
          "title": "Measured",
          "type": "string"
        },
        "law": {
          "anyOf": [
            {
              "discriminator": {
                "mapping": {
                  "imc": "#/$defs/IMCConfig",
                  "on_off": "#/$defs/OnOffConfig",
                  "open_loop": "#/$defs/OpenLoopConfig",
                  "p": "#/$defs/PConfig",
                  "pi": "#/$defs/PIConfig",
                  "pid": "#/$defs/PIDConfig",
                  "scheduled": "#/$defs/ScheduledConfig",
                  "sliding": "#/$defs/SlidingModeConfig",
                  "smith": "#/$defs/SmithPredictorConfig"
                },
                "propertyName": "type"
              },
              "oneOf": [
                {
                  "$ref": "#/$defs/OpenLoopConfig"
                },
                {
                  "$ref": "#/$defs/PConfig"
                },
                {
                  "$ref": "#/$defs/PIConfig"
                },
                {
                  "$ref": "#/$defs/PIDConfig"
                },
                {
                  "$ref": "#/$defs/IMCConfig"
                },
                {
                  "$ref": "#/$defs/OnOffConfig"
                },
                {
                  "$ref": "#/$defs/SmithPredictorConfig"
                },
                {
                  "$ref": "#/$defs/ScheduledConfig"
                },
                {
                  "$ref": "#/$defs/SlidingModeConfig"
                }
              ]
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Law"
        },
        "feedforward": {
          "anyOf": [
            {
              "discriminator": {
                "mapping": {
                  "affine": "#/$defs/AffineConfig",
                  "identity": "#/$defs/IdentityConfig",
                  "none": "#/$defs/NoFeedforwardConfig",
                  "table": "#/$defs/TableConfig"
                },
                "propertyName": "type"
              },
              "oneOf": [
                {
                  "$ref": "#/$defs/IdentityConfig"
                },
                {
                  "$ref": "#/$defs/NoFeedforwardConfig"
                },
                {
                  "$ref": "#/$defs/AffineConfig"
                },
                {
                  "$ref": "#/$defs/TableConfig"
                }
              ]
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Maps the measured signal's unit to the output's; the law adds to it. Omit for identity (the setpoint itself) when the units agree, else none.",
          "title": "Feedforward"
        },
        "is_default": {
          "default": false,
          "title": "Is Default",
          "type": "boolean"
        },
        "min_period_s": {
          "anyOf": [
            {
              "exclusiveMinimum": 0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Update the law at most this often; omit to update on every reading.",
          "title": "Min Period S"
        },
        "setpoint_period_s": {
          "anyOf": [
            {
              "exclusiveMinimum": 0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "While following a moving setpoint (a ramp, a profile), re-apply its feedforward this often between readings; the law steps only on readings. Omit for max(0.1 s, poll_s / 4) from the measured signal's poll_s.",
          "title": "Setpoint Period S"
        },
        "on_fault": {
          "$ref": "#/$defs/OnFaultEntry",
          "default": "freeze",
          "description": "What it does once its source has been faulty (stale, invalid, offline) for its wait, or at once when its law raises: freeze (default: stays frozen, resumes by itself), manual, stop (its output's stop), stop_device (its output's device's stop), or {freeze_s: <s>, then: manual|stop|stop_device}. Each but freeze latches until a person resets it; a law error takes at least manual."
        }
      },
      "required": [
        "measured"
      ],
      "title": "ControllerEntry",
      "type": "object"
    },
    "CustomJwt": {
      "additionalProperties": false,
      "description": "`runner.front.proxy.jwt`: a signed assertion the `custom` preset verifies.",
      "properties": {
        "header": {
          "title": "Header",
          "type": "string"
        },
        "jwks_url": {
          "title": "Jwks Url",
          "type": "string"
        },
        "issuer": {
          "title": "Issuer",
          "type": "string"
        },
        "audience": {
          "title": "Audience",
          "type": "string"
        },
        "algorithms": {
          "items": {
            "type": "string"
          },
          "title": "Algorithms",
          "type": "array"
        }
      },
      "required": [
        "header",
        "jwks_url",
        "issuer",
        "audience",
        "algorithms"
      ],
      "title": "CustomJwt",
      "type": "object"
    },
    "DeviceEntry": {
      "additionalProperties": true,
      "description": "The envelope of one device in the rig file: flyball's keys, the same for every driver.\n\n`driver:` picks the driver's config model by type; the driver's own\nfields sit flat beside these keys, and every key that is not one of them\nis the driver's (`driver_config`).",
      "properties": {
        "driver": {
          "title": "Driver",
          "type": "string"
        },
        "label": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Label"
        },
        "poll_s": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Poll S"
        },
        "signals": {
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/SignalMeta"
              },
              {
                "$ref": "#/$defs/NamespaceMeta"
              }
            ]
          },
          "title": "Signals",
          "type": "object"
        },
        "inputs": {
          "additionalProperties": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "number"
              }
            ]
          },
          "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
          "title": "Inputs",
          "type": "object"
        },
        "reads": {
          "anyOf": [
            {
              "$ref": "#/$defs/Reads"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "retry_max_age_s": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
          "title": "Retry Max Age S"
        },
        "stop": {
          "anyOf": [
            {
              "additionalProperties": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "$ref": "#/$defs/Keep"
                  }
                ]
              },
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
          "title": "Stop"
        },
        "on_shutdown": {
          "anyOf": [
            {
              "$ref": "#/$defs/OnShutdown"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
        },
        "permissive": {
          "anyOf": [
            {
              "additionalProperties": {
                "$ref": "#/$defs/Permissive"
              },
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
          "title": "Permissive"
        }
      },
      "required": [
        "driver"
      ],
      "title": "DeviceEntry",
      "type": "object"
    },
    "FakeGpioConfigTagged": {
      "properties": {
        "levels": {
          "additionalProperties": {
            "type": "boolean"
          },
          "description": "Input levels by line.",
          "title": "Levels",
          "type": "object"
        },
        "type": {
          "const": "fake_gpio",
          "default": "fake_gpio",
          "title": "Type",
          "type": "string"
        }
      },
      "title": "FakeGpioConfigTagged",
      "type": "object"
    },
    "FakeI2cConfigTagged": {
      "properties": {
        "registers": {
          "additionalProperties": {
            "additionalProperties": {
              "items": {
                "type": "integer"
              },
              "type": "array"
            },
            "type": "object"
          },
          "title": "Registers",
          "type": "object"
        },
        "replies": {
          "additionalProperties": {
            "items": {
              "items": {
                "type": "integer"
              },
              "type": "array"
            },
            "type": "array"
          },
          "title": "Replies",
          "type": "object"
        },
        "blocking": {
          "default": false,
          "description": "Run this fake's writes on the Writer thread, as a real bus would.",
          "title": "Blocking",
          "type": "boolean"
        },
        "type": {
          "const": "fake_i2c",
          "default": "fake_i2c",
          "title": "Type",
          "type": "string"
        }
      },
      "title": "FakeI2cConfigTagged",
      "type": "object"
    },
    "FakeOneWireConfigTagged": {
      "properties": {
        "texts": {
          "additionalProperties": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ]
          },
          "title": "Texts",
          "type": "object"
        },
        "type": {
          "const": "fake_onewire",
          "default": "fake_onewire",
          "title": "Type",
          "type": "string"
        }
      },
      "title": "FakeOneWireConfigTagged",
      "type": "object"
    },
    "FakePwmConfigTagged": {
      "properties": {
        "type": {
          "const": "fake_pwm",
          "default": "fake_pwm",
          "title": "Type",
          "type": "string"
        }
      },
      "title": "FakePwmConfigTagged",
      "type": "object"
    },
    "FakeRegisterLinkConfigTagged": {
      "properties": {
        "registers": {
          "additionalProperties": {
            "type": "integer"
          },
          "title": "Registers",
          "type": "object"
        },
        "blocking": {
          "default": false,
          "description": "Run this fake's writes on the Writer thread, as a real bus would.",
          "title": "Blocking",
          "type": "boolean"
        },
        "type": {
          "const": "fake_registers",
          "default": "fake_registers",
          "title": "Type",
          "type": "string"
        }
      },
      "title": "FakeRegisterLinkConfigTagged",
      "type": "object"
    },
    "FakeSpiConfigTagged": {
      "properties": {
        "replies": {
          "items": {
            "items": {
              "type": "integer"
            },
            "type": "array"
          },
          "title": "Replies",
          "type": "array"
        },
        "type": {
          "const": "fake_spi",
          "default": "fake_spi",
          "title": "Type",
          "type": "string"
        }
      },
      "title": "FakeSpiConfigTagged",
      "type": "object"
    },
    "FakeTextLinkConfigTagged": {
      "properties": {
        "replies": {
          "additionalProperties": {
            "type": "string"
          },
          "title": "Replies",
          "type": "object"
        },
        "blocking": {
          "default": false,
          "description": "Run this fake's writes on the Writer thread, as a real instrument would.",
          "title": "Blocking",
          "type": "boolean"
        },
        "type": {
          "const": "fake_text",
          "default": "fake_text",
          "title": "Type",
          "type": "string"
        }
      },
      "title": "FakeTextLinkConfigTagged",
      "type": "object"
    },
    "FakeUartConfigTagged": {
      "properties": {
        "replies": {
          "items": {
            "format": "binary",
            "type": "string"
          },
          "title": "Replies",
          "type": "array"
        },
        "type": {
          "const": "fake_uart",
          "default": "fake_uart",
          "title": "Type",
          "type": "string"
        }
      },
      "title": "FakeUartConfigTagged",
      "type": "object"
    },
    "FreezeThen": {
      "additionalProperties": false,
      "description": "`on_fault: {freeze_s: <s>, then: <action>}`: frozen `freeze_s` of fault time, then act.",
      "properties": {
        "freeze_s": {
          "description": "Seconds of fault time (accrued across flicker) to stay frozen before `then`.",
          "minimum": 0,
          "title": "Freeze S",
          "type": "number"
        },
        "then": {
          "enum": [
            "manual",
            "stop",
            "stop_device"
          ],
          "title": "Then",
          "type": "string"
        }
      },
      "required": [
        "freeze_s",
        "then"
      ],
      "title": "FreezeThen",
      "type": "object"
    },
    "FrontConfig": {
      "additionalProperties": false,
      "description": "`runner.front`: how `flyball run`'s front serves this rig. Read by the front, never here.",
      "properties": {
        "listen": {
          "default": "127.0.0.1:8000",
          "description": "Where the front listens.",
          "title": "Listen",
          "type": "string"
        },
        "auth": {
          "default": "local",
          "description": "Who gets in: the shape.",
          "enum": [
            "local",
            "password",
            "proxy",
            "sso"
          ],
          "title": "Auth",
          "type": "string"
        },
        "url": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The external URL the rig is reached at.",
          "title": "Url"
        },
        "tls": {
          "anyOf": [
            {
              "$ref": "#/$defs/TlsFiles"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "password": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`auth: password`: a `$scrypt$` line from `flyball password`.",
          "title": "Password"
        },
        "anonymous": {
          "default": "none",
          "enum": [
            "none",
            "read"
          ],
          "title": "Anonymous",
          "type": "string"
        },
        "proxy": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProxyConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "uv": {
          "default": false,
          "title": "Uv",
          "type": "boolean"
        },
        "login": {
          "default": "12h",
          "title": "Login",
          "type": "string"
        },
        "trusted_proxies": {
          "items": {
            "type": "string"
          },
          "title": "Trusted Proxies",
          "type": "array"
        },
        "tokens": {
          "anyOf": [
            {
              "$ref": "#/$defs/TokensConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Named-token lifetime ceilings: default_lifetime, max_lifetime."
        }
      },
      "title": "FrontConfig",
      "type": "object"
    },
    "FurnaceConfigTagged": {
      "additionalProperties": false,
      "properties": {
        "zones": {
          "default": 3,
          "minimum": 1,
          "title": "Zones",
          "type": "integer"
        },
        "power_w": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "items": {
                "type": "number"
              },
              "type": "array"
            }
          ],
          "default": 2000.0,
          "title": "Power W"
        },
        "capacity_j_per_k": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "items": {
                "type": "number"
              },
              "type": "array"
            }
          ],
          "default": 5000.0,
          "title": "Capacity J Per K"
        },
        "coupling_w_per_k": {
          "default": 5.0,
          "minimum": 0,
          "title": "Coupling W Per K",
          "type": "number"
        },
        "loss_w_per_k": {
          "default": 2.0,
          "minimum": 0,
          "title": "Loss W Per K",
          "type": "number"
        },
        "emissivity": {
          "default": 0.8,
          "maximum": 1,
          "minimum": 0,
          "title": "Emissivity",
          "type": "number"
        },
        "area_m2": {
          "default": 0.02,
          "minimum": 0,
          "title": "Area M2",
          "type": "number"
        },
        "ambient_c": {
          "default": 20.0,
          "live": "outputs.*",
          "title": "Ambient C",
          "type": "number"
        },
        "sample_capacity_j_per_k": {
          "default": 800.0,
          "exclusiveMinimum": 0,
          "title": "Sample Capacity J Per K",
          "type": "number"
        },
        "sample_coupling_w_per_k": {
          "default": 4.0,
          "minimum": 0,
          "title": "Sample Coupling W Per K",
          "type": "number"
        },
        "sample_zone": {
          "default": 2,
          "minimum": 1,
          "title": "Sample Zone",
          "type": "integer"
        },
        "sensor_lag_s": {
          "default": 3.0,
          "minimum": 0,
          "title": "Sensor Lag S",
          "type": "number"
        },
        "noise": {
          "default": 0.0,
          "live": "stats.noise",
          "minimum": 0,
          "title": "Noise",
          "type": "number"
        },
        "seed": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Seed"
        },
        "initial_c": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "live": "outputs.*",
          "title": "Initial C"
        },
        "type": {
          "const": "sim_furnace",
          "default": "sim_furnace",
          "title": "Type",
          "type": "string"
        }
      },
      "title": "FurnaceConfigTagged",
      "type": "object"
    },
    "GpioConfigTagged": {
      "properties": {
        "chip": {
          "default": "gpiochip0",
          "title": "Chip",
          "type": "string"
        },
        "type": {
          "const": "gpio",
          "default": "gpio",
          "title": "Type",
          "type": "string"
        }
      },
      "title": "GpioConfigTagged",
      "type": "object"
    },
    "I2cConfigTagged": {
      "properties": {
        "bus": {
          "minimum": 0,
          "title": "Bus",
          "type": "integer"
        },
        "type": {
          "const": "i2c",
          "default": "i2c",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "bus"
      ],
      "title": "I2cConfigTagged",
      "type": "object"
    },
    "IMCConfig": {
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "imc",
          "default": "imc",
          "title": "Type",
          "type": "string"
        },
        "gain": {
          "title": "Gain",
          "type": "number"
        },
        "tau_s": {
          "title": "Tau S",
          "type": "number"
        },
        "dead_time_s": {
          "default": 0.0,
          "title": "Dead Time S",
          "type": "number"
        },
        "lam_s": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Lam S"
        },
        "derivative": {
          "default": true,
          "title": "Derivative",
          "type": "boolean"
        },
        "b": {
          "default": 1.0,
          "title": "B",
          "type": "number"
        },
        "n": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "N"
        }
      },
      "required": [
        "gain",
        "tau_s"
      ],
      "title": "IMCConfig",
      "type": "object"
    },
    "IdentityConfig": {
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "identity",
          "default": "identity",
          "title": "Type",
          "type": "string"
        }
      },
      "title": "IdentityConfig",
      "type": "object"
    },
    "Keep": {
      "const": "keep",
      "type": "string"
    },
    "ModbusRtuConfigTagged": {
      "properties": {
        "port": {
          "title": "Port",
          "type": "string"
        },
        "baud": {
          "default": 9600,
          "title": "Baud",
          "type": "integer"
        },
        "type": {
          "const": "modbus_rtu",
          "default": "modbus_rtu",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "port"
      ],
      "title": "ModbusRtuConfigTagged",
      "type": "object"
    },
    "ModbusTcpConfigTagged": {
      "properties": {
        "host": {
          "title": "Host",
          "type": "string"
        },
        "port": {
          "default": 502,
          "title": "Port",
          "type": "integer"
        },
        "timeout_s": {
          "default": 3.0,
          "description": "Socket timeout for the pymodbus client.",
          "title": "Timeout S",
          "type": "number"
        },
        "type": {
          "const": "modbus_tcp",
          "default": "modbus_tcp",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "host"
      ],
      "title": "ModbusTcpConfigTagged",
      "type": "object"
    },
    "NamespaceMeta": {
      "additionalProperties": false,
      "description": "A namespace's metadata in the rig file: label, period, and the metadata of what is under it.\n\nA namespace's own driver settings (an I\u00b2C address) are not here: the\ndriver declares its namespaces in its own config, typed, and the\nenvelope only sets metadata on what the driver declared.",
      "properties": {
        "label": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Label"
        },
        "poll_s": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Poll S"
        },
        "tags": {
          "anyOf": [
            {
              "additionalProperties": {
                "type": "string"
              },
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Tags"
        },
        "signals": {
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/SignalMeta"
              },
              {
                "$ref": "#/$defs/NamespaceMeta"
              }
            ]
          },
          "title": "Signals",
          "type": "object"
        }
      },
      "title": "NamespaceMeta",
      "type": "object"
    },
    "NoFeedforwardConfig": {
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "none",
          "default": "none",
          "title": "Type",
          "type": "string"
        }
      },
      "title": "NoFeedforwardConfig",
      "type": "object"
    },
    "OnFaultEntry": {
      "anyOf": [
        {
          "enum": [
            "freeze",
            "manual",
            "stop",
            "stop_device"
          ],
          "type": "string"
        },
        {
          "$ref": "#/$defs/FreezeThen"
        }
      ]
    },
    "OnNoValue": {
      "description": "What a banded signal does when it has no value because of a fault (`invalid`, `stale`).",
      "enum": [
        "fire",
        "ignore"
      ],
      "title": "OnNoValue",
      "type": "string"
    },
    "OnOffConfig": {
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "on_off",
          "default": "on_off",
          "title": "Type",
          "type": "string"
        },
        "high": {
          "title": "High",
          "type": "number"
        },
        "low": {
          "default": 0.0,
          "title": "Low",
          "type": "number"
        },
        "hysteresis": {
          "default": 0.0,
          "title": "Hysteresis",
          "type": "number"
        }
      },
      "required": [
        "high"
      ],
      "title": "OnOffConfig",
      "type": "object"
    },
    "OnShutdown": {
      "enum": [
        "stop",
        "keep"
      ],
      "type": "string"
    },
    "OneWireConfigTagged": {
      "properties": {
        "root": {
          "default": "/sys/bus/w1/devices",
          "title": "Root",
          "type": "string"
        },
        "type": {
          "const": "onewire",
          "default": "onewire",
          "title": "Type",
          "type": "string"
        }
      },
      "title": "OneWireConfigTagged",
      "type": "object"
    },
    "OpenLoopConfig": {
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "open_loop",
          "default": "open_loop",
          "title": "Type",
          "type": "string"
        }
      },
      "title": "OpenLoopConfig",
      "type": "object"
    },
    "PConfig": {
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "p",
          "default": "p",
          "title": "Type",
          "type": "string"
        },
        "kp": {
          "title": "Kp",
          "type": "number"
        }
      },
      "required": [
        "kp"
      ],
      "title": "PConfig",
      "type": "object"
    },
    "PIConfig": {
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "pi",
          "default": "pi",
          "title": "Type",
          "type": "string"
        },
        "kp": {
          "default": 0,
          "title": "Kp",
          "type": "number"
        },
        "ki": {
          "default": 0,
          "title": "Ki",
          "type": "number"
        },
        "tt_s": {
          "default": 0,
          "title": "Tt S",
          "type": "number"
        },
        "b": {
          "default": 1.0,
          "title": "B",
          "type": "number"
        }
      },
      "title": "PIConfig",
      "type": "object"
    },
    "PIDConfig": {
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "pid",
          "default": "pid",
          "title": "Type",
          "type": "string"
        },
        "kp": {
          "default": 0,
          "title": "Kp",
          "type": "number"
        },
        "ki": {
          "default": 0,
          "title": "Ki",
          "type": "number"
        },
        "kd": {
          "default": 0,
          "title": "Kd",
          "type": "number"
        },
        "tt_s": {
          "default": 0.0,
          "title": "Tt S",
          "type": "number"
        },
        "b": {
          "default": 1.0,
          "title": "B",
          "type": "number"
        },
        "n": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "N"
        }
      },
      "title": "PIDConfig",
      "type": "object"
    },
    "Permissive": {
      "additionalProperties": false,
      "description": "A demand's permissive: writes to it are refused unless `signal`'s value is in the band.\n\n`above` and `below` bound the value (either or both); a value with no reading, or no\nvalue (`stale`, `invalid`, `pending`), refuses -- a permissive fails closed. A write\nof the demand's resolved stop value, and a stop itself, are never refused.",
      "properties": {
        "signal": {
          "description": "The address whose value permits the write.",
          "title": "Signal",
          "type": "string"
        },
        "above": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Permit only above this value.",
          "title": "Above"
        },
        "below": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Permit only below this value.",
          "title": "Below"
        }
      },
      "required": [
        "signal"
      ],
      "title": "Permissive",
      "type": "object"
    },
    "PlantConfigTagged": {
      "additionalProperties": false,
      "properties": {
        "model": {
          "default": "lag",
          "enum": [
            "lag",
            "integrator",
            "fopdt"
          ],
          "title": "Model",
          "type": "string"
        },
        "tau_s": {
          "default": 10.0,
          "description": "Time constant (lag, fopdt).",
          "exclusiveMinimum": 0,
          "title": "Tau S",
          "type": "number"
        },
        "dead_time_s": {
          "default": 0.0,
          "description": "Dead time (fopdt).",
          "minimum": 0,
          "title": "Dead Time S",
          "type": "number"
        },
        "gain": {
          "default": 1.0,
          "title": "Gain",
          "type": "number"
        },
        "leak": {
          "default": 0.0,
          "description": "Drain rate (integrator).",
          "minimum": 0,
          "title": "Leak",
          "type": "number"
        },
        "ambient": {
          "default": 0.0,
          "description": "Where a lag rests with no input (lag, fopdt).",
          "live": "output",
          "title": "Ambient",
          "type": "number"
        },
        "initial": {
          "default": 0.0,
          "live": "output",
          "title": "Initial",
          "type": "number"
        },
        "noise": {
          "default": 0.0,
          "description": "Gaussian noise on what is read, in the output's unit.",
          "live": "stats.noise",
          "minimum": 0,
          "title": "Noise",
          "type": "number"
        },
        "seed": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Seed"
        },
        "type": {
          "const": "sim_plant",
          "default": "sim_plant",
          "title": "Type",
          "type": "string"
        }
      },
      "title": "PlantConfigTagged",
      "type": "object"
    },
    "ProxyConfig": {
      "additionalProperties": false,
      "description": "`runner.front.proxy`: the identity layer in front of the front (`auth: proxy`).",
      "properties": {
        "preset": {
          "enum": [
            "tailscale",
            "authelia",
            "oauth2-proxy",
            "authentik",
            "pomerium",
            "cloudflare",
            "custom"
          ],
          "title": "Preset",
          "type": "string"
        },
        "from": {
          "anyOf": [
            {
              "const": "unix",
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Whose unsigned headers are believed: `unix` (the front's socket), or addresses and CIDRs.",
          "title": "From"
        },
        "secret_file": {
          "anyOf": [
            {
              "format": "path",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Secret File"
        },
        "team": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "cloudflare: the Access team name.",
          "title": "Team"
        },
        "issuer": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Issuer"
        },
        "audience": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Audience"
        },
        "grants": {
          "additionalProperties": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "description": "A grant (names pending D-034) -> subjects and `group:<id>`s.",
          "title": "Grants",
          "type": "object"
        },
        "user_header": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "User Header"
        },
        "groups_header": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Groups Header"
        },
        "separator": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Separator"
        },
        "jwt": {
          "anyOf": [
            {
              "$ref": "#/$defs/CustomJwt"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        }
      },
      "required": [
        "preset"
      ],
      "title": "ProxyConfig",
      "type": "object"
    },
    "PwmConfigTagged": {
      "properties": {
        "chip": {
          "default": 0,
          "minimum": 0,
          "title": "Chip",
          "type": "integer"
        },
        "root": {
          "default": "/sys/class/pwm",
          "title": "Root",
          "type": "string"
        },
        "type": {
          "const": "pwm",
          "default": "pwm",
          "title": "Type",
          "type": "string"
        }
      },
      "title": "PwmConfigTagged",
      "type": "object"
    },
    "Rate": {
      "description": "`value` per `per`. On the wire also `{\"per_minute\": 2}`: one key naming the unit.",
      "properties": {
        "value": {
          "title": "Value",
          "type": "number"
        },
        "per": {
          "$ref": "#/$defs/TimeUnit",
          "default": "second"
        }
      },
      "required": [
        "value"
      ],
      "title": "Rate",
      "type": "object"
    },
    "Reads": {
      "additionalProperties": false,
      "description": "A device's `reads:`: when failed reads put it offline, and how it retries.\n\n`fail_after` reads that raise in a row put the device `offline`; it is\nthen read again after each wait in `backoff_s` in turn, the last one\nrepeating, until a read succeeds. `give_up_after_s` stops the retries\nthat long after `offline` was raised (null: never). A key left out is\nthe runner's `reads:`, then the built-in default.",
      "properties": {
        "fail_after": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Reads that raise in a row before the device is offline.",
          "title": "Fail After"
        },
        "backoff_s": {
          "anyOf": [
            {
              "items": {
                "type": "number"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Seconds between retries while offline, in turn; the last repeats.",
          "title": "Backoff S"
        },
        "give_up_after_s": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Stop retrying this long after the device went offline; null: never.",
          "title": "Give Up After S"
        }
      },
      "title": "Reads",
      "type": "object"
    },
    "RunnerConfig": {
      "additionalProperties": false,
      "description": "The `runner:` section: how the process serves, not what the rig is.\n\nEverything here is fixed for the life of the process and says nothing\nabout the equipment, so it may live in the rig file or in a file of its\nown that layers with it. A command-line flag overrides a value here.\nA path is relative to the first rig file's directory.",
      "properties": {
        "host": {
          "default": "127.0.0.1",
          "description": "Bind address; loopback unless reachable.",
          "title": "Host",
          "type": "string"
        },
        "port": {
          "default": 8000,
          "title": "Port",
          "type": "integer"
        },
        "log_level": {
          "default": "info",
          "title": "Log Level",
          "type": "string"
        },
        "store": {
          "anyOf": [
            {
              "format": "path",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Sessions and versions; default <rig>.sqlite beside the file.",
          "title": "Store"
        },
        "store_dir": {
          "anyOf": [
            {
              "format": "path",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Where stores live, one per rig by name: <dir>/<name>.sqlite.",
          "title": "Store Dir"
        },
        "programs": {
          "anyOf": [
            {
              "format": "path",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Program files to import; default programs/ beside the file.",
          "title": "Programs"
        },
        "tunings": {
          "anyOf": [
            {
              "format": "path",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Control-law configs; default tunings/ beside the file.",
          "title": "Tunings"
        },
        "drivers": {
          "anyOf": [
            {
              "format": "path",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Driver .py files; default drivers/ beside the file.",
          "title": "Drivers"
        },
        "auth": {
          "$ref": "#/$defs/AuthConfig",
          "description": "Who may reach a bare runner (no front): token, anonymous."
        },
        "front": {
          "anyOf": [
            {
              "$ref": "#/$defs/FrontConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "How `flyball run`'s front serves the rig: listen, auth, url, tls. Read by the front only; a runner the front started ignores `host`, `port` and `auth`."
        },
        "compose": {
          "default": false,
          "description": "Build up a hardware rig over the API.",
          "title": "Compose",
          "type": "boolean"
        },
        "mcp": {
          "default": true,
          "description": "Mount the MCP servers at /mcp.",
          "title": "Mcp",
          "type": "boolean"
        },
        "root_path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Serve under this path prefix.",
          "title": "Root Path"
        },
        "allow_save": {
          "default": false,
          "description": "Let the API write rig files: a save to a path, a sim save.",
          "title": "Allow Save",
          "type": "boolean"
        },
        "allow_shutdown": {
          "default": false,
          "description": "Let the API stop or restart the runner.",
          "title": "Allow Shutdown",
          "type": "boolean"
        },
        "on_shutdown": {
          "default": "stop",
          "description": "What the runner's shutdown does to outputs: stop (each device's resolved stop, best-effort, not latched) or keep (writes nothing: outputs stay energised with no process watching them). A device's own `on_shutdown: keep` wins over stop.",
          "enum": [
            "stop",
            "keep"
          ],
          "title": "On Shutdown",
          "type": "string"
        },
        "reads": {
          "$ref": "#/$defs/RunnerReads",
          "description": "When failed reads put a device offline, and how it retries; a device's own `reads:` wins."
        },
        "keep": {
          "default": "1h",
          "description": "How much the scratch record holds while nothing is recorded, in the rig's clock (`1h`, `30m`); `0` keeps none.",
          "title": "Keep",
          "type": "string"
        },
        "keep_size": {
          "default": "256MB",
          "description": "The most the scratch record may take on disk; the oldest goes first.",
          "title": "Keep Size",
          "type": "string"
        },
        "retain": {
          "default": "0",
          "description": "Delete an unpinned session this long after it ended (`30d`); `0` keeps all.",
          "title": "Retain",
          "type": "string"
        },
        "rotate": {
          "default": "0",
          "description": "Close a recording at this length and continue it in a new session (`24h`); `0` never rotates.",
          "title": "Rotate",
          "type": "string"
        },
        "max_store": {
          "default": "0",
          "description": "Keep the store under this size by deleting the oldest data, of any kind, never pinned (`20GB`); `0` sets no cap.",
          "title": "Max Store",
          "type": "string"
        },
        "run": {
          "additionalProperties": true,
          "deprecated": true,
          "description": "Deprecated: `runner.front`. Accepted for one release: `serve_ui` is read as `front.listen`, `uv` as `front.uv`, with a warning.",
          "title": "Run",
          "type": "object"
        }
      },
      "title": "RunnerConfig",
      "type": "object"
    },
    "RunnerReads": {
      "additionalProperties": false,
      "description": "`runner.reads`: the rig's default for when failed reads put a device offline.\n\nA device's own `reads:` wins key by key. `give_up_after_s` is a device's\nonly: rig-wide, a device retries for ever.",
      "properties": {
        "fail_after": {
          "default": 3,
          "description": "Reads that raise in a row before a device is offline.",
          "title": "Fail After",
          "type": "integer"
        },
        "backoff_s": {
          "description": "Seconds between retries while offline, in turn; the last repeats.",
          "items": {
            "type": "number"
          },
          "title": "Backoff S",
          "type": "array"
        }
      },
      "title": "RunnerReads",
      "type": "object"
    },
    "ScheduledConfig": {
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "scheduled",
          "default": "scheduled",
          "title": "Type",
          "type": "string"
        },
        "points": {
          "items": {
            "items": {
              "type": "number"
            },
            "type": "array"
          },
          "title": "Points",
          "type": "array"
        },
        "tt_s": {
          "default": 0.0,
          "title": "Tt S",
          "type": "number"
        },
        "b": {
          "default": 1.0,
          "title": "B",
          "type": "number"
        },
        "n": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "N"
        }
      },
      "required": [
        "points"
      ],
      "title": "ScheduledConfig",
      "type": "object"
    },
    "SerialConfigTagged": {
      "properties": {
        "port": {
          "title": "Port",
          "type": "string"
        },
        "baudrate": {
          "default": 9600,
          "exclusiveMinimum": 0,
          "title": "Baudrate",
          "type": "integer"
        },
        "timeout": {
          "default": 1.0,
          "exclusiveMinimum": 0,
          "title": "Timeout",
          "type": "number"
        },
        "type": {
          "const": "uart",
          "default": "uart",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "port"
      ],
      "title": "SerialConfigTagged",
      "type": "object"
    },
    "SerialLinkConfigTagged": {
      "properties": {
        "port": {
          "title": "Port",
          "type": "string"
        },
        "baud": {
          "default": 9600,
          "title": "Baud",
          "type": "integer"
        },
        "terminator": {
          "default": "\n",
          "title": "Terminator",
          "type": "string"
        },
        "timeout_s": {
          "default": 1.0,
          "title": "Timeout S",
          "type": "number"
        },
        "type": {
          "const": "serial",
          "default": "serial",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "port"
      ],
      "title": "SerialLinkConfigTagged",
      "type": "object"
    },
    "SignalMeta": {
      "additionalProperties": false,
      "description": "A signal's metadata in the rig file, and the access to remove.\n\n`access` names the set to keep (`\"r\"`); `readable`, `published` and\n`writable` drop one flag each and take only `false` -- the driver\ndeclares what it can honour, the file cannot add to it. `limits` only\nnarrows the driver's: a demand is clamped to both.\n\nA key left out leaves the driver's value; a key given as `null` clears\nit back to the unset default (`label: null` is the titlecased name,\n`warning: null` no band). `limits: null` clears only the file's narrowing,\nnever the driver's limits.",
      "properties": {
        "label": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Label"
        },
        "range": {
          "anyOf": [
            {
              "$ref": "#/$defs/Bounds"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "precision": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Precision"
        },
        "warning": {
          "anyOf": [
            {
              "$ref": "#/$defs/Bounds"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "alarm": {
          "anyOf": [
            {
              "$ref": "#/$defs/Bounds"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "poll_s": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Poll S"
        },
        "stale_after_s": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Stale After S"
        },
        "limits": {
          "anyOf": [
            {
              "$ref": "#/$defs/Bounds"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "max_rate": {
          "anyOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/Rate"
                },
                {
                  "additionalProperties": false,
                  "maxProperties": 1,
                  "minProperties": 1,
                  "properties": {
                    "per_nanosecond": {
                      "type": "number"
                    },
                    "per_microsecond": {
                      "type": "number"
                    },
                    "per_millisecond": {
                      "type": "number"
                    },
                    "per_second": {
                      "type": "number"
                    },
                    "per_minute": {
                      "type": "number"
                    },
                    "per_hour": {
                      "type": "number"
                    },
                    "per_day": {
                      "type": "number"
                    }
                  },
                  "type": "object"
                }
              ],
              "title": "Rate"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "on_no_value": {
          "anyOf": [
            {
              "$ref": "#/$defs/OnNoValue"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "A banded signal with no value because of a fault: fire (band_unknown) or ignore. Default: fire with an alarm band, ignore with only a warning band."
        },
        "tags": {
          "anyOf": [
            {
              "additionalProperties": {
                "type": "string"
              },
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Tags"
        },
        "record": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "false: left out of a recording started with the default selection (a raw value a derived signal is computed from). Default: recorded.",
          "title": "Record"
        },
        "access": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Access"
        },
        "readable": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Readable"
        },
        "published": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Published"
        },
        "writable": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Writable"
        }
      },
      "title": "SignalMeta",
      "type": "object"
    },
    "SlidingModeConfig": {
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "sliding",
          "default": "sliding",
          "title": "Type",
          "type": "string"
        },
        "k": {
          "title": "K",
          "type": "number"
        },
        "lam": {
          "title": "Lam",
          "type": "number"
        },
        "boundary": {
          "title": "Boundary",
          "type": "number"
        }
      },
      "required": [
        "k",
        "lam",
        "boundary"
      ],
      "title": "SlidingModeConfig",
      "type": "object"
    },
    "SmithPredictorConfig": {
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "smith",
          "default": "smith",
          "title": "Type",
          "type": "string"
        },
        "kp": {
          "title": "Kp",
          "type": "number"
        },
        "ki": {
          "title": "Ki",
          "type": "number"
        },
        "gain": {
          "title": "Gain",
          "type": "number"
        },
        "tau_s": {
          "title": "Tau S",
          "type": "number"
        },
        "dead_time_s": {
          "title": "Dead Time S",
          "type": "number"
        },
        "tt_s": {
          "default": 0.0,
          "title": "Tt S",
          "type": "number"
        },
        "feedforward": {
          "default": 1.0,
          "title": "Feedforward",
          "type": "number"
        },
        "b": {
          "default": 1.0,
          "title": "B",
          "type": "number"
        }
      },
      "required": [
        "kp",
        "ki",
        "gain",
        "tau_s",
        "dead_time_s"
      ],
      "title": "SmithPredictorConfig",
      "type": "object"
    },
    "SpiConfigTagged": {
      "properties": {
        "bus": {
          "minimum": 0,
          "title": "Bus",
          "type": "integer"
        },
        "chip_select": {
          "minimum": 0,
          "title": "Chip Select",
          "type": "integer"
        },
        "speed_hz": {
          "default": 1000000,
          "exclusiveMinimum": 0,
          "title": "Speed Hz",
          "type": "integer"
        },
        "spi_mode": {
          "default": 0,
          "maximum": 3,
          "minimum": 0,
          "title": "Spi Mode",
          "type": "integer"
        },
        "type": {
          "const": "spi",
          "default": "spi",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "bus",
        "chip_select"
      ],
      "title": "SpiConfigTagged",
      "type": "object"
    },
    "TableConfig": {
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "table",
          "default": "table",
          "title": "Type",
          "type": "string"
        },
        "points": {
          "items": {
            "maxItems": 2,
            "minItems": 2,
            "prefixItems": [
              {
                "type": "number"
              },
              {
                "type": "number"
              }
            ],
            "type": "array"
          },
          "title": "Points",
          "type": "array"
        },
        "rate_gain": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Rate Gain"
        }
      },
      "required": [
        "points"
      ],
      "title": "TableConfig",
      "type": "object"
    },
    "TimeUnit": {
      "description": "The unit a rate is expressed per.",
      "oneOf": [
        {
          "const": "nanosecond",
          "title": "per nanosecond"
        },
        {
          "const": "microsecond",
          "title": "per microsecond"
        },
        {
          "const": "millisecond",
          "title": "per millisecond"
        },
        {
          "const": "second",
          "title": "per second"
        },
        {
          "const": "minute",
          "title": "per minute"
        },
        {
          "const": "hour",
          "title": "per hour"
        },
        {
          "const": "day",
          "title": "per day"
        }
      ],
      "title": "TimeUnit",
      "type": "string"
    },
    "TlsFiles": {
      "additionalProperties": false,
      "description": "`runner.front.tls`: the certificate the front serves, reloaded when renewed.",
      "properties": {
        "cert": {
          "format": "path",
          "title": "Cert",
          "type": "string"
        },
        "key": {
          "format": "path",
          "title": "Key",
          "type": "string"
        }
      },
      "required": [
        "cert",
        "key"
      ],
      "title": "TlsFiles",
      "type": "object"
    },
    "TokensConfig": {
      "additionalProperties": false,
      "description": "`runner.front.tokens` (or flyballd.yaml's top-level `tokens:`): named-token lifetimes.\n\nRead and validated by the front (Go, `daemon/internal/front.ResolveLifetimes`); this side\nonly shapes the block and forbids unknown keys. An unparseable, out-of-range or\notherwise invalid value falls back to the built-in, with a warning at start -- it never\nstops the runner (D-028).",
      "properties": {
        "default_lifetime": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "A token's lifetime when created without an explicit `expires_in`. A Go duration plus a `d` suffix for days (e.g. `90d`, `36h`). Unset: the built-in 90 days.",
          "title": "Default Lifetime"
        },
        "max_lifetime": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The hard cap on a token's lifetime, for tokens that are neither cleartext nor kind `agent` (those keep a fixed 30-day cap, tightened further if this is smaller). May only tighten the built-in ceiling of 365 days, never loosen it. Unset: the built-in 365 days.",
          "title": "Max Lifetime"
        }
      },
      "title": "TokensConfig",
      "type": "object"
    },
    "VisaLinkConfigTagged": {
      "properties": {
        "resource": {
          "title": "Resource",
          "type": "string"
        },
        "timeout_ms": {
          "default": 2000,
          "title": "Timeout Ms",
          "type": "integer"
        },
        "backend": {
          "default": "@py",
          "title": "Backend",
          "type": "string"
        },
        "type": {
          "const": "visa",
          "default": "visa",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "resource"
      ],
      "title": "VisaLinkConfigTagged",
      "type": "object"
    },
    "Channel": {
      "additionalProperties": false,
      "description": "One input as a signal: which pin, what it measures, and how volts become that.",
      "properties": {
        "channel": {
          "maximum": 7,
          "minimum": 0,
          "title": "Channel",
          "type": "integer"
        },
        "quantity": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The quantity's own name, if it differs from the signal's.",
          "title": "Quantity"
        },
        "unit": {
          "default": "V",
          "title": "Unit",
          "type": "string"
        },
        "scale": {
          "default": 1.0,
          "description": "Signal units per volt.",
          "title": "Scale",
          "type": "number"
        },
        "offset": {
          "default": 0.0,
          "title": "Offset",
          "type": "number"
        }
      },
      "required": [
        "channel"
      ],
      "title": "Channel",
      "type": "object"
    },
    "Ads1115Config": {
      "description": "`channels: { pressure: { channel: 0, scale: 25.0, unit: kPa } }`.",
      "properties": {
        "link": {
          "anyOf": [
            {
              "discriminator": {
                "mapping": {
                  "fake_i2c": "#/$defs/FakeI2cConfigTagged"
                },
                "propertyName": "type"
              },
              "oneOf": [
                {
                  "$ref": "#/$defs/FakeI2cConfigTagged"
                }
              ]
            },
            {
              "type": "string"
            }
          ],
          "title": "Link"
        },
        "channels": {
          "additionalProperties": {
            "$ref": "#/$defs/flyball_chips__ads1115__Channel"
          },
          "title": "Channels",
          "type": "object"
        },
        "i2c_address": {
          "default": 72,
          "maximum": 75,
          "minimum": 72,
          "title": "I2C Address",
          "type": "integer"
        },
        "gain": {
          "default": 1,
          "description": "PGA gain: 2/3, 1, 2, 4, 8 or 16.",
          "title": "Gain",
          "type": "number"
        }
      },
      "required": [
        "link",
        "channels"
      ],
      "title": "Ads1115Config",
      "type": "object"
    },
    "CurrentLoopChannel": {
      "additionalProperties": false,
      "description": "One instrument on one ADC channel: the sense resistor, and the 4-20mA span.\n\n`value = mA * scale + offset`, the same shape as\n[Register][flyball_linux.devices.i2c_table.Register]'s `scale`/`offset`,\nwith milliamps standing in for Register's raw counts. For \"4 mA = 0 ppm\nO2, 20 mA = 25% O2\": `scale = 25.0 / 16.0`, `offset = -4 * scale`, i.e.\n`scale=1.5625, offset=-6.25`.",
      "properties": {
        "channel": {
          "description": "The channel on the wrapped ADC.",
          "minimum": 0,
          "title": "Channel",
          "type": "integer"
        },
        "quantity": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The quantity's own name, if it differs from the signal's.",
          "title": "Quantity"
        },
        "unit": {
          "default": "1",
          "title": "Unit",
          "type": "string"
        },
        "resistor_ohms": {
          "default": 250.0,
          "description": "The loop's sense resistor.",
          "exclusiveMinimum": 0,
          "title": "Resistor Ohms",
          "type": "number"
        },
        "scale": {
          "default": 1.0,
          "description": "Signal units per milliamp.",
          "title": "Scale",
          "type": "number"
        },
        "offset": {
          "default": 0.0,
          "title": "Offset",
          "type": "number"
        },
        "low_ma": {
          "default": 3.6,
          "description": "At or below this, the loop is faulted.",
          "title": "Low Ma",
          "type": "number"
        },
        "high_ma": {
          "default": 21.0,
          "description": "At or above this, the loop is faulted.",
          "title": "High Ma",
          "type": "number"
        }
      },
      "required": [
        "channel"
      ],
      "title": "CurrentLoopChannel",
      "type": "object"
    },
    "Mcp3008Config": {
      "description": "`channels: { level: { channel: 0 } }`, in volts unless `unit` and `scale` say otherwise.",
      "properties": {
        "link": {
          "anyOf": [
            {
              "discriminator": {
                "mapping": {
                  "fake_spi": "#/$defs/FakeSpiConfigTagged"
                },
                "propertyName": "type"
              },
              "oneOf": [
                {
                  "$ref": "#/$defs/FakeSpiConfigTagged"
                }
              ]
            },
            {
              "type": "string"
            }
          ],
          "title": "Link"
        },
        "channels": {
          "additionalProperties": {
            "$ref": "#/$defs/flyball_chips__mcp3008__Channel"
          },
          "title": "Channels",
          "type": "object"
        },
        "vref": {
          "default": 3.3,
          "description": "The reference voltage on VREF.",
          "exclusiveMinimum": 0,
          "title": "Vref",
          "type": "number"
        }
      },
      "required": [
        "link",
        "channels"
      ],
      "title": "Mcp3008Config",
      "type": "object"
    },
    "flyball_chips__ads1115__Channel": {
      "additionalProperties": false,
      "description": "One input as a signal: which pin, what it measures, and how volts become that.\n\nRange, precision and bands are not here -- they are the envelope's\n`signals:` metadata, the same for every driver.",
      "properties": {
        "channel": {
          "maximum": 3,
          "minimum": 0,
          "title": "Channel",
          "type": "integer"
        },
        "quantity": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The quantity's own name, if it differs from the signal's.",
          "title": "Quantity"
        },
        "unit": {
          "default": "V",
          "title": "Unit",
          "type": "string"
        },
        "scale": {
          "default": 1.0,
          "description": "Signal units per volt.",
          "title": "Scale",
          "type": "number"
        },
        "offset": {
          "default": 0.0,
          "title": "Offset",
          "type": "number"
        }
      },
      "required": [
        "channel"
      ],
      "title": "Channel",
      "type": "object"
    },
    "flyball_chips__mcp3008__Channel": {
      "additionalProperties": false,
      "description": "One input as a signal: which pin, what it measures, and how volts become that.",
      "properties": {
        "channel": {
          "maximum": 7,
          "minimum": 0,
          "title": "Channel",
          "type": "integer"
        },
        "quantity": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The quantity's own name, if it differs from the signal's.",
          "title": "Quantity"
        },
        "unit": {
          "default": "V",
          "title": "Unit",
          "type": "string"
        },
        "scale": {
          "default": 1.0,
          "description": "Signal units per volt.",
          "title": "Scale",
          "type": "number"
        },
        "offset": {
          "default": 0.0,
          "title": "Offset",
          "type": "number"
        }
      },
      "required": [
        "channel"
      ],
      "title": "Channel",
      "type": "object"
    },
    "GpioLineConfig": {
      "description": "`driver: gpio_line`: `{ link, line }`, or `pin: GPIO18` from the board profile.",
      "properties": {
        "link": {
          "anyOf": [
            {
              "discriminator": {
                "mapping": {
                  "fake_gpio": "#/$defs/FakeGpioConfigTagged",
                  "gpio": "#/$defs/GpioConfigTagged"
                },
                "propertyName": "type"
              },
              "oneOf": [
                {
                  "$ref": "#/$defs/FakeGpioConfigTagged"
                },
                {
                  "$ref": "#/$defs/GpioConfigTagged"
                }
              ]
            },
            {
              "type": "string"
            }
          ],
          "title": "Link"
        },
        "line": {
          "minimum": 0,
          "title": "Line",
          "type": "integer"
        },
        "direction": {
          "default": "output",
          "enum": [
            "output",
            "input"
          ],
          "title": "Direction",
          "type": "string"
        },
        "invert": {
          "default": false,
          "description": "An active-low relay board or switch.",
          "title": "Invert",
          "type": "boolean"
        },
        "initial": {
          "default": false,
          "description": "An output's level at startup.",
          "title": "Initial",
          "type": "boolean"
        },
        "pull_up": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "An input's bias: true pulls up, false down, omitted as-is.",
          "title": "Pull Up"
        }
      },
      "required": [
        "link",
        "line"
      ],
      "title": "GpioLineConfig",
      "type": "object"
    },
    "PwmChannelConfig": {
      "description": "`driver: pwm_channel`: `{ link, channel }`, or `pin: PWM0` from the board profile.",
      "properties": {
        "link": {
          "anyOf": [
            {
              "discriminator": {
                "mapping": {
                  "fake_pwm": "#/$defs/FakePwmConfigTagged",
                  "pwm": "#/$defs/PwmConfigTagged"
                },
                "propertyName": "type"
              },
              "oneOf": [
                {
                  "$ref": "#/$defs/FakePwmConfigTagged"
                },
                {
                  "$ref": "#/$defs/PwmConfigTagged"
                }
              ]
            },
            {
              "type": "string"
            }
          ],
          "title": "Link"
        },
        "channel": {
          "minimum": 0,
          "title": "Channel",
          "type": "integer"
        },
        "frequency_hz": {
          "default": 1000.0,
          "exclusiveMinimum": 0,
          "title": "Frequency Hz",
          "type": "number"
        },
        "invert": {
          "default": false,
          "title": "Invert",
          "type": "boolean"
        },
        "unit": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The unit `drive` is set in; omitted, it is the duty itself.",
          "title": "Unit"
        },
        "quantity": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "With `unit`: what `drive` then is ('temperature').",
          "title": "Quantity"
        },
        "span": {
          "anyOf": [
            {
              "$ref": "#/$defs/Bounds"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "With `unit`: the value meaning 0 % and the one meaning 100 %."
        }
      },
      "required": [
        "link",
        "channel"
      ],
      "title": "PwmChannelConfig",
      "type": "object"
    },
    "Register": {
      "additionalProperties": false,
      "description": "Where a value lives and how it converts: one line of an `i2c_table` device's tree.",
      "properties": {
        "address": {
          "description": "The register address on the chip.",
          "maximum": 255,
          "minimum": 0,
          "title": "Address",
          "type": "integer"
        },
        "length": {
          "default": 2,
          "description": "Bytes.",
          "maximum": 8,
          "minimum": 1,
          "title": "Length",
          "type": "integer"
        },
        "signed": {
          "default": false,
          "title": "Signed",
          "type": "boolean"
        },
        "byteorder": {
          "default": "big",
          "enum": [
            "big",
            "little"
          ],
          "title": "Byteorder",
          "type": "string"
        },
        "shift": {
          "default": 0,
          "description": "Right-shift the raw value first: 12-bit left-justified ADCs.",
          "minimum": 0,
          "title": "Shift",
          "type": "integer"
        },
        "scale": {
          "default": 1.0,
          "title": "Scale",
          "type": "number"
        },
        "offset": {
          "default": 0.0,
          "title": "Offset",
          "type": "number"
        },
        "unit": {
          "default": "1",
          "title": "Unit",
          "type": "string"
        },
        "quantity": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The quantity's own name, if it differs from the signal's.",
          "title": "Quantity"
        },
        "write": {
          "default": false,
          "description": "Also a demand: a DAC output, a setpoint.",
          "title": "Write",
          "type": "boolean"
        },
        "role": {
          "anyOf": [
            {
              "const": "setting",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`setting`: a writable entry that changes how the instrument behaves (a range, a frequency, a configuration register), not what controls the process; a controller cannot drive it. Omitted: a writable entry is a demand.",
          "title": "Role"
        }
      },
      "required": [
        "address"
      ],
      "title": "Register",
      "type": "object"
    },
    "ModbusRegister": {
      "additionalProperties": false,
      "description": "One line of a `modbus` device's tree: where a value lives, and how it converts.\n\n`value = raw * scale`. Every register is readable and published; `write`\nalso makes it writable. `input` registers are read-only on real hardware,\nso `write` on one is refused here too.",
      "properties": {
        "address": {
          "title": "Address",
          "type": "integer"
        },
        "kind": {
          "default": "holding",
          "enum": [
            "holding",
            "input",
            "coil"
          ],
          "title": "Kind",
          "type": "string"
        },
        "unit": {
          "title": "Unit",
          "type": "string"
        },
        "scale": {
          "default": 1.0,
          "title": "Scale",
          "type": "number"
        },
        "write": {
          "default": false,
          "title": "Write",
          "type": "boolean"
        },
        "role": {
          "anyOf": [
            {
              "const": "setting",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`setting`: a writable entry that changes how the instrument behaves (a range, a frequency, a configuration register), not what controls the process; a controller cannot drive it. Omitted: a writable entry is a demand.",
          "title": "Role"
        }
      },
      "required": [
        "address",
        "unit"
      ],
      "title": "ModbusRegister",
      "type": "object"
    },
    "PyMeasureSignal": {
      "additionalProperties": false,
      "description": "One line of a `pymeasure` device's tree: one property, wrapped as a signal.\n\nA property with a setter is a demand (`RPW`: its readback is the value\nlast set, whether or not the property also has a getter); a\ngetter-only one is an output, readable on demand. `publish` streams a\ngetter-only one too, and needs a getter -- see\n[PyMeasure][flyball_pymeasure.PyMeasure].",
      "properties": {
        "property": {
          "description": "The instrument's attribute name.",
          "title": "Property",
          "type": "string"
        },
        "unit": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Overrides the docstring's unit.",
          "title": "Unit"
        },
        "publish": {
          "default": false,
          "title": "Publish",
          "type": "boolean"
        },
        "role": {
          "anyOf": [
            {
              "const": "setting",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`setting`: a writable entry that changes how the instrument behaves (a range, a frequency, a configuration register), not what controls the process; a controller cannot drive it. Omitted: a writable entry is a demand.",
          "title": "Role"
        }
      },
      "required": [
        "property"
      ],
      "title": "PyMeasureSignal",
      "type": "object"
    },
    "QCoDeSSignal": {
      "additionalProperties": false,
      "description": "One line of a `qcodes` device's tree: one parameter, wrapped as a signal.\n\nA settable parameter is a demand (`RPW`: its readback is the value last\nset, whether or not it can be read back from the instrument); a\ngettable-only one is an output, readable on demand. `publish` streams a\ngettable-only one too, and needs a getter -- see\n[QCoDeS][flyball_qcodes.QCoDeS].",
      "properties": {
        "property": {
          "description": "The parameter's name, dotted for a submodule: 'source.voltage'.",
          "title": "Property",
          "type": "string"
        },
        "unit": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Overrides the parameter's own `unit`.",
          "title": "Unit"
        },
        "publish": {
          "default": false,
          "title": "Publish",
          "type": "boolean"
        },
        "role": {
          "anyOf": [
            {
              "const": "setting",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`setting`: a writable entry that changes how the instrument behaves (a range, a frequency, a configuration register), not what controls the process; a controller cannot drive it. Omitted: a writable entry is a demand.",
          "title": "Role"
        }
      },
      "required": [
        "property"
      ],
      "title": "QCoDeSSignal",
      "type": "object"
    },
    "ScpiSignal": {
      "additionalProperties": false,
      "description": "One line of a `scpi` device's tree: a query, a write template, or both.\n\n`query` alone: an output, `[RP]`. `write`, with or without `query`: a\ndemand, `[RPW]` -- its readback is the value last committed. Metadata\nsuch as range, precision and limits are not here -- they are the\nenvelope's `signals:` metadata, which applies to any driver's\ntree.",
      "properties": {
        "query": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "A query; the reply is this signal's value.",
          "title": "Query"
        },
        "write": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "A command with `{value}` formatted in, sent on commit.",
          "title": "Write"
        },
        "unit": {
          "title": "Unit",
          "type": "string"
        },
        "quantity": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The quantity's own name, if it differs from the signal's.",
          "title": "Quantity"
        },
        "scale": {
          "default": 1.0,
          "description": "A reply or a demand is multiplied/divided by this.",
          "title": "Scale",
          "type": "number"
        },
        "role": {
          "anyOf": [
            {
              "const": "setting",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`setting`: a writable entry that changes how the instrument behaves (a range, a frequency, a configuration register), not what controls the process; a controller cannot drive it. Omitted: a writable entry is a demand.",
          "title": "Role"
        }
      },
      "required": [
        "unit"
      ],
      "title": "ScpiSignal",
      "type": "object"
    },
    "SensorEntry": {
      "additionalProperties": false,
      "description": "One sensor of an `sht4x_set`: its I2C address.",
      "properties": {
        "i2c_address": {
          "default": 68,
          "maximum": 119,
          "minimum": 3,
          "title": "I2C Address",
          "type": "integer"
        }
      },
      "title": "SensorEntry",
      "type": "object"
    },
    "DaqPort": {
      "additionalProperties": false,
      "description": "A `sim_daq` signal spelled out: which port, and what it measures if the plant cannot say.",
      "properties": {
        "port": {
          "title": "Port",
          "type": "string"
        },
        "quantity": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "What the output is: 'temperature', 'level'.",
          "title": "Quantity"
        },
        "unit": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Unit"
        },
        "label": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Display text: 'Dry line humidity'.",
          "title": "Label"
        },
        "tags": {
          "anyOf": [
            {
              "additionalProperties": {
                "type": "string"
              },
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Groupings across the tree, `{axis: name}`: `{line: dry}`.",
          "title": "Tags"
        },
        "range": {
          "anyOf": [
            {
              "$ref": "#/$defs/Bounds"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "precision": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Precision"
        },
        "warning": {
          "anyOf": [
            {
              "$ref": "#/$defs/Bounds"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The band a value is normal inside; outside it, a warning."
        },
        "alarm": {
          "anyOf": [
            {
              "$ref": "#/$defs/Bounds"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The band a value is acceptable inside; outside it, an alarm."
        }
      },
      "required": [
        "port"
      ],
      "title": "DaqPort",
      "type": "object"
    },
    "PlantConfig": {
      "additionalProperties": false,
      "description": "A plant model with one input and one output, shared by the devices that use it.\n\nIts ports are `input` (a drive, 0 to 1 of full) and `output`, whose\nquantity the `sim_daq` reading it says, since a lag may be an oven or a\npressure vessel. An unknown key is refused, so the old `kind` does not\nsilently mean the default model.",
      "properties": {
        "model": {
          "default": "lag",
          "enum": [
            "lag",
            "integrator",
            "fopdt"
          ],
          "title": "Model",
          "type": "string"
        },
        "tau_s": {
          "default": 10.0,
          "description": "Time constant (lag, fopdt).",
          "exclusiveMinimum": 0,
          "title": "Tau S",
          "type": "number"
        },
        "dead_time_s": {
          "default": 0.0,
          "description": "Dead time (fopdt).",
          "minimum": 0,
          "title": "Dead Time S",
          "type": "number"
        },
        "gain": {
          "default": 1.0,
          "title": "Gain",
          "type": "number"
        },
        "leak": {
          "default": 0.0,
          "description": "Drain rate (integrator).",
          "minimum": 0,
          "title": "Leak",
          "type": "number"
        },
        "ambient": {
          "default": 0.0,
          "description": "Where a lag rests with no input (lag, fopdt).",
          "live": "output",
          "title": "Ambient",
          "type": "number"
        },
        "initial": {
          "default": 0.0,
          "live": "output",
          "title": "Initial",
          "type": "number"
        },
        "noise": {
          "default": 0.0,
          "description": "Gaussian noise on what is read, in the output's unit.",
          "live": "stats.noise",
          "minimum": 0,
          "title": "Noise",
          "type": "number"
        },
        "seed": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Seed"
        }
      },
      "title": "PlantConfig",
      "type": "object"
    },
    "PlantLink": {
      "anyOf": [
        {
          "$ref": "#/$defs/PlantConfig"
        },
        {
          "type": "string"
        }
      ]
    },
    "DrivePort": {
      "additionalProperties": false,
      "description": "A `sim_drive` signal spelled out: which port, and the unit and limits it is set in.\n\nIn the default `demand: input` form the signal is declared in that unit\nwith those limits, so an overlay can mirror a real device's writable\nsignal unit for unit (`blender.humidity [W] %RH 0..100`); a demand is\nmapped linearly over `limits` onto the port's 0..1 drive.\n\n`demand: output` instead declares the signal in the plant's own output\nunit -- what a `sim_daq` reading the same plant would read -- and\n`commit` inverts the plant's static model to find the drive, so a\ncontroller can hand it the setpoint directly (the `setpoint`\nfeedforward). `quantity`/`unit` may be omitted when the plant knows its\noutput (a furnace zone); `limits` may be omitted when the plant's model\nhas a static inverse (`Lag`, `Fopdt`; not `Integrator`) -- otherwise\nboth must be given.",
      "properties": {
        "port": {
          "title": "Port",
          "type": "string"
        },
        "demand": {
          "default": "input",
          "enum": [
            "input",
            "output"
          ],
          "title": "Demand",
          "type": "string"
        },
        "label": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Display text: 'Zone 1 heater'.",
          "title": "Label"
        },
        "tags": {
          "anyOf": [
            {
              "additionalProperties": {
                "type": "string"
              },
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Groupings across the tree, `{axis: name}`: `{zone: 1}`.",
          "title": "Tags"
        },
        "quantity": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "What is set: 'humidity', 'power' -- or, in output mode, what the plant's output measures, if it cannot say.",
          "title": "Quantity"
        },
        "unit": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Unit"
        },
        "limits": {
          "anyOf": [
            {
              "$ref": "#/$defs/Bounds"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Input mode: what maps onto the port's drive, `limits[0]` off, `limits[1]` full. Output mode: the deliverable span, defaulted from the plant's model if it has one."
        }
      },
      "required": [
        "port"
      ],
      "title": "DrivePort",
      "type": "object"
    },
    "ValueEntry": {
      "additionalProperties": false,
      "description": "One operator-entered value: its starting number, unit and what it may be set to.",
      "properties": {
        "initial": {
          "description": "Its value from build, and after a restart unless one was written since while this stayed the same.",
          "title": "Initial",
          "type": "number"
        },
        "unit": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The unit symbol (`%`, `\u00b0C`, `L/min`); omit for none.",
          "title": "Unit"
        },
        "quantity": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "What it is (`humidity`); default: the value's name.",
          "title": "Quantity"
        },
        "label": {
          "default": "",
          "description": "The display text; default: the titled name.",
          "title": "Label",
          "type": "string"
        },
        "limits": {
          "anyOf": [
            {
              "$ref": "#/$defs/Bounds"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "What a write is clamped to, `[low, high]`, in its unit."
        }
      },
      "required": [
        "initial"
      ],
      "title": "ValueEntry",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "name": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Name"
    },
    "board": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "A board profile: a name on the board path, or a path to the file.",
      "title": "Board"
    },
    "recording": {
      "default": false,
      "description": "Open a recording session when the runner starts.",
      "title": "Recording",
      "type": "boolean"
    },
    "clock": {
      "anyOf": [
        {
          "$ref": "#/$defs/ClockEntry"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Run the rig's time faster, or stepped; simulated rigs only."
    },
    "links": {
      "additionalProperties": {
        "discriminator": {
          "mapping": {
            "fake_gpio": "#/$defs/FakeGpioConfigTagged",
            "fake_i2c": "#/$defs/FakeI2cConfigTagged",
            "fake_onewire": "#/$defs/FakeOneWireConfigTagged",
            "fake_pwm": "#/$defs/FakePwmConfigTagged",
            "fake_registers": "#/$defs/FakeRegisterLinkConfigTagged",
            "fake_spi": "#/$defs/FakeSpiConfigTagged",
            "fake_text": "#/$defs/FakeTextLinkConfigTagged",
            "fake_uart": "#/$defs/FakeUartConfigTagged",
            "gpio": "#/$defs/GpioConfigTagged",
            "i2c": "#/$defs/I2cConfigTagged",
            "modbus_rtu": "#/$defs/ModbusRtuConfigTagged",
            "modbus_tcp": "#/$defs/ModbusTcpConfigTagged",
            "onewire": "#/$defs/OneWireConfigTagged",
            "pwm": "#/$defs/PwmConfigTagged",
            "serial": "#/$defs/SerialLinkConfigTagged",
            "sim_furnace": "#/$defs/FurnaceConfigTagged",
            "sim_plant": "#/$defs/PlantConfigTagged",
            "spi": "#/$defs/SpiConfigTagged",
            "uart": "#/$defs/SerialConfigTagged",
            "visa": "#/$defs/VisaLinkConfigTagged"
          },
          "propertyName": "type"
        },
        "oneOf": [
          {
            "$ref": "#/$defs/FakeGpioConfigTagged"
          },
          {
            "$ref": "#/$defs/FakeI2cConfigTagged"
          },
          {
            "$ref": "#/$defs/FakeOneWireConfigTagged"
          },
          {
            "$ref": "#/$defs/FakePwmConfigTagged"
          },
          {
            "$ref": "#/$defs/FakeRegisterLinkConfigTagged"
          },
          {
            "$ref": "#/$defs/FakeSpiConfigTagged"
          },
          {
            "$ref": "#/$defs/FakeTextLinkConfigTagged"
          },
          {
            "$ref": "#/$defs/FakeUartConfigTagged"
          },
          {
            "$ref": "#/$defs/GpioConfigTagged"
          },
          {
            "$ref": "#/$defs/I2cConfigTagged"
          },
          {
            "$ref": "#/$defs/ModbusRtuConfigTagged"
          },
          {
            "$ref": "#/$defs/ModbusTcpConfigTagged"
          },
          {
            "$ref": "#/$defs/OneWireConfigTagged"
          },
          {
            "$ref": "#/$defs/PwmConfigTagged"
          },
          {
            "$ref": "#/$defs/SerialLinkConfigTagged"
          },
          {
            "$ref": "#/$defs/FurnaceConfigTagged"
          },
          {
            "$ref": "#/$defs/PlantConfigTagged"
          },
          {
            "$ref": "#/$defs/SpiConfigTagged"
          },
          {
            "$ref": "#/$defs/SerialConfigTagged"
          },
          {
            "$ref": "#/$defs/VisaLinkConfigTagged"
          },
          {
            "type": "null",
            "title": "Removed by this layer"
          }
        ]
      },
      "title": "Links",
      "type": "object"
    },
    "devices": {
      "additionalProperties": {
        "oneOf": [
          {
            "type": "object",
            "title": "ads1115",
            "description": "`channels: { pressure: { channel: 0, scale: 25.0, unit: kPa } }`.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "ads1115"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_i2c": "#/$defs/FakeI2cConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeI2cConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "channels": {
                "additionalProperties": {
                  "$ref": "#/$defs/Channel"
                },
                "title": "Channels",
                "type": "object"
              },
              "i2c_address": {
                "default": 72,
                "maximum": 75,
                "minimum": 72,
                "title": "I2C Address",
                "type": "integer"
              },
              "gain": {
                "default": 1,
                "description": "PGA gain: 2/3, 1, 2, 4, 8 or 16.",
                "title": "Gain",
                "type": "number"
              }
            },
            "required": [
              "driver",
              "link",
              "channels"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "bme280",
            "description": "`has_humidity: false` for a BMP280 (no humidity registers or calibration).",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "bme280"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_i2c": "#/$defs/FakeI2cConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeI2cConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "i2c_address": {
                "default": 118,
                "maximum": 119,
                "minimum": 3,
                "title": "I2C Address",
                "type": "integer"
              },
              "has_humidity": {
                "default": true,
                "title": "Has Humidity",
                "type": "boolean"
              },
              "osrs_t": {
                "default": 1,
                "title": "Osrs T",
                "type": "integer"
              },
              "osrs_p": {
                "default": 1,
                "title": "Osrs P",
                "type": "integer"
              },
              "osrs_h": {
                "default": 1,
                "title": "Osrs H",
                "type": "integer"
              }
            },
            "required": [
              "driver",
              "link"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "ccs811",
            "description": "One chip by its I2C address.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "ccs811"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_i2c": "#/$defs/FakeI2cConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeI2cConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "i2c_address": {
                "default": 90,
                "maximum": 119,
                "minimum": 3,
                "title": "I2C Address",
                "type": "integer"
              }
            },
            "required": [
              "driver",
              "link"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "current_loop",
            "description": "`driver: current_loop`. Wraps an `ads1115`/`mcp3008` channel; `channels` is its own tree.\n\n```yaml\noxygen:\n  driver: current_loop\n  adc:\n    driver: ads1115\n    link: i2c1\n    i2c_address: 0x48\n  channels:\n    o2:\n      channel: 0\n      unit: \"%\"\n      resistor_ohms: 250.0\n      scale: 1.5625   # 25% over the 16 mA span\n      offset: -6.25   # so 4 mA -> 0 %, 20 mA -> 25 %\n```",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "current_loop"
              },
              "link": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "A transport (or a simulated plant), by name.",
                "title": "Link"
              },
              "adc": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Ads1115Config"
                  },
                  {
                    "$ref": "#/$defs/Mcp3008Config"
                  }
                ],
                "description": "The ADC the loop's sense-resistor voltage is read on.",
                "title": "Adc"
              },
              "channels": {
                "additionalProperties": {
                  "$ref": "#/$defs/CurrentLoopChannel"
                },
                "title": "Channels",
                "type": "object"
              }
            },
            "required": [
              "driver",
              "adc",
              "channels"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "dosing_pump",
            "description": "`driver: dosing_pump`: `{ pump, ml_per_s }`, `pump` a nested `pwm_channel` or `gpio_line`.\n\n`drive_fraction` (pwm only) is the duty the pump runs at during a dispense, default full\ndrive. `max_dispense_ml` optionally caps a single `dispense` call.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "dosing_pump"
              },
              "link": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "A transport (or a simulated plant), by name.",
                "title": "Link"
              },
              "pump": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/PwmChannelConfig"
                  },
                  {
                    "$ref": "#/$defs/GpioLineConfig"
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Pump"
              },
              "ml_per_s": {
                "description": "Delivery rate at full drive (pwm) or on (gpio).",
                "exclusiveMinimum": 0,
                "title": "Ml Per S",
                "type": "number"
              },
              "drive_fraction": {
                "default": 1.0,
                "exclusiveMinimum": 0.0,
                "maximum": 1.0,
                "title": "Drive Fraction",
                "type": "number"
              },
              "max_dispense_ml": {
                "anyOf": [
                  {
                    "exclusiveMinimum": 0,
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Max Dispense Ml"
              }
            },
            "required": [
              "driver",
              "pump",
              "ml_per_s"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "ds18b20",
            "description": "`driver: ds18b20`: the probe's id under `/sys/bus/w1/devices`.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "ds18b20"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_onewire": "#/$defs/FakeOneWireConfigTagged",
                        "onewire": "#/$defs/OneWireConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeOneWireConfigTagged"
                      },
                      {
                        "$ref": "#/$defs/OneWireConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "probe_id": {
                "title": "Probe Id",
                "type": "string"
              }
            },
            "required": [
              "driver",
              "link",
              "probe_id"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "ezo_do",
            "description": "One EZO-DO circuit, alone on its UART.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "ezo_do"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_uart": "#/$defs/FakeUartConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeUartConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              }
            },
            "required": [
              "driver",
              "link"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "ezo_ec",
            "description": "One EZO-EC circuit, alone on its UART.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "ezo_ec"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_uart": "#/$defs/FakeUartConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeUartConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              }
            },
            "required": [
              "driver",
              "link"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "ezo_orp",
            "description": "One EZO-ORP circuit, alone on its UART.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "ezo_orp"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_uart": "#/$defs/FakeUartConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeUartConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              }
            },
            "required": [
              "driver",
              "link"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "ezo_ph",
            "description": "One EZO-pH circuit, alone on its UART.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "ezo_ph"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_uart": "#/$defs/FakeUartConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeUartConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              }
            },
            "required": [
              "driver",
              "link"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "gpio_line",
            "description": "`driver: gpio_line`: `{ link, line }`, or `pin: GPIO18` from the board profile.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "gpio_line"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_gpio": "#/$defs/FakeGpioConfigTagged",
                        "gpio": "#/$defs/GpioConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeGpioConfigTagged"
                      },
                      {
                        "$ref": "#/$defs/GpioConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "line": {
                "minimum": 0,
                "title": "Line",
                "type": "integer"
              },
              "direction": {
                "default": "output",
                "enum": [
                  "output",
                  "input"
                ],
                "title": "Direction",
                "type": "string"
              },
              "invert": {
                "default": false,
                "description": "An active-low relay board or switch.",
                "title": "Invert",
                "type": "boolean"
              },
              "initial": {
                "default": false,
                "description": "An output's level at startup.",
                "title": "Initial",
                "type": "boolean"
              },
              "pull_up": {
                "anyOf": [
                  {
                    "type": "boolean"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "An input's bias: true pulls up, false down, omitted as-is.",
                "title": "Pull Up"
              }
            },
            "required": [
              "driver",
              "link",
              "line"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "htu21d",
            "description": "One chip; there is no i2c_address pin, but the field stays for parity with sht31/sht4x.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "htu21d"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_i2c": "#/$defs/FakeI2cConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeI2cConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "i2c_address": {
                "default": 64,
                "maximum": 119,
                "minimum": 3,
                "title": "I2C Address",
                "type": "integer"
              }
            },
            "required": [
              "driver",
              "link"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "hx711",
            "description": "One chip, two raw GPIO lines: `{ link, clock_line, data_line, scale, offset }`.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "hx711"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_gpio": "#/$defs/FakeGpioConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeGpioConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "clock_line": {
                "description": "PD_SCK: the host drives this.",
                "minimum": 0,
                "title": "Clock Line",
                "type": "integer"
              },
              "data_line": {
                "description": "DOUT: the chip drives this.",
                "minimum": 0,
                "title": "Data Line",
                "type": "integer"
              },
              "gain": {
                "default": "a128",
                "enum": [
                  "a128",
                  "b32",
                  "a64"
                ],
                "title": "Gain",
                "type": "string"
              },
              "scale": {
                "default": 1.0,
                "description": "Weight units per raw ADC count.",
                "title": "Scale",
                "type": "number"
              },
              "offset": {
                "default": 0.0,
                "title": "Offset",
                "type": "number"
              }
            },
            "required": [
              "driver",
              "link",
              "clock_line",
              "data_line"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "i2c_table",
            "description": "`driver: i2c_table`. `registers` is the driver's own tree -- see `Register`.\n\n```yaml\nboard_temp:\n  driver: i2c_table\n  link: i2c1\n  i2c_address: 0x48\n  registers:\n    temperature: { address: 0, length: 2, signed: true, scale: 0.0078125, unit: \"\u00b0C\" }\n```",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "i2c_table"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_i2c": "#/$defs/FakeI2cConfigTagged",
                        "i2c": "#/$defs/I2cConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeI2cConfigTagged"
                      },
                      {
                        "$ref": "#/$defs/I2cConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "i2c_address": {
                "description": "The chip's bus address.",
                "maximum": 119,
                "minimum": 3,
                "title": "I2C Address",
                "type": "integer"
              },
              "registers": {
                "additionalProperties": {
                  "$ref": "#/$defs/Register"
                },
                "title": "Registers",
                "type": "object"
              }
            },
            "required": [
              "driver",
              "link",
              "i2c_address",
              "registers"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "mcp3008",
            "description": "`channels: { level: { channel: 0 } }`, in volts unless `unit` and `scale` say otherwise.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "mcp3008"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_spi": "#/$defs/FakeSpiConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeSpiConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "channels": {
                "additionalProperties": {
                  "$ref": "#/$defs/Channel"
                },
                "title": "Channels",
                "type": "object"
              },
              "vref": {
                "default": 3.3,
                "description": "The reference voltage on VREF.",
                "exclusiveMinimum": 0,
                "title": "Vref",
                "type": "number"
              }
            },
            "required": [
              "driver",
              "link",
              "channels"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "mcp4725",
            "description": "`driver: mcp4725`: `{ link, i2c_address }`, in a bare 0-1 fraction unless `unit` and `span`.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "mcp4725"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_i2c": "#/$defs/FakeI2cConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeI2cConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "i2c_address": {
                "default": 96,
                "maximum": 119,
                "minimum": 3,
                "title": "I2C Address",
                "type": "integer"
              },
              "unit": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The unit `drive` is set in; omitted, it is the fraction itself.",
                "title": "Unit"
              },
              "quantity": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "With `unit`: what `drive` then is ('drive').",
                "title": "Quantity"
              },
              "span": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Bounds"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "With `unit`: the value meaning 0 % and the one meaning 100 %."
              }
            },
            "required": [
              "driver",
              "link"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "mhz19",
            "description": "One MH-Z19(B) on its own UART port.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "mhz19"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_uart": "#/$defs/FakeUartConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeUartConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              }
            },
            "required": [
              "driver",
              "link"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "modbus",
            "description": "`driver: modbus`. `registers` is the driver's own tree -- see `ModbusRegister`.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "modbus"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_registers": "#/$defs/FakeRegisterLinkConfigTagged",
                        "modbus_rtu": "#/$defs/ModbusRtuConfigTagged",
                        "modbus_tcp": "#/$defs/ModbusTcpConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeRegisterLinkConfigTagged"
                      },
                      {
                        "$ref": "#/$defs/ModbusTcpConfigTagged"
                      },
                      {
                        "$ref": "#/$defs/ModbusRtuConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "registers": {
                "additionalProperties": {
                  "$ref": "#/$defs/ModbusRegister"
                },
                "title": "Registers",
                "type": "object"
              },
              "unit_id": {
                "default": 1,
                "title": "Unit Id",
                "type": "integer"
              }
            },
            "required": [
              "driver",
              "link",
              "registers"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "ms5611",
            "description": "One chip by its I2C address; `osr` trades conversion time for resolution.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "ms5611"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_i2c": "#/$defs/FakeI2cConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeI2cConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "i2c_address": {
                "default": 119,
                "maximum": 119,
                "minimum": 3,
                "title": "I2C Address",
                "type": "integer"
              },
              "osr": {
                "default": 4096,
                "enum": [
                  256,
                  512,
                  1024,
                  2048,
                  4096
                ],
                "title": "Osr",
                "type": "integer"
              }
            },
            "required": [
              "driver",
              "link"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "pulse_counter",
            "description": "`driver: pulse_counter`: `{ link, line, pulses_per_litre }`, or `pin:` from a board profile.\n\n`pulses_per_litre` is the sensor's datasheet constant (450 for a\nYF-S201); `debounce_s` guards against contact bounce or electrical\nnoise on the pulse line.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "pulse_counter"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_gpio": "#/$defs/FakeGpioConfigTagged",
                        "gpio": "#/$defs/GpioConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeGpioConfigTagged"
                      },
                      {
                        "$ref": "#/$defs/GpioConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "line": {
                "minimum": 0,
                "title": "Line",
                "type": "integer"
              },
              "pulses_per_litre": {
                "exclusiveMinimum": 0,
                "title": "Pulses Per Litre",
                "type": "number"
              },
              "debounce_s": {
                "default": 0.0,
                "minimum": 0,
                "title": "Debounce S",
                "type": "number"
              },
              "pull_up": {
                "anyOf": [
                  {
                    "type": "boolean"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The line's bias: true pulls up, false down, omitted as-is.",
                "title": "Pull Up"
              }
            },
            "required": [
              "driver",
              "link",
              "line",
              "pulses_per_litre"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "pwm_channel",
            "description": "`driver: pwm_channel`: `{ link, channel }`, or `pin: PWM0` from the board profile.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "pwm_channel"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_pwm": "#/$defs/FakePwmConfigTagged",
                        "pwm": "#/$defs/PwmConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakePwmConfigTagged"
                      },
                      {
                        "$ref": "#/$defs/PwmConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "channel": {
                "minimum": 0,
                "title": "Channel",
                "type": "integer"
              },
              "frequency_hz": {
                "default": 1000.0,
                "exclusiveMinimum": 0,
                "title": "Frequency Hz",
                "type": "number"
              },
              "invert": {
                "default": false,
                "title": "Invert",
                "type": "boolean"
              },
              "unit": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The unit `drive` is set in; omitted, it is the duty itself.",
                "title": "Unit"
              },
              "quantity": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "With `unit`: what `drive` then is ('temperature').",
                "title": "Quantity"
              },
              "span": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Bounds"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "With `unit`: the value meaning 0 % and the one meaning 100 %."
              }
            },
            "required": [
              "driver",
              "link",
              "channel"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "pymeasure",
            "description": "`driver: pymeasure`. `channels` is the driver's own tree -- see `PyMeasureSignal`.\n\nNamed `channels`, not `signals`: the envelope's `signals:` key is\nreserved for signal metadata, the same for every driver.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "pymeasure"
              },
              "link": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "A transport (or a simulated plant), by name.",
                "title": "Link"
              },
              "instrument": {
                "description": "Dotted class, e.g. pymeasure.instruments.keithley.Keithley2400",
                "title": "Instrument",
                "type": "string"
              },
              "adapter": {
                "description": "The resource: 'GPIB::24', 'ASRL/dev/ttyUSB0', a VISA string.",
                "title": "Adapter",
                "type": "string"
              },
              "kwargs": {
                "additionalProperties": true,
                "title": "Kwargs",
                "type": "object"
              },
              "channels": {
                "additionalProperties": {
                  "$ref": "#/$defs/PyMeasureSignal"
                },
                "title": "Channels",
                "type": "object"
              }
            },
            "required": [
              "driver",
              "instrument",
              "adapter",
              "channels"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "qcodes",
            "description": "`driver: qcodes`. `channels` is the driver's own tree -- see `QCoDeSSignal`.\n\nNamed `channels`, not `signals`: the envelope's `signals:` key is\nreserved for signal metadata, the same for every driver.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "qcodes"
              },
              "link": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "A transport (or a simulated plant), by name.",
                "title": "Link"
              },
              "instrument": {
                "description": "Dotted class, e.g. qcodes.instrument_drivers.Keithley.Keithley2450",
                "title": "Instrument",
                "type": "string"
              },
              "instrument_name": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The QCoDeS instrument's own `name`; defaults to the device's.",
                "title": "Instrument Name"
              },
              "args": {
                "items": {},
                "title": "Args",
                "type": "array"
              },
              "kwargs": {
                "additionalProperties": true,
                "title": "Kwargs",
                "type": "object"
              },
              "channels": {
                "additionalProperties": {
                  "$ref": "#/$defs/QCoDeSSignal"
                },
                "title": "Channels",
                "type": "object"
              }
            },
            "required": [
              "driver",
              "instrument",
              "channels"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "scd30",
            "description": "One chip by its I2C address, in continuous-measurement mode.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "scd30"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_i2c": "#/$defs/FakeI2cConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeI2cConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "i2c_address": {
                "default": 97,
                "maximum": 119,
                "minimum": 3,
                "title": "I2C Address",
                "type": "integer"
              },
              "pressure_mbar": {
                "default": 0,
                "description": "0 disables ambient-pressure compensation",
                "title": "Pressure Mbar",
                "type": "integer"
              },
              "sleep": {
                "default": true,
                "title": "Sleep",
                "type": "boolean"
              }
            },
            "required": [
              "driver",
              "link"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "scd4x",
            "description": "One chip by its I2C address; `variant: scd41` unlocks `single_shot`.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "scd4x"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_i2c": "#/$defs/FakeI2cConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeI2cConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "i2c_address": {
                "default": 98,
                "maximum": 119,
                "minimum": 3,
                "title": "I2C Address",
                "type": "integer"
              },
              "variant": {
                "default": "scd40",
                "enum": [
                  "scd40",
                  "scd41"
                ],
                "title": "Variant",
                "type": "string"
              },
              "low_power": {
                "default": false,
                "title": "Low Power",
                "type": "boolean"
              },
              "single_shot": {
                "default": false,
                "title": "Single Shot",
                "type": "boolean"
              },
              "sleep": {
                "default": true,
                "title": "Sleep",
                "type": "boolean"
              }
            },
            "required": [
              "driver",
              "link"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "scpi",
            "description": "`driver: scpi`. `channels` is the driver's own tree -- see `ScpiSignal`.\n\nNamed `channels`, not `signals`: the envelope's `signals:` key is\nreserved for signal metadata (range, precision, limits, ...), the same for\nevery driver, so a driver's own config may not use that name.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "scpi"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_text": "#/$defs/FakeTextLinkConfigTagged",
                        "serial": "#/$defs/SerialLinkConfigTagged",
                        "visa": "#/$defs/VisaLinkConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeTextLinkConfigTagged"
                      },
                      {
                        "$ref": "#/$defs/VisaLinkConfigTagged"
                      },
                      {
                        "$ref": "#/$defs/SerialLinkConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "channels": {
                "additionalProperties": {
                  "$ref": "#/$defs/ScpiSignal"
                },
                "title": "Channels",
                "type": "object"
              },
              "stop_command": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What a stop sends (`OUTP OFF`, `INP OFF`): the instrument's own stop. Omitted: a stop leaves its outputs as they are -- the right string differs by instrument, so none is assumed.",
                "title": "Stop Command"
              }
            },
            "required": [
              "driver",
              "link",
              "channels"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "sgp30",
            "description": "One chip by its I2C address.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "sgp30"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_i2c": "#/$defs/FakeI2cConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeI2cConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "i2c_address": {
                "default": 88,
                "maximum": 119,
                "minimum": 3,
                "title": "I2C Address",
                "type": "integer"
              },
              "baseline": {
                "anyOf": [
                  {
                    "maxItems": 2,
                    "minItems": 2,
                    "prefixItems": [
                      {
                        "type": "integer"
                      },
                      {
                        "type": "integer"
                      }
                    ],
                    "type": "array"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "[co2eq, tvoc] IAQ baseline words to restore at startup, as read back earlier from Sgp30Sensor.get_baseline; omit to let the chip's own algorithm re-settle from cold.",
                "title": "Baseline"
              }
            },
            "required": [
              "driver",
              "link"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "sgp40",
            "description": "One chip by its I2C address.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "sgp40"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_i2c": "#/$defs/FakeI2cConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeI2cConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "i2c_address": {
                "default": 89,
                "maximum": 119,
                "minimum": 3,
                "title": "I2C Address",
                "type": "integer"
              },
              "humidity_source": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Signal address to read humidity compensation from on each read; omit to use the datasheet's fixed 50 %RH default.",
                "title": "Humidity Source"
              },
              "temperature_source": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Signal address to read temperature compensation from on each read; omit to use the datasheet's fixed 25 degC default.",
                "title": "Temperature Source"
              }
            },
            "required": [
              "driver",
              "link"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "sht31",
            "description": "One chip by its I2C address.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "sht31"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_i2c": "#/$defs/FakeI2cConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeI2cConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "i2c_address": {
                "default": 68,
                "maximum": 119,
                "minimum": 3,
                "title": "I2C Address",
                "type": "integer"
              },
              "precision": {
                "default": "high",
                "enum": [
                  "high",
                  "medium",
                  "low"
                ],
                "title": "Precision",
                "type": "string"
              }
            },
            "required": [
              "driver",
              "link"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "sht4x",
            "description": "One chip by its I2C address.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "sht4x"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_i2c": "#/$defs/FakeI2cConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeI2cConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "i2c_address": {
                "default": 68,
                "maximum": 119,
                "minimum": 3,
                "title": "I2C Address",
                "type": "integer"
              },
              "precision": {
                "default": "high",
                "enum": [
                  "high",
                  "medium",
                  "low"
                ],
                "title": "Precision",
                "type": "string"
              }
            },
            "required": [
              "driver",
              "link"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "sht4x_set",
            "description": "Several chips on one bus, one namespace per sensor: `sensors: {dry: {i2c_address: 0x45}}`.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "sht4x_set"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_i2c": "#/$defs/FakeI2cConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeI2cConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "sensors": {
                "additionalProperties": {
                  "$ref": "#/$defs/SensorEntry"
                },
                "title": "Sensors",
                "type": "object"
              },
              "precision": {
                "default": "high",
                "enum": [
                  "high",
                  "medium",
                  "low"
                ],
                "title": "Precision",
                "type": "string"
              }
            },
            "required": [
              "driver",
              "link",
              "sensors"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "sim_daq",
            "description": "Read chosen outputs of a simulated plant as this device's `[RP]` signals.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "sim_daq"
              },
              "link": {
                "$ref": "#/$defs/PlantLink",
                "description": "The plant link read: `sim_plant`, or another package's own `MultiPlant` link, such as `examples/furnace`'s `sim_furnace`."
              },
              "ports": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "$ref": "#/$defs/DaqPort"
                    }
                  ]
                },
                "description": "Signal path -> the plant's output port; spelled out with `quantity` and `unit` when the plant does not say what a port measures (a bare `sim_plant`). A dotted path (`dry.humidity`) puts the signal in a namespace, so the device can mirror a real one's addresses.",
                "title": "Ports",
                "type": "object"
              }
            },
            "required": [
              "driver",
              "link",
              "ports"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "sim_drive",
            "description": "Drive chosen inputs of a simulated plant from this device's `[W]` signals.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "sim_drive"
              },
              "link": {
                "$ref": "#/$defs/PlantLink",
                "description": "The plant link driven: `sim_plant`, or another package's own `MultiPlant` link, such as `examples/furnace`'s `sim_furnace`."
              },
              "ports": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "$ref": "#/$defs/DrivePort"
                    }
                  ]
                },
                "description": "Signal path -> the plant's input port, or spelled out with the `quantity`, `unit` and `limits` the signal is set in (mapped linearly onto the port's 0..1 drive), or `demand: output` to declare it in the plant's own output unit and let `commit` invert the plant. A dotted path is a namespace.",
                "title": "Ports",
                "type": "object"
              }
            },
            "required": [
              "driver",
              "link",
              "ports"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "stepper",
            "description": "`driver: stepper`: `{ link, step_line, direction_line, steps_per_s }`.\n\n`enable_line` (optional) is driven active for the duration of a move and released\nafterwards; `enable_active_low` (default true) matches the common driver-IC convention.\n`steps_per_unit` (optional) lets `move()` be called in engineering units instead of raw\nsteps. `pulse_width_s` is how long the step line is held high per pulse.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "stepper"
              },
              "link": {
                "anyOf": [
                  {
                    "discriminator": {
                      "mapping": {
                        "fake_gpio": "#/$defs/FakeGpioConfigTagged",
                        "gpio": "#/$defs/GpioConfigTagged"
                      },
                      "propertyName": "type"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/FakeGpioConfigTagged"
                      },
                      {
                        "$ref": "#/$defs/GpioConfigTagged"
                      }
                    ]
                  },
                  {
                    "type": "string"
                  }
                ],
                "title": "Link"
              },
              "step_line": {
                "minimum": 0,
                "title": "Step Line",
                "type": "integer"
              },
              "direction_line": {
                "minimum": 0,
                "title": "Direction Line",
                "type": "integer"
              },
              "steps_per_s": {
                "exclusiveMinimum": 0,
                "title": "Steps Per S",
                "type": "number"
              },
              "enable_line": {
                "anyOf": [
                  {
                    "minimum": 0,
                    "type": "integer"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Enable Line"
              },
              "enable_active_low": {
                "default": true,
                "title": "Enable Active Low",
                "type": "boolean"
              },
              "steps_per_unit": {
                "anyOf": [
                  {
                    "exclusiveMinimum": 0,
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Steps Per Unit"
              },
              "pulse_width_s": {
                "default": 0.0005,
                "minimum": 0,
                "title": "Pulse Width S",
                "type": "number"
              }
            },
            "required": [
              "driver",
              "link",
              "step_line",
              "direction_line",
              "steps_per_s"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "values",
            "description": "Operator-entered numbers, each a writable setting other devices' inputs may follow.",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              },
              "driver": {
                "const": "values"
              },
              "link": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "A transport (or a simulated plant), by name.",
                "title": "Link"
              },
              "values": {
                "additionalProperties": {
                  "$ref": "#/$defs/ValueEntry"
                },
                "description": "Each value by name: `{initial, unit, label, limits}`.",
                "title": "Values",
                "type": "object"
              }
            },
            "required": [
              "driver"
            ],
            "not": {
              "required": [
                "config"
              ]
            }
          },
          {
            "type": "object",
            "title": "overlay of a device declared in a base",
            "properties": {
              "label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Label"
              },
              "poll_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Poll S"
              },
              "signals": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SignalMeta"
                    },
                    {
                      "$ref": "#/$defs/NamespaceMeta"
                    }
                  ]
                },
                "title": "Signals",
                "type": "object"
              },
              "inputs": {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "description": "Input name -> what it follows: an address on another device (`hum_sensors.dry.humidity`), or a number (`36.5`). Every input the driver declares must be given one; it has no default.",
                "title": "Inputs",
                "type": "object"
              },
              "reads": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Reads"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              },
              "retry_max_age_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How long a value a failed write kept may wait to be sent again; older is dropped, not sent. Omit for 60 s.",
                "title": "Retry Max Age S"
              },
              "stop": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "anyOf": [
                        {
                          "type": "number"
                        },
                        {
                          "$ref": "#/$defs/Keep"
                        }
                      ]
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> what a stop writes to it: a number, or `keep` to leave it as it is. A demand left out takes its driver's `off`, else keeps. Refused on a device whose driver has a stop command (it stops the whole device).",
                "title": "Stop"
              },
              "on_shutdown": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/OnShutdown"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "What the runner's shutdown does to this device: `stop` (its resolved stop) or `keep` (writes nothing). Omit for the runner's `on_shutdown`."
              },
              "permissive": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "$ref": "#/$defs/Permissive"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Demand path -> the value condition a write to it needs: `{signal: <address>, above: <n>, below: <n>}`. Fails closed when the signal has no value.",
                "title": "Permissive"
              }
            },
            "not": {
              "required": [
                "driver"
              ]
            }
          },
          {
            "type": "null",
            "title": "Removed by this layer"
          }
        ]
      },
      "title": "Devices",
      "type": "object"
    },
    "controllers": {
      "additionalProperties": {
        "oneOf": [
          {
            "$ref": "#/$defs/ControllerEntry"
          },
          {
            "type": "null",
            "title": "Removed by this layer"
          }
        ]
      },
      "description": "Keyed by the output signal's address.",
      "title": "Controllers",
      "type": "object"
    },
    "runner": {
      "anyOf": [
        {
          "$ref": "#/$defs/RunnerConfig"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "files": {
      "items": {
        "format": "path",
        "type": "string"
      },
      "title": "Files",
      "type": "array"
    },
    "resumed": {
      "default": false,
      "title": "Resumed",
      "type": "boolean"
    },
    "extends": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "title": "Extends",
      "description": "This file's own bases, merged in order before its own keys."
    }
  },
  "title": "RigConfig",
  "type": "object",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
