{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"The humidity rig","text":"<p>A chamber held at a target relative humidity by blending the output of two pumps \u2014 one wet, one dry \u2014 under closed-loop control. Flyball's device model is the substrate; this book covers only what is specific to this rig: the hardware, its two devices, and how to operate it.</p> You want to\u2026 Read build or repair the board Hardware understand what the software sees and drives The rig run it Operating <p>Anything about the device model, the control loop or the CLI in general is in the flyball book, not here.</p>"},{"location":"#the-rig-in-one-sentence","title":"The rig, in one sentence","text":"<p>Three SHT4x humidity/temperature sensors on one I\u00b2C bus (<code>hum_sensors</code>, driver <code>sht4x_set</code>) and two PWM-driven pumps blended by one driver (<code>blender</code>, driver <code>dual_pump_blender</code>); a PI controller (<code>blender.humidity</code>) regulates the chamber's humidity (<code>hum_sensors.chamber.humidity</code>) by moving the blend. <code>rig.yaml</code> is the real rig; <code>sim.yaml</code> overlays it with no hardware attached, for development and for this book's examples. Both are quoted in full in Configuration.</p>"},{"location":"hardware/board/","title":"Board and wiring","text":"<p>What connects to what: the TB6612 driver, the SHT4x, pins, power?</p>"},{"location":"hardware/bom/","title":"Bill of materials","text":"<p>What to buy.</p>"},{"location":"hardware/pi/","title":"Raspberry Pi setup","text":"<p>How do I prepare a Pi: OS, I\u00b2C, PWM, the setup script, installing flyball and this example?</p>"},{"location":"hardware/pumps/","title":"Pumps and PWM","text":"<p>How is pump flow related to PWM duty, and how was that measured?</p>"},{"location":"operating/autotune/","title":"Autotune on this rig","text":"<p>Which experiment to run, what a good fit looks like, and what to do with a bad one.</p> <p>This rig has no autotune program step or CLI command of its own \u2014 flyball's generic <code>flyball.autotune</code> library (<code>controller/src/flyball/autotune/</code>) is run as a script against the live rig, with the target controller's law temporarily set to <code>OpenLoop</code> so a step is applied without the PI law fighting it. Nothing here is humidity-specific.</p>"},{"location":"operating/autotune/#which-experiment","title":"Which experiment","text":"<p><code>flyball.autotune</code> offers two:</p> experiment measures cost <code>StepTest</code> a step response, fit to an FOPDT model one clean step, held at each end <code>RelayTest</code> the critical gain/period, off a forced limit cycle deliberately oscillates the rig <p>Prefer <code>StepTest</code> here. The chamber is slow and the wet/dry blend is a finite resource (pump runtime, water and desiccant consumption) \u2014 a <code>RelayTest</code> cycles the blend back and forth for several periods to read the oscillation, which costs more of both than one step does, and the model a <code>StepTest</code> fits is reusable for simulation and for re-tuning later without another run.</p>"},{"location":"operating/autotune/#sizing-the-experiment","title":"Sizing the experiment","text":"<p><code>StepTest(base, size, window, band, timeout)</code> needs a plateau at <code>base</code> before it steps, and treats a further <code>window</code>-second span within <code>band</code> as the new plateau. Against this rig:</p> <ul> <li><code>base</code>/<code>size</code>: keep the step within the supply span (<code>dry: 10.0</code>,   <code>wet: 90.0</code> in <code>rig.yaml</code>) and away from its ends \u2014 a step that rails   (see The blender device)   isn't measuring the linear part of the plant. A base near the middle of   the working range with a size of 10\u201315 %RH is a reasonable start.</li> <li><code>window</code>: must exceed the plant's dead time, or the flat stretch   right after the step reads as the plateau and the fit never sees the   response. <code>sim.yaml</code>'s <code>sim_humidity_chamber</code> plant is a pure first-order   lag (<code>tau_s: 45.0</code>) with no dead time term at all \u2014 useful for   exercising the fit code, not for sizing <code>window</code> against real transport   delay. On real hardware, expect dead time from the tubing between the   blend point and the chamber, and from the sensor's own response; size   <code>window</code> generously until a real run confirms it.</li> <li><code>band</code>: above the chamber sensor's noise (<code>sim.yaml</code>'s plant adds   \u00b10.3 %RH Gaussian noise to the chamber reading only \u2014 the real SHT4x's   own noise floor is unmeasured), well below <code>size</code>.</li> <li><code>timeout</code>: an upper bound on how long either plateau is allowed to   take, so a rig that never settles fails the experiment instead of   hanging it.</li> </ul>"},{"location":"operating/autotune/#fitting-and-tuning","title":"Fitting and tuning","text":"<p><code>fit_fopdt</code> turns the logged <code>(time, reading)</code> pairs into an <code>FOPDT</code>: <code>gain</code>, <code>tau</code>, <code>dead_time</code>, and an <code>error</code> (RMS residual, in reading units \u2014 %RH here). <code>imc(model)</code> is the preferred rule for a <code>StepTest</code> result: IMC/lambda tuning, PI by default (<code>derivative=False</code> keeps it PI, matching <code>blender.humidity</code>'s current law). <code>FOPDT.normalised_dead_time</code> (<code>\u03b8/(\u03b8+\u03c4)</code>) says how hard the plant is: below 0.2 most tunings work; above 0.6 no PID does well and the rig may need detuning rather than a cleverer rule.</p>"},{"location":"operating/autotune/#what-a-good-fit-looks-like","title":"What a good fit looks like","text":"<ul> <li><code>error</code> small relative to <code>band</code> and to the step <code>size</code> \u2014 the model   tracks the logged response, not just its two endpoints.</li> <li><code>dead_time</code> and <code>tau</code> both positive and the right order of magnitude for   a chamber this size (seconds to low tens of seconds for <code>dead_time</code>,   tens of seconds for <code>tau</code>, on <code>sim.yaml</code>'s numbers) \u2014 a fitted <code>tau</code>   near zero or wildly larger than the experiment's own <code>window</code> means the   window was too short to see the actual settling.</li> <li><code>normalised_dead_time</code> comfortably below 0.6.</li> </ul>"},{"location":"operating/autotune/#what-to-do-with-a-bad-one","title":"What to do with a bad one","text":"<ul> <li>Fit never completes (<code>ExperimentIncompleteError</code>/   <code>ExperimentTimeoutError</code>): the plateau band was too tight for the   sensor's noise, or <code>timeout</code> too short for the plant \u2014 widen <code>band</code> or   <code>timeout</code> and rerun.</li> <li>Response smaller than expected (<code>ResponseTooSmallError</code>, from   <code>RelayTest</code>, or a suspiciously small <code>error</code>-free fit from <code>StepTest</code>):   <code>size</code> was too small against the noise, or the step landed near a rail   (see the split-range rail)   and clipped.</li> <li><code>dead_time</code> swallowing the whole response: <code>window</code> was shorter   than the true dead time, so the flat pre-response stretch was read as   the new plateau \u2014 widen <code>window</code> and rerun rather than trusting the fit.</li> <li>The plant is genuinely dead-time-dominated   (<code>normalised_dead_time</code> above ~0.6): expect any PID to be slow;   detuning (a larger <code>lam</code> in <code>imc</code>) is the honest answer, not a better   rule.</li> </ul> <p><code>Gains.of_ideal</code>/the fitted <code>Gains</code> give <code>kp</code>, <code>ki</code>, <code>kd</code>, <code>tt</code> in the same parallel form <code>blender.humidity</code>'s <code>law: { tag: PI, kp, ki, tt }</code> already uses, so a fitted result drops straight into <code>rig.yaml</code> once it looks right.</p>"},{"location":"operating/failures/","title":"Failure modes","text":"<p>What goes wrong, how it shows up, and what to do.</p> <p>Nothing here is humidity-specific machinery \u2014 it's flyball's generic device offline/condition handling, and this rig's own error types, read together.</p>"},{"location":"operating/failures/#a-sensor-read-fails-the-device-goes-offline","title":"A sensor read fails: the device goes offline","text":"<p><code>hum_sensors</code> is polled on a period (<code>poll_s</code>); if any exception escapes <code>Sht4xSet.read</code> \u2014 a bad CRC or a short reply (<code>HardwareError</code>, raised directly by <code>decode</code> in <code>flyball_linux.devices.chips.sht4x</code>), or an I2C bus error under it \u2014 the runtime's polling loop (<code>flyball.runtime.polling.Polling._read</code>) doesn't retry: it records an <code>offline</code> <code>Condition</code> (<code>Level.ERROR</code>, the exception's message) against the device, stops polling it, and emits an event. The device stays offline \u2014 no more reads, no more samples, nothing publishing \u2014 until explicitly restarted (<code>POST /api/devices/hum_sensors/restart</code>), which clears the condition and resumes polling on the same period.</p> <p>Because <code>hum_sensors</code> is three atomic namespaces read independently, a CRC failure on <code>wet</code> alone still takes the whole device offline, not just that namespace \u2014 <code>read</code> raising at all is what the polling loop sees, whichever namespace was mid-transaction.</p>"},{"location":"operating/failures/#a-read-that-succeeds-but-is-slow","title":"A read that succeeds but is slow","text":"<p>If a device's <code>read</code> takes longer than its own <code>poll_s</code>, the same <code>_read</code> records a <code>slow</code> <code>Condition</code> (<code>Level.WARNING</code>) instead \u2014 the device keeps polling, but something (a stretched I2C transaction, system load) is eating into the margin. Worth watching if <code>hum_sensors</code>' <code>poll_s: 1</code> starts showing this: the fixed conversion wait (\u00a7the sensor device, ~8.3 ms at high precision) is a small fraction of a second, so a slow condition here points at the bus or the host, not the sensor's own timing.</p>"},{"location":"operating/failures/#a-demand-is-refused","title":"A demand is refused","text":"<p>These are refused before anything reaches the blender \u2014 a 409 from the API, or a raised <code>ConflictError</code> from a script \u2014 and change nothing:</p> <ul> <li>A <code>together</code> pair set in part. <code>dry_flow</code> and <code>wet_flow</code> (and   <code>dry_effort</code>/<code>wet_effort</code>) must arrive in the same demand \u2014 \"<code>'\u2026dry_flow'   is set with wet_flow</code>\". See The blender device.</li> <li>A demand on a signal a controller drives. <code>blender.humidity</code> while   <code>blender.humidity</code> (the controller) is regulating \u2014 put it in <code>manual</code>   first (a program step, or <code>POST /api/controllers/blender.humidity/manual</code>).</li> </ul>"},{"location":"operating/failures/#the-blend-cant-reach-the-target","title":"The blend can't reach the target","text":"<p>Not a failure the rig refuses \u2014 <code>commit</code> still runs, still writes the pumps \u2014 but the result isn't what was asked for:</p> <ul> <li>The target is outside <code>[dry, wet]</code>: <code>calculate_wet_fraction</code> rails   to the nearer supply end rather than raising, and the write state for   <code>humidity</code> reports <code>at_limit: \"low\"</code> or <code>\"high\"</code>. Watch   <code>expected_humidity</code> against the demanded target \u2014 a persistent gap   between them, with <code>at_limit</code> set, means the supply lines themselves   need attention (the dry line isn't dry enough, or the wet line isn't   wet enough), not the controller.</li> <li>The requested flow exceeds what the blend can deliver:   <code>humidity.pumps.errors.FlowsOverdrivenError</code> \u2014 raised by   <code>DualPumps.set_flows</code>/<code>validate_flows</code> for a direct <code>dry_flow</code>/<code>wet_flow</code>   demand that exceeds a line's <code>max_flow</code>. A <code>humidity</code> demand instead   goes through <code>Absolute(flow, OnOverdrive.CLAMP)</code>, so <code>commit</code>'s own   split-range path derates to what's achievable rather than raising \u2014 see   Limits.</li> <li>The two supply humidities aren't in order:   <code>humidity.blender.SupplyHumiditiesError</code> \u2014 <code>wet</code> must read (or be   configured) greater than <code>dry</code>, or there is no span to blend across.   Raised at build time from a configured <code>supply:</code>, or at <code>commit</code> time if   the bound sensors' readings cross (the wet line reading drier than the   dry line, say, or a swapped I2C address).</li> </ul>"},{"location":"operating/failures/#the-pumps-themselves-fail","title":"The pumps themselves fail","text":"<p><code>humidity.pumps.errors.PumpHardwareError</code> wraps whatever the underlying <code>PwmLink</code> raises on <code>configure</code>/<code>enable</code> \u2014 a real sysfs write can fail if the PWM overlay isn't enabled, or the chip's gone away. <code>stop()</code> (<code>PumpPair.stop</code>, called by the blender's <code>stop</code> command) tries both lines even if one fails, and raises a <code>PumpErrorGroup</code> \u2014 both individual errors, not just the first \u2014 if either does.</p>"},{"location":"operating/failures/#recovering","title":"Recovering","text":"<p>A device stays offline until <code>POST /api/devices/{name}/restart</code>, or the daemon is restarted outright. Building the blender always disables both PWM channels first \u2014 <code>PwmPump.__init__</code> calls <code>link.enable(channel, False)</code> before anything else \u2014 so a fresh <code>flyball-daemon</code> start never inherits a pump left running by a process that died mid-write; it's a property of the driver's own construction, not something the daemon has to arrange.</p>"},{"location":"operating/first-run/","title":"First run","text":"<p>Start the daemon, confirm the sensor reads, drive a pump by hand, set a setpoint, stop.</p> <p>This walks through <code>sim.yaml</code> \u2014 no hardware needed \u2014 but every command below is identical against real hardware (<code>flyball-daemon rig.yaml</code> alone); only the addresses and the daemon's boot line change.</p>"},{"location":"operating/first-run/#start-the-daemon","title":"Start the daemon","text":"<p>This rig has no daemon of its own \u2014 <code>flyball-daemon</code> (from <code>flyball</code> itself) serves it directly:</p> <pre><code>cd examples/humidity\nuv run flyball-daemon rig.yaml sim.yaml\n</code></pre> <p>It binds to <code>127.0.0.1:8000</code> by default \u2014 loopback only \u2014 and, beside loading the rig, picks up <code>tunings/</code> next to the first rig file automatically: <code>tunings/brisk.yaml</code> and <code>tunings/gentle.yaml</code> are loaded onto <code>rig.tunings</code> as <code>\"brisk\"</code> and <code>\"gentle\"</code>, two alternative laws for <code>blender.humidity</code> a <code>regulate</code> step can name (see Humidity programs). A <code>programs/</code> directory beside the rig file would be imported the same way; this rig doesn't have one yet.</p>"},{"location":"operating/first-run/#confirm-the-rig-loaded","title":"Confirm the rig loaded","text":"<pre><code>flyball devices\n</code></pre> <p>lists <code>hum_sensors</code> and <code>blender</code>. <code>flyball status</code> shows more: every signal's latest value, the controller, and anything waiting. This package's own thin client works the same way against its own API:</p> <pre><code>humidity status\n</code></pre>"},{"location":"operating/first-run/#read-a-signal","title":"Read a signal","text":"<pre><code>flyball read hum_sensors.chamber.humidity\nflyball read hum_sensors.dry --fresh    # a namespace: one sample, forced fresh\nflyball read hum_sensors               # a device: one sample per sensor\n</code></pre> <p><code>GET /api/read/{address}</code> underneath \u2014 a signal gives a <code>Reading</code>, a namespace gives a <code>Sample</code>, a device gives one <code>Sample</code> per namespace it reads in its own transaction. <code>--fresh</code> forces a new hardware read instead of the last polled value.</p>"},{"location":"operating/first-run/#drive-a-pump-by-hand","title":"Drive a pump by hand","text":"<p><code>dry_flow</code> and <code>wet_flow</code> are a <code>together</code> pair \u2014 a demand naming one alone is refused, so a single-signal <code>flyball demand</code> won't do; put both in one demand instead. This package's <code>humidity</code> client wraps exactly that call (<code>PUT /api/devices/{name}/demand</code>):</p> <pre><code>humidity set blender dry_flow=0.2 wet_flow=0.2\n</code></pre> <p><code>blend_flow</code>, by contrast, isn't <code>together</code> with anything, so it takes a plain single-signal demand:</p> <pre><code>flyball demand blender.blend_flow 1.2\n</code></pre>"},{"location":"operating/first-run/#set-a-target-humidity","title":"Set a target humidity","text":"<p><code>blender.humidity</code> is driven by the <code>blender.humidity</code> controller, marked <code>default: true</code> in <code>rig.yaml</code> \u2014 so <code>humidity</code> itself refuses a direct demand while the controller owns it (409: a signal a controller drives). Aim the controller instead. The simplest way is a one-step program (see Humidity programs for the full vocabulary):</p> <pre><code># aim.yaml\nname: aim\nsteps:\n  - regulate: {setpoint: 45}\n</code></pre> <pre><code>flyball program run aim.yaml\n</code></pre> <p><code>setpoint: 45</code> means 45 %RH; <code>loop</code> is omitted, so it aims the rig's default controller. <code>flyball status</code> (or <code>flyball watch controllers</code>) shows the chamber humidity moving towards it.</p>"},{"location":"operating/first-run/#stop","title":"Stop","text":"<pre><code>humidity stop\n</code></pre> <p>calls the blender's <code>stop</code> command directly (<code>POST /api/devices/blender/commands/stop</code>) \u2014 both pumps off at once, bypassing whatever demand is pending. This does not touch the controller's mode: it's still regulating, and will move the pumps again on its next tick. To stop regulating as well, put the controller in manual with a program step (<code>- manual: {}</code>) or <code>POST /api/controllers/blender.humidity/manual</code>.</p>"},{"location":"operating/limits/","title":"Limits","text":"<p>Achievable range, flow, and the reasons for each limit \u2014 as <code>rig.yaml</code> and the driver code actually declare them.</p>"},{"location":"operating/limits/#humidity","title":"Humidity","text":"<p><code>blender.humidity [W]</code> is clamped to <code>[0, 100]</code> by the signal's own <code>limits</code>. Within that, what's actually achievable is bounded by the two supply humidities \u2014 <code>dry: 0x45</code>, <code>wet: 0x46</code> in <code>hum_sensors</code>, whatever they currently read (or, on a rig with no sensor bound, the configured <code>supply.dry</code>/<code>supply.wet</code>). A target outside <code>[dry, wet]</code> rails to the nearer end rather than being refused \u2014 see the split-range arithmetic \u2014 and the resulting <code>WriteState.at_limit</code> says which end. With room air on the dry line, the practical floor is ambient %RH, not zero.</p> <p><code>hum_sensors.chamber.humidity</code>'s own signal <code>range</code> is <code>[0, 100]</code> (precision 2), and <code>rig.yaml</code> adds a <code>warn: [20, 80]</code> band on top \u2014 inside <code>[0, 100]</code> but a narrower range the UI flags outside of, independent of any control limit.</p>"},{"location":"operating/limits/#flow","title":"Flow","text":"<p>Each pump line is limited to its own <code>max_flow</code> (<code>rig.yaml</code>: 2.0 L/min for both <code>dry</code> and <code>wet</code>). <code>dry_flow</code>/<code>wet_flow [RPW]</code> are clamped there directly; <code>blend_flow [RW]</code> (the total flow a <code>humidity</code> demand mixes to) is not itself limited by a signal range, but the resulting per-line flows still are.</p> <p>The achievable total flow depends on the blend, not just on the two <code>max_flow</code>s: at a wet fraction <code>b</code>, <code>dry_flow = (1\u2212b)\u00b7total \u2264 dry_max</code> and <code>wet_flow = b\u00b7total \u2264 wet_max</code>, so <code>total \u2264 min(dry_max/(1\u2212b), wet_max/b)</code>. That ceiling is lowest near either rail \u2014 as <code>b \u2192 0</code> it falls to <code>dry_max</code> alone (2.0 L/min here), since almost none of the total may come from the wet line \u2014 and highest at the 50/50 blend, <code>2 \u00b7 min(dry_max, wet_max)</code> (4.0 L/min, with this rig's equal 2.0 L/min lines \u2014 the same number the simple sum would give here only because the two maxima happen to match; with unequal lines they'd differ). <code>MaxFlows.flows_at_blend</code> (<code>humidity/pumps/types.py</code>) is the exact computation; <code>Absolute(flow, OnOverdrive.CLAMP)</code> (what <code>commit</code> uses for a <code>humidity</code> demand) derates to whatever the blend can actually deliver rather than raising.</p>"},{"location":"operating/limits/#effort-and-deadband","title":"Effort and deadband","text":"<p><code>dry_effort</code>/<code>wet_effort [RPW]</code> are <code>[0, 1]</code> of full PWM drive. <code>deadband</code> (<code>rig.yaml</code>: 0.05 for both lines) is the effort below which a line doesn't turn \u2014 <code>PwmPump.calculate_duty_ratio</code> maps <code>[0, 1]</code> effort onto <code>[deadband, 1]</code> duty, so effort 0 is genuinely off and any effort above 0 already clears the deadband; there's no achievable duty between \"off\" and the deadband's floor.</p>"},{"location":"operating/limits/#sensor-range-and-rate","title":"Sensor range and rate","text":"<p><code>humidity [RP]</code>: <code>[0, 100]</code> %RH, precision 2 (<code>Sht4xSet</code>'s declared signal spec). <code>temperature [RP]</code>: <code>[-40, 125]</code> \u00b0C, precision 2 \u2014 not clamped further by the decode itself (see the sensor device).</p> <p><code>hum_sensors.chamber</code> polls every 1 s (the device's own <code>poll_s</code>); <code>hum_sensors.dry</code>/<code>hum_sensors.wet</code> every 5 s (<code>rig.yaml</code>'s per-namespace override) \u2014 the supply lines drift slowly enough that polling them as often as the chamber buys nothing. <code>blender</code> itself polls every 1 s, but its <code>read</code> never touches the bus: every value it reports is computed from the pumps' own state, so its poll rate only bounds how fresh the readback is, not any hardware transaction rate.</p>"},{"location":"operating/limits/#the-controller","title":"The controller","text":"<p><code>blender.humidity</code>'s law is <code>PI, kp: 0.8, ki: 0.02, tt: 60</code> \u2014 no derivative term, appropriate for a noisy %RH reading (see Autotune on this rig for how those gains would be re-fitted). <code>tt: 60</code> is the back-calculation tracking constant for anti-windup, in seconds \u2014 how fast the integral unwinds once the target stops railing.</p>"},{"location":"operating/programs/","title":"Humidity programs","text":"<p>Running this rig's programs \u2014 the general vocabulary, with a worked tour of the rig's own <code>programs/demo.yaml</code>.</p> <p>There is no humidity-specific program vocabulary: <code>regulate</code>, <code>ramp</code>, <code>hold</code>, <code>arrive</code>, <code>manual</code>, <code>set</code>, <code>command</code> and <code>wait</code> are flyball's own steps, and this rig uses them exactly as any other rig would \u2014 naming <code>blender.humidity</code> (the controller) or <code>blender</code> (the device) where a step needs one. Every step below names the controller by its field, which is still <code>loop</code> on the wire even though the concept is a controller \u2014 a writable signal has at most one controller, so naming <code>blender.humidity</code> means \"the controller driving it\". Every example omits <code>loop</code> where this rig has only one controller: the rig's <code>default: true</code> controller is used.</p>"},{"location":"operating/programs/#programsdemoyaml","title":"<code>programs/demo.yaml</code>","text":"<p><code>flyball-daemon</code> imports every file under <code>programs/</code> beside the rig file on start, so this one is already in the library once the daemon is up. It's a tour of the rig for the simulator \u2014 manual pumps, closed-loop regulation under each of the two stored tunings, ramps paced two different ways, an unwaited ramp caught by <code>arrive</code>, and an operator wait:</p> <pre><code>name: demo\ndescription: &gt;-\n  Manual pumps, P then PID regulation, ramps, an unwaited ramp caught by\n  arrive, an operator wait.\nsteps:\n  # --- by hand: the controller stays manual, the blender takes demands directly ---\n  - set: { device: blender, values: { dry_flow: 2, wet_flow: 2 } }\n  - hold: { minutes: 2, message: \"2 L/min each way: the chamber heads for the midpoint of the supplies\" }\n  - set: { device: blender, values: { dry_effort: 0.1, wet_effort: 0.9 } }\n  - hold: { minutes: 2, message: \"wet-heavy efforts: humidity climbs\" }\n  - command: { device: blender, device_command: stop }\n  - hold: { minutes: 1, message: \"pumps stopped: drifting back towards ambient\" }\n\n  # --- closed loop: P first, then PID ---\n  - regulate: { setpoint: 60, tuning: gentle }\n  - arrive: { within: 1, readings: 5, timeout: { minutes: 8 }, message: \"settling at 60 under P\" }\n  - hold: { minutes: 1, message: \"holding 60 under P: expect a steady offset\" }\n  - regulate: { setpoint: 60, tuning: brisk }\n  - hold: { minutes: 2, message: \"same setpoint, PID: the integrator closes the offset\" }\n\n  # --- ramps: paced by rate, then by duration ---\n  - ramp: { to: 30, per_minute: 10 }\n  - hold: { minutes: 1, message: \"dry soak at 30\" }\n  - ramp: { to: 75, minutes: 3, wait: false }\n  - hold: { minutes: 1, message: \"ramp to 75 running unattended\" }\n  - arrive: { within: 1.5, readings: 3, timeout: { minutes: 8 }, message: \"catching the ramp\" }\n  - hold: { minutes: 1, message: \"wet soak at 75\" }\n\n  # --- the operator ---\n  - wait:\n      message: \"Kick the chamber if you like (a disturbance, a supply change), then press go\"\n      timeout: { minutes: 5 }\n  - arrive: { within: 1, readings: 5, timeout: { minutes: 8 }, message: \"recovering\" }\n\n  # --- back to manual, pumps off ---\n  - manual: blender.humidity\n  - command: { device: blender, device_command: stop }\n</code></pre> <p>A few things worth noticing:</p> <ul> <li><code>hold</code>'s <code>duration</code> is the primary field, folded flat: <code>hold:   {minutes: 2, message: \"\u2026\"}</code> sets <code>duration</code> directly from <code>minutes</code>   (<code>Duration</code> takes any of its unit keys \u2014 <code>seconds</code>, <code>minutes</code>, <code>hours</code>,   \u2026 \u2014 or a bare number of seconds) rather than nesting <code>duration:   {minutes: 2}</code>. <code>ramp</code>'s <code>pace</code> folds the same way when it's a   <code>Duration</code> (<code>minutes: 3</code> alongside <code>to</code>) or a <code>Speed</code> (<code>per_minute: 10</code>)   \u2014 either reads directly onto the one pace field.</li> <li><code>arrive</code>'s <code>timeout</code> doesn't fold, because <code>loop</code> \u2014 not <code>timeout</code> \u2014   is <code>arrive</code>'s primary field, so <code>timeout: {minutes: 8}</code> stays nested.</li> <li><code>manual: blender.humidity</code> is the bare-scalar form: <code>manual</code>'s only   field, <code>loop</code>, is primary, so naming just a controller (with nothing   else to set) can be the step's whole value, the same way <code>wait:   \"message\"</code> is short for <code>wait: {message: \"message\"}</code>.</li> <li><code>set</code>'s <code>device</code>/<code>values</code> put a demand across several of <code>blender</code>'s   writable signals at once \u2014 <code>dry_flow</code>/<code>wet_flow</code> and   <code>dry_effort</code>/<code>wet_effort</code> are each <code>together</code> pairs, so both members   must be in the one <code>set</code> step.</li> <li><code>command</code>'s <code>device_command</code>, not <code>command</code> \u2014 the step's own tag   already uses that word \u2014 calls <code>blender.stop()</code> exactly as <code>POST   /api/devices/blender/commands/stop</code> would.</li> <li>Every <code>tuning: gentle</code>/<code>tuning: brisk</code> swaps <code>blender.humidity</code>'s law   bumplessly before aiming, by the stem of a file under <code>tunings/</code>.</li> </ul>"},{"location":"operating/programs/#running-it","title":"Running it","text":"<pre><code>flyball program check --local programs/demo.yaml   # validates offline, against the commands installed here\nflyball program check programs/demo.yaml           # validates against the live rig: names a missing tuning or controller\nflyball program run programs/demo.yaml\nflyball program status\nflyball program stop                                # interrupt\n</code></pre> <p><code>flyball program run FILE [--interrupt]</code> sends the file's document to <code>POST /api/programs/run</code>; the rig normalises it and applies the first step before answering. <code>--interrupt</code> stops whatever is already running first \u2014 without it, a program already running refuses a second. At real time this program takes roughly 25 rig-minutes; <code>flyball-daemon rig.yaml sim.yaml --set clock.speed=6</code> runs the simulated clock at 6\u00d7, about four minutes of wall time, to watch the whole tour.</p>"},{"location":"reference/humidity/","title":"<code>humidity</code>","text":"<p>API reference, generated from the source. This package now carries only its own arithmetic and glue \u2014 the hardware drivers (<code>sht4x</code>, <code>sht4x_set</code>, <code>i2c</code>, <code>pwm</code>) live in <code>flyball-linux</code>, a dependency, and the daemon is the generic <code>flyball-daemon</code>; neither is documented here.</p> <p>Registers this package's tagged configs (the blender driver and the simulated plant).</p> <p>Importing <code>humidity</code> -- directly, or through the <code>flyball.configs</code> entry point <code>discover()</code> reads -- makes <code>dual_pump_blender</code> and <code>sim_humidity_chamber</code> valid in a rig file. <code>i2c</code>, <code>sht4x</code>, <code>sht4x_set</code> and <code>pwm</code> come from <code>flyball-linux</code>, a dependency, so importing it too is enough to register those (rig.yaml/sim.yaml don't need this package to do it explicitly).</p>"},{"location":"reference/humidity/#humidity.blender","title":"humidity.blender","text":"<p>The dual-pump blender: a composite actuator that mixes a dry and a wet line to a target %RH.</p> <p>A controller drives <code>humidity</code>; people run the commands. <code>set_flows</code>, <code>set_efforts</code> and <code>stop</code> drive the lines directly and put the blender in that mode; a humidity demand puts it back in BLEND, where <code>commit</code> does the split-range arithmetic once per delivery. <code>mode</code> says which is in force.</p>"},{"location":"reference/humidity/#humidity.blender.DRY","title":"DRY  <code>module-attribute</code>","text":"<pre><code>DRY = Section('dry', 'Dry line')\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.WET","title":"WET  <code>module-attribute</code>","text":"<pre><code>WET = Section('wet', 'Wet line')\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.BlenderError","title":"BlenderError","text":"<p>               Bases: <code>Exception</code></p>"},{"location":"reference/humidity/#humidity.blender.HumidityRailError","title":"HumidityRailError","text":"<p>               Bases: <code>BlenderError</code>, <code>UnachievableError</code></p> <p>The target humidity is outside the range the two lines can mix to.</p> <p>Not raised: the blend rails to the nearer end and <code>humidity</code>'s <code>WriteState.at_limit</code> says so; the remedy is a wetter or drier supply.</p>"},{"location":"reference/humidity/#humidity.blender.SupplyHumiditiesError","title":"SupplyHumiditiesError","text":"<p>               Bases: <code>BlenderError</code>, <code>UnachievableError</code></p> <p>The wet and dry line humidities are not in order. Raised: with no span there is no blend.</p>"},{"location":"reference/humidity/#humidity.blender.SupplyHumiditiesError.__init__","title":"__init__","text":"<pre><code>__init__(humidities: SupplyHumidities) -&gt; None\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.Mode","title":"Mode","text":"<p>               Bases: <code>Labelled</code></p> <p>What is driving the pumps: a humidity demand, or the last command by hand. Starts manual.</p>"},{"location":"reference/humidity/#humidity.blender.Mode.BLEND","title":"BLEND  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>BLEND = ('blend', 'Blending to a target humidity')\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.Mode.MANUAL","title":"MANUAL  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>MANUAL = ('manual', 'Set by hand')\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender","title":"DualPumpBlender","text":"<p>               Bases: <code>Committable</code></p> <p>Two pumps, blended to a target %RH; also settable directly by flow or by effort.</p> <p>A demand on <code>humidity</code> puts the blender in BLEND: <code>commit</code> does the split-range arithmetic once per delivery (<code>calculate_wet_fraction</code>), however many of a new target, a changed supply reading and a new blend flow arrived together -- one pump write. <code>set_flows</code>, <code>set_efforts</code> and <code>stop</code> drive the lines at once and change the mode, so a supply reading re-blends only while blending. The flows and efforts are demands whose readbacks follow whatever is driving the pumps.</p>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.flows","title":"flows  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>flows = Namespace('flows', 'Flows')\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.efforts","title":"efforts  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>efforts = Namespace('efforts', 'Efforts')\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.max_flows","title":"max_flows  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_flows = Namespace('max_flows', 'Max flows')\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.humidities","title":"humidities  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>humidities = Namespace('humidities', 'Flow humidities')\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.supply_defaults","title":"supply_defaults  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supply_defaults = Namespace('supply_defaults', 'Supply humidities when unbound')\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.dry_max_flow","title":"dry_max_flow  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>dry_max_flow = max_flows.config(DRY, 'Dry max flow', FLOW)\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.wet_max_flow","title":"wet_max_flow  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>wet_max_flow = max_flows.config(WET, 'Wet max flow', FLOW)\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.dry_supply_default","title":"dry_supply_default  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>dry_supply_default = supply_defaults.config(DRY, 'Dry line humidity', HUMIDITY)\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.wet_supply_default","title":"wet_supply_default  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>wet_supply_default = supply_defaults.config(WET, 'Wet line humidity', HUMIDITY)\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.dry_supply","title":"dry_supply  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>dry_supply = humidities.input(DRY, 'Dry line humidity', HUMIDITY, default=dry_supply_default)\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.wet_supply","title":"wet_supply  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>wet_supply = humidities.input(WET, 'Wet line humidity', HUMIDITY, default=wet_supply_default)\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.humidity","title":"humidity  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>humidity = Demand('humidity', 'Target humidity', HUMIDITY, limits=(dry_supply, wet_supply))\n</code></pre> <p>Clamped to what the lines can mix: the supply humidities, as they read now.</p>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.dry_flow","title":"dry_flow  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>dry_flow = flows.demand(DRY, 'Dry pump flow', FLOW, limits=(0.0, dry_max_flow))\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.wet_flow","title":"wet_flow  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>wet_flow = flows.demand(WET, 'Wet pump flow', FLOW, limits=(0.0, wet_max_flow))\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.dry_effort","title":"dry_effort  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>dry_effort = efforts.demand(DRY, 'Dry pump effort', EFFORT, limits=(0.0, 1.0))\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.wet_effort","title":"wet_effort  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>wet_effort = efforts.demand(WET, 'Wet pump effort', EFFORT, limits=(0.0, 1.0))\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.expected_humidity","title":"expected_humidity  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>expected_humidity = Output('expected_humidity', 'Expected humidity', HUMIDITY, range=(0.0, 100.0), precision=1)\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.mode","title":"mode  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>mode = Output('mode', 'Mode', vtype=Mode, initial=Mode.MANUAL)\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.blend","title":"blend  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>blend = Namespace('blend', 'Blend')\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.blend_flow","title":"blend_flow  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>blend_flow = blend.setting('flow', 'Blend flow', vtype=BlendFlow, initial=DefaultBlendFlow)\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.wet_fraction","title":"wet_fraction  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>wet_fraction = blend.demand('wet_fraction', 'Wet fraction', WET_FRACTION, limits=(0.0, 1.0))\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.__init__","title":"__init__","text":"<pre><code>__init__(name: str, pumps: DualPumps, *, supply: SupplyHumidities = DefaultHumidities, blend_flow: Positive = 1.0, label: str | None = None) -&gt; None\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.commit","title":"commit","text":"<pre><code>commit(time_ns: int) -&gt; None\n</code></pre> <p>A humidity demand starts blending; while blending, a moved supply re-blends.</p>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.set_blend","title":"set_blend","text":"<pre><code>set_blend(blend_flow: BlendFlow) -&gt; None\n</code></pre> <p>Choose how much air the blend moves.</p> <p>An absolute flow (and what to do if the lines cannot give it), a fraction of the most the blend can move at this mix, or a fraction of the flow guaranteed at every mix. Takes effect at once when blending, else at the next blend.</p>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.set_humidity","title":"set_humidity","text":"<pre><code>set_humidity(humidity: Humidity, blend_flow: BlendFlow | None = None) -&gt; None\n</code></pre> <p>Blend to a humidity at a blend flow, by hand: the controller, if any, goes to manual.</p> <p>Either left out keeps its current value. What a controller does through the <code>humidity</code> demand, done in one go from a program or a form.</p>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.set_fraction","title":"set_fraction","text":"<pre><code>set_fraction(blend_flow: BlendFlow, wet_fraction: float) -&gt; None\n</code></pre> <p>Blend at a wet fraction by hand, at a blend flow; either left out keeps its value.</p>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.set_flows","title":"set_flows","text":"<pre><code>set_flows(dry: Annotated[Flow, dry_flow], wet: Annotated[Flow, wet_flow]) -&gt; None\n</code></pre> <p>Drive each line at a flow. A line left out keeps its current flow.</p>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.set_efforts","title":"set_efforts","text":"<pre><code>set_efforts(dry: Annotated[Normalised, dry_effort], wet: Annotated[Normalised, wet_effort]) -&gt; None\n</code></pre> <p>Drive each line at an effort, 0-1 of full. A line left out keeps its current effort.</p>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlender.stop","title":"stop","text":"<pre><code>stop() -&gt; None\n</code></pre> <p>Stop both pumps at once; a controller driving the target goes to manual.</p>"},{"location":"reference/humidity/#humidity.blender.PumpLineConfig","title":"PumpLineConfig  <code>pydantic-model</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>One line's PWM channel and its limits.</p> <p>Config:</p> <ul> <li><code>extra</code>: <code>forbid</code></li> </ul> <p>Fields:</p> <ul> <li> <code>channel</code>                 (<code>int</code>)             </li> <li> <code>deadband</code>                 (<code>Normalised</code>)             </li> <li> <code>max_flow</code>                 (<code>Positive</code>)             </li> </ul>"},{"location":"reference/humidity/#humidity.blender.PumpLineConfig.model_config","title":"model_config  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>model_config = ConfigDict(extra='forbid')\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.PumpLineConfig.channel","title":"channel  <code>pydantic-field</code>","text":"<pre><code>channel: int\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.PumpLineConfig.deadband","title":"deadband  <code>pydantic-field</code>","text":"<pre><code>deadband: Normalised = 0.0\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.PumpLineConfig.max_flow","title":"max_flow  <code>pydantic-field</code>","text":"<pre><code>max_flow: Positive\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.SupplyConfig","title":"SupplyConfig  <code>pydantic-model</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>The supply lines' humidity, when not followed from a sensor (<code>bound</code>).</p> <p>Config:</p> <ul> <li><code>extra</code>: <code>forbid</code></li> </ul> <p>Fields:</p> <ul> <li> <code>dry</code>                 (<code>Humidity</code>)             </li> <li> <code>wet</code>                 (<code>Humidity</code>)             </li> </ul>"},{"location":"reference/humidity/#humidity.blender.SupplyConfig.model_config","title":"model_config  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>model_config = ConfigDict(extra='forbid')\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.SupplyConfig.dry","title":"dry  <code>pydantic-field</code>","text":"<pre><code>dry: Humidity\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.SupplyConfig.wet","title":"wet  <code>pydantic-field</code>","text":"<pre><code>wet: Humidity\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlenderConfig","title":"DualPumpBlenderConfig","text":"<p>               Bases: <code>DriverConfig[DualPumpBlender]</code></p> <p>Two channels of one PWM chip, blended by <code>commit</code>.</p> <p>The pumps write duties through the <code>PwmLink</code> protocol (flyball-linux's <code>pwm</code>/<code>fake_pwm</code>), not a device of their own: the split-range arithmetic is the blender's, so it owns both channels directly.</p>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlenderConfig.link","title":"link  <code>instance-attribute</code>","text":"<pre><code>link: PwmLinkConfig | str\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlenderConfig.frequency_hz","title":"frequency_hz  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>frequency_hz: Positive = 20000.0\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlenderConfig.dry","title":"dry  <code>instance-attribute</code>","text":"<pre><code>dry: PumpLineConfig\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlenderConfig.wet","title":"wet  <code>instance-attribute</code>","text":"<pre><code>wet: PumpLineConfig\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlenderConfig.blend_flow","title":"blend_flow  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>blend_flow: Positive = 1.0\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlenderConfig.supply","title":"supply  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supply: SupplyConfig | None = None\n</code></pre> <p>A starting supply humidity, for a rig with no sensor bound to <code>dry</code>/<code>wet</code>.</p>"},{"location":"reference/humidity/#humidity.blender.DualPumpBlenderConfig.build","title":"build","text":"<pre><code>build(name: str, label: str | None = None) -&gt; DualPumpBlender\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.expected_humidity_from_flows","title":"expected_humidity_from_flows","text":"<pre><code>expected_humidity_from_flows(flows: SupplyFlows, humidities: SupplyHumidities) -&gt; float | None\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.expected_humidity_from_wet_fraction","title":"expected_humidity_from_wet_fraction","text":"<pre><code>expected_humidity_from_wet_fraction(wet_fraction: Normalised | Limit, humidities: SupplyHumidities) -&gt; float\n</code></pre>"},{"location":"reference/humidity/#humidity.blender.calculate_wet_fraction","title":"calculate_wet_fraction","text":"<pre><code>calculate_wet_fraction(humidities: SupplyHumidities, target: float) -&gt; Normalised | Limit\n</code></pre> <p>The wet fraction for <code>target</code>, or the end it rails to: LOW is all dry, HIGH all wet.</p>"},{"location":"reference/humidity/#humidity.cli","title":"humidity.cli","text":"<p>Command line client for a running <code>humidity-daemon</code>, over its HTTP API.</p> <p>Imports nothing from the domain: the wire format is the contract (plan <code>DEVICE-MODEL-PLAN.md</code> \u00a74). Addresses are whatever the running rig file declares -- <code>blender.humidity</code>, <code>hum_sensors.chamber.humidity</code> for <code>rig.yaml</code>; a <code>--set</code>-only rig may use others.</p>"},{"location":"reference/humidity/#humidity.cli.DEFAULT_URL","title":"DEFAULT_URL  <code>module-attribute</code>","text":"<pre><code>DEFAULT_URL = 'http://127.0.0.1:8000'\n</code></pre>"},{"location":"reference/humidity/#humidity.cli.EXIT_ERROR","title":"EXIT_ERROR  <code>module-attribute</code>","text":"<pre><code>EXIT_ERROR = 1\n</code></pre>"},{"location":"reference/humidity/#humidity.cli.EXIT_UNREACHABLE","title":"EXIT_UNREACHABLE  <code>module-attribute</code>","text":"<pre><code>EXIT_UNREACHABLE = 3\n</code></pre>"},{"location":"reference/humidity/#humidity.cli.CliError","title":"CliError","text":"<p>               Bases: <code>Exception</code></p> <p>Anything the user should see as a one-line message, not a traceback.</p>"},{"location":"reference/humidity/#humidity.cli.CliError.code","title":"code  <code>instance-attribute</code>","text":"<pre><code>code = code\n</code></pre>"},{"location":"reference/humidity/#humidity.cli.CliError.__init__","title":"__init__","text":"<pre><code>__init__(message: str, code: int = EXIT_ERROR) -&gt; None\n</code></pre>"},{"location":"reference/humidity/#humidity.cli.request","title":"request","text":"<pre><code>request(args: Namespace, method: str, path: str, body: Any = None) -&gt; Any\n</code></pre>"},{"location":"reference/humidity/#humidity.cli.detail","title":"detail","text":"<pre><code>detail(response: Response) -&gt; str\n</code></pre> <p>The server's own explanation, if it gave one.</p>"},{"location":"reference/humidity/#humidity.cli.emit","title":"emit","text":"<pre><code>emit(args: Namespace, value: Any) -&gt; None\n</code></pre>"},{"location":"reference/humidity/#humidity.cli.flatten","title":"flatten","text":"<pre><code>flatten(value: dict[str, Any], prefix: str = '') -&gt; str\n</code></pre> <p>Nested dicts as indented <code>key: value</code> lines. Lists stay compact.</p>"},{"location":"reference/humidity/#humidity.cli.parse_values","title":"parse_values","text":"<pre><code>parse_values(pairs: list[str]) -&gt; dict[str, float]\n</code></pre> <p><code>[\"flows.dry=0.4\", \"flows.wet=0.6\"]</code> -&gt; <code>{\"flows.dry\": 0.4, \"flows.wet\": 0.6}</code>.</p>"},{"location":"reference/humidity/#humidity.cli.cmd_status","title":"cmd_status","text":"<pre><code>cmd_status(args: Namespace) -&gt; None\n</code></pre>"},{"location":"reference/humidity/#humidity.cli.cmd_health","title":"cmd_health","text":"<pre><code>cmd_health(args: Namespace) -&gt; None\n</code></pre>"},{"location":"reference/humidity/#humidity.cli.cmd_device","title":"cmd_device","text":"<pre><code>cmd_device(args: Namespace) -&gt; None\n</code></pre>"},{"location":"reference/humidity/#humidity.cli.cmd_read","title":"cmd_read","text":"<pre><code>cmd_read(args: Namespace) -&gt; None\n</code></pre>"},{"location":"reference/humidity/#humidity.cli.cmd_set","title":"cmd_set","text":"<pre><code>cmd_set(args: Namespace) -&gt; None\n</code></pre>"},{"location":"reference/humidity/#humidity.cli.cmd_command","title":"cmd_command","text":"<pre><code>cmd_command(args: Namespace) -&gt; None\n</code></pre>"},{"location":"reference/humidity/#humidity.cli.cmd_stop","title":"cmd_stop","text":"<pre><code>cmd_stop(args: Namespace) -&gt; None\n</code></pre>"},{"location":"reference/humidity/#humidity.cli.cmd_controller","title":"cmd_controller","text":"<pre><code>cmd_controller(args: Namespace) -&gt; None\n</code></pre>"},{"location":"reference/humidity/#humidity.cli.format_frame","title":"format_frame","text":"<pre><code>format_frame(frame: Any, raw: bool = False) -&gt; str\n</code></pre> <p>One sample per line, falling back to JSON for anything unexpected.</p>"},{"location":"reference/humidity/#humidity.cli.cmd_watch","title":"cmd_watch","text":"<pre><code>cmd_watch(args: Namespace) -&gt; None\n</code></pre> <p>Follow the sample stream until interrupted.</p>"},{"location":"reference/humidity/#humidity.cli.parser","title":"parser","text":"<pre><code>parser() -&gt; argparse.ArgumentParser\n</code></pre>"},{"location":"reference/humidity/#humidity.cli.main","title":"main","text":"<pre><code>main(argv: list[str] | None = None) -&gt; int\n</code></pre>"},{"location":"reference/humidity/#humidity.sim","title":"humidity.sim","text":"<p>A humidity chamber, physically parameterised, doubling as a fake PWM chip.</p> <p><code>HumidityChamber</code> mixes a dry and a wet air stream (<code>dry_rh</code>, <code>wet_rh</code>, their own maximum flows) into a chamber of <code>volume_l</code>, leaking towards <code>ambient_rh</code> at <code>exchange_per_min</code>; the chamber's sensor lags the true value by <code>sensor_tau_s</code>. It is two things at once:</p> <ul> <li>a <code>MultiPlant</code> (<code>flyball.sim.furnace</code>) -- <code>sim_daq</code> (<code>hum_sensors</code>) reads   its six named outputs, one per leaf the real <code>hum_sensors</code> declares   (<code>chamber_humidity</code>, <code>chamber_temperature</code>, <code>dry_humidity</code>,   <code>dry_temperature</code>, <code>wet_humidity</code>, <code>wet_temperature</code>);</li> <li>a <code>flyball_linux.links.pwm.PwmLink</code> (<code>configure</code>/<code>enable</code>) -- the real   <code>dual_pump_blender</code> driver (unmodified) drives it exactly as it would a   hardware PWM chip, channel 0 the dry line and channel 1 the wet line,   matching <code>rig.yaml</code>'s <code>dry.channel</code>/<code>wet.channel</code>. So <code>sim.yaml</code>'s   <code>blender</code> device is the genuine <code>DualPumpBlender</code> -- every signal, unit,   role and command matches the real rig exactly, because it is the same   class; <code>expected_humidity</code>, <code>flows.*</code>, <code>efforts.*</code> and <code>mode</code> are the   blender's own bookkeeping, not read back from the chamber, precisely as   on the real rig (plan \u00a71.6).</li> </ul> <p>The dry and wet supplies drift slowly (a sinusoid, decorrelated by phase) and carry their own reading noise, so the \"Inputs\" page is not a flat line; every temperature drifts the same slow way, the chamber's also warming a little with total flow. None of this is physically coupled back into anything but the chamber's own humidity, which is driven by the (drifting) supply humidities actually delivered.</p>"},{"location":"reference/humidity/#humidity.sim.Port","title":"Port  <code>module-attribute</code>","text":"<pre><code>Port = Literal['chamber_humidity', 'chamber_temperature', 'dry_humidity', 'dry_temperature', 'wet_humidity', 'wet_temperature']\n</code></pre>"},{"location":"reference/humidity/#humidity.sim.PORTS","title":"PORTS  <code>module-attribute</code>","text":"<pre><code>PORTS: tuple[Port, ...] = ('chamber_humidity', 'chamber_temperature', 'dry_humidity', 'dry_temperature', 'wet_humidity', 'wet_temperature')\n</code></pre>"},{"location":"reference/humidity/#humidity.sim.DRY_CHANNEL","title":"DRY_CHANNEL  <code>module-attribute</code>","text":"<pre><code>DRY_CHANNEL = 0\n</code></pre> <p>The PWM channel <code>configure</code>/<code>enable</code> treats as the dry line -- <code>rig.yaml</code>'s <code>dry.channel</code>.</p>"},{"location":"reference/humidity/#humidity.sim.WET_CHANNEL","title":"WET_CHANNEL  <code>module-attribute</code>","text":"<pre><code>WET_CHANNEL = 1\n</code></pre> <p>The PWM channel <code>configure</code>/<code>enable</code> treats as the wet line -- <code>rig.yaml</code>'s <code>wet.channel</code>.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamber","title":"HumidityChamber","text":"<p>A <code>MultiPlant</code> and a <code>PwmLink</code> at once: see the module docstring.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamber.inputs","title":"inputs  <code>instance-attribute</code>","text":"<pre><code>inputs: dict[str, float] = {}\n</code></pre> <p>Empty: this plant is driven through <code>configure</code>/<code>enable</code>, not a named input.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamber.output_names","title":"output_names  <code>instance-attribute</code>","text":"<pre><code>output_names = PORTS\n</code></pre>"},{"location":"reference/humidity/#humidity.sim.HumidityChamber.__init__","title":"__init__","text":"<pre><code>__init__(*, volume_l: float, dry_flow_l_per_min: float, wet_flow_l_per_min: float, flow_l_per_min: float, dry_rh: float, wet_rh: float, ambient_rh: float, exchange_per_min: float, initial_rh: float, temperature_c: float, sensor_tau_s: float, noise_rh: float, supply_noise_rh: float, supply_drift_rh: float, supply_drift_period_s: float, temperature_noise_c: float, temperature_drift_c: float, flow_warming_c_per_lpm: float, seed: int | None) -&gt; None\n</code></pre>"},{"location":"reference/humidity/#humidity.sim.HumidityChamber.configure","title":"configure","text":"<pre><code>configure(channel: int, period_ns: int, duty_ns: int) -&gt; None\n</code></pre>"},{"location":"reference/humidity/#humidity.sim.HumidityChamber.enable","title":"enable","text":"<pre><code>enable(channel: int, on: bool) -&gt; None\n</code></pre>"},{"location":"reference/humidity/#humidity.sim.HumidityChamber.output","title":"output","text":"<pre><code>output(port: str) -&gt; float\n</code></pre>"},{"location":"reference/humidity/#humidity.sim.HumidityChamber.advance","title":"advance","text":"<pre><code>advance(time_ns: int) -&gt; None\n</code></pre> <p>Step the chamber to <code>time_ns</code>, in sub-steps short enough for the sensor lag.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamber.feedforward","title":"feedforward","text":"<pre><code>feedforward(port: str, demand: float) -&gt; float\n</code></pre> <p>The <code>wet_fraction</code> that would settle <code>demand</code>, at <code>flow_l_per_min</code>, unclamped.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamber.inverse_feedforward","title":"inverse_feedforward","text":"<pre><code>inverse_feedforward(port: str, drive: float) -&gt; float\n</code></pre> <p>The humidity a steady <code>wet_fraction</code> of <code>drive</code> settles at, at <code>flow_l_per_min</code>.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig","title":"HumidityChamberConfig","text":"<p>               Bases: <code>Config[HumidityChamber]</code></p> <p>A mixing-model chamber; every field is physical, and documents its own unit.</p> <p>Steady state (no drift, no clamp): <code>wet_fraction</code> 0 rests at <code>dry_rh</code>, 1 at <code>wet_rh</code>, in between along that span, diluted further towards <code>ambient_rh</code> by <code>exchange_per_min</code>. Live drive comes from the real <code>dual_pump_blender</code>, through <code>configure</code>/<code>enable</code> (see the module docstring) -- <code>dry_flow_l_per_min</code>/<code>wet_flow_l_per_min</code> should equal that device's own <code>dry.max_flow</code>/<code>wet.max_flow</code>, so what the blender believes it delivers is what the chamber actually receives.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.volume_l","title":"volume_l  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>volume_l: Positive = 20.0\n</code></pre> <p>The chamber's volume, litres.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.dry_flow_l_per_min","title":"dry_flow_l_per_min  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>dry_flow_l_per_min: Positive = 2.0\n</code></pre> <p>The dry line's flow at full duty -- match the blender's <code>dry.max_flow</code>.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.wet_flow_l_per_min","title":"wet_flow_l_per_min  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>wet_flow_l_per_min: Positive = 2.0\n</code></pre> <p>The wet line's flow at full duty -- match the blender's <code>wet.max_flow</code>.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.flow_l_per_min","title":"flow_l_per_min  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>flow_l_per_min: Positive = 1.0\n</code></pre> <p>The nominal total flow <code>feedforward</code>/<code>inverse_feedforward</code> assume; not used live.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.dry_rh","title":"dry_rh  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>dry_rh: Humidity = 10.0\n</code></pre> <p>The dry supply's humidity at rest, before drift, %RH.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.wet_rh","title":"wet_rh  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>wet_rh: Humidity = 90.0\n</code></pre> <p>The wet supply's humidity at rest, before drift, %RH.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.ambient_rh","title":"ambient_rh  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ambient_rh: Humidity = 45.0\n</code></pre> <p>The room's humidity the chamber leaks towards, %RH.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.exchange_per_min","title":"exchange_per_min  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>exchange_per_min: NonNegative = 0.01\n</code></pre> <p>The chamber's air exchange with the room (leak), a fraction of its volume per minute.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.initial_rh","title":"initial_rh  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>initial_rh: Humidity = 40.0\n</code></pre> <p>The chamber's starting humidity, %RH.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.temperature_c","title":"temperature_c  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>temperature_c: Temperature = 21.0\n</code></pre> <p>Every temperature output's steady baseline, \u00b0C.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.sensor_tau_s","title":"sensor_tau_s  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>sensor_tau_s: Positive = 3.0\n</code></pre> <p>The chamber sensor's own first-order lag, seconds.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.noise_rh","title":"noise_rh  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>noise_rh: NonNegative = 0.3\n</code></pre> <p>Gaussian noise on the chamber's humidity reading, %RH.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.supply_noise_rh","title":"supply_noise_rh  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supply_noise_rh: NonNegative = 0.15\n</code></pre> <p>Gaussian noise on the dry/wet humidity readings, each sample, %RH.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.supply_drift_rh","title":"supply_drift_rh  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supply_drift_rh: NonNegative = 2.0\n</code></pre> <p>Amplitude of a slow sinusoidal drift on the supplies' humidity, \u00b1 %RH.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.supply_drift_period_s","title":"supply_drift_period_s  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supply_drift_period_s: Positive = 600.0\n</code></pre> <p>The period of the supply drift, and of the slower temperature drift, seconds.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.temperature_noise_c","title":"temperature_noise_c  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>temperature_noise_c: NonNegative = 0.05\n</code></pre> <p>Gaussian noise on every temperature reading, \u00b0C.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.temperature_drift_c","title":"temperature_drift_c  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>temperature_drift_c: NonNegative = 0.3\n</code></pre> <p>Amplitude of a slow sinusoidal drift on every temperature around <code>temperature_c</code>, \u00b1 \u00b0C.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.flow_warming_c_per_lpm","title":"flow_warming_c_per_lpm  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>flow_warming_c_per_lpm: NonNegative = 0.1\n</code></pre> <p>How much the chamber warms per L/min of total flow through it, \u00b0C per L/min.</p>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>seed: int | None = None\n</code></pre>"},{"location":"reference/humidity/#humidity.sim.HumidityChamberConfig.build","title":"build","text":"<pre><code>build() -&gt; HumidityChamber\n</code></pre>"},{"location":"reference/humidity/#humidity.units","title":"humidity.units","text":"<p>Units and quantities for the humidity rig.</p> <p><code>HUMIDITY</code> and <code>TEMPERATURE</code> are <code>flyball_linux</code>'s own -- <code>hum_sensors</code> is built from its <code>sht4x_set</code>, so reusing its <code>Quantity</code> objects (not just matching their symbols) is what makes a real reading and a sim one the same quantity, <code>is</code>, not just <code>==</code>. <code>FLOW</code> and <code>EFFORT</code> are this rig's own: the <code>Quantity</code>s the blender declares its signals with -- name and unit, nothing else (range, precision and bands live on the signal). The <code>Annotated</code> aliases below type plain pydantic fields (a driver config's <code>max_flow</code>, a supply's <code>dry</code>/<code>wet</code>) where a bare float would lose the unit in the schema -- a <code>Quantity</code> stands as its own <code>Annotated</code> metadata, so the alias needs no separate <code>UnitRef</code>. Bounds are the signal's <code>limits</code>, not the type's, so none are given here.</p>"},{"location":"reference/humidity/#humidity.units.LitrePerMinute","title":"LitrePerMinute  <code>module-attribute</code>","text":"<pre><code>LitrePerMinute = Litre / Minute\n</code></pre>"},{"location":"reference/humidity/#humidity.units.FLOW","title":"FLOW  <code>module-attribute</code>","text":"<pre><code>FLOW = Quantity('flow', LitrePerMinute)\n</code></pre>"},{"location":"reference/humidity/#humidity.units.EFFORT","title":"EFFORT  <code>module-attribute</code>","text":"<pre><code>EFFORT = Quantity('effort', One)\n</code></pre> <p>A pump's drive, 0 to 1 of full.</p>"},{"location":"reference/humidity/#humidity.units.WET_FRACTION","title":"WET_FRACTION  <code>module-attribute</code>","text":"<pre><code>WET_FRACTION = Quantity('wet fraction', One)\n</code></pre> <p>The share of the blend drawn from the wet line, 0 to 1.</p>"},{"location":"reference/humidity/#humidity.units.Humidity","title":"Humidity  <code>module-attribute</code>","text":"<pre><code>Humidity = Annotated[float, HUMIDITY]\n</code></pre> <p>Relative humidity of a stream or the chamber.</p>"},{"location":"reference/humidity/#humidity.units.Temperature","title":"Temperature  <code>module-attribute</code>","text":"<pre><code>Temperature = Annotated[float, TEMPERATURE]\n</code></pre> <p>A sensor's temperature reading: absolute, on the Celsius scale.</p>"},{"location":"reference/humidity/#humidity.units.Flow","title":"Flow  <code>module-attribute</code>","text":"<pre><code>Flow = Annotated[float, FLOW]\n</code></pre> <p>Volumetric flow of a supply stream, as the pumps deliver it.</p>"},{"location":"reference/humidity/#humidity.units.WetFraction","title":"WetFraction  <code>module-attribute</code>","text":"<pre><code>WetFraction = Normalised\n</code></pre> <p>Share of the blend drawn from the wet stream. A ratio, not a quantity.</p>"},{"location":"reference/humidity/#humidity.units.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['EFFORT', 'FLOW', 'HUMIDITY', 'TEMPERATURE', 'WET_FRACTION', 'Flow', 'Humidity', 'LitrePerMinute', 'PercentRH', 'Temperature', 'WetFraction']\n</code></pre>"},{"location":"reference/humidity/pumps/","title":"<code>humidity.pumps</code>","text":""},{"location":"reference/humidity/pumps/#humidity.pumps.drivers","title":"humidity.pumps.drivers","text":""},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PumpDriver","title":"PumpDriver","text":"<p>               Bases: <code>Protocol</code></p>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PumpDriver.effort","title":"effort  <code>property</code>","text":"<pre><code>effort: Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PumpDriver.set_effort","title":"set_effort","text":"<pre><code>set_effort(effort: Normalised) -&gt; Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PumpDriver.stop","title":"stop","text":"<pre><code>stop() -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PwmPump","title":"PwmPump","text":"<p>One <code>PwmLink</code> channel, driven 0-1 of full with an optional deadband.</p> <p>Real hardware (<code>flyball_linux.links.pwm.PwmConfig</code>, tag <code>pwm</code>) and its fake (<code>fake_pwm</code>) share this protocol, so this driver -- and the blender that uses it -- never touches a real chip in a test.</p>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PwmPump.__slots__","title":"__slots__  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>__slots__ = ('_deadband', '_effort', '_link', 'channel', 'period_ns')\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PwmPump.channel","title":"channel  <code>instance-attribute</code>","text":"<pre><code>channel = channel\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PwmPump.period_ns","title":"period_ns  <code>instance-attribute</code>","text":"<pre><code>period_ns = round(1000000000.0 / frequency_hz)\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PwmPump.deadband","title":"deadband  <code>property</code>","text":"<pre><code>deadband: float\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PwmPump.effort","title":"effort  <code>property</code>","text":"<pre><code>effort: Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PwmPump.__init__","title":"__init__","text":"<pre><code>__init__(link: PwmLink, channel: int, frequency_hz: Positive, deadband: Normalised = 0.0) -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PwmPump.calculate_duty_ratio","title":"calculate_duty_ratio","text":"<pre><code>calculate_duty_ratio(effort: float) -&gt; float\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PwmPump.set_effort","title":"set_effort","text":"<pre><code>set_effort(effort: Normalised) -&gt; Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PwmPump.stop","title":"stop","text":"<pre><code>stop() -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.DualPumpDriver","title":"DualPumpDriver","text":"<p>               Bases: <code>Protocol</code></p>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.DualPumpDriver.dry_effort","title":"dry_effort  <code>property</code>","text":"<pre><code>dry_effort: Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.DualPumpDriver.wet_effort","title":"wet_effort  <code>property</code>","text":"<pre><code>wet_effort: Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.DualPumpDriver.efforts","title":"efforts  <code>property</code>","text":"<pre><code>efforts: SupplyEfforts\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.DualPumpDriver.set_efforts","title":"set_efforts","text":"<pre><code>set_efforts(efforts: SupplyEfforts) -&gt; SupplyEfforts\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.DualPumpDriver.stop","title":"stop","text":"<pre><code>stop() -&gt; SupplyEfforts\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PumpPair","title":"PumpPair","text":"<p>               Bases: <code>DualPumpDriver</code></p>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PumpPair.dry","title":"dry  <code>instance-attribute</code>","text":"<pre><code>dry = dry\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PumpPair.wet","title":"wet  <code>instance-attribute</code>","text":"<pre><code>wet = wet\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PumpPair.dry_effort","title":"dry_effort  <code>property</code>","text":"<pre><code>dry_effort: Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PumpPair.wet_effort","title":"wet_effort  <code>property</code>","text":"<pre><code>wet_effort: Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PumpPair.__init__","title":"__init__","text":"<pre><code>__init__(dry: PumpDriver, wet: PumpDriver) -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PumpPair.set_efforts","title":"set_efforts","text":"<pre><code>set_efforts(efforts: SupplyEfforts) -&gt; SupplyEfforts\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PumpPair.set_dry_effort","title":"set_dry_effort","text":"<pre><code>set_dry_effort(effort: Normalised) -&gt; Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PumpPair.set_wet_effort","title":"set_wet_effort","text":"<pre><code>set_wet_effort(effort: Normalised) -&gt; Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PumpPair.stop_dry","title":"stop_dry","text":"<pre><code>stop_dry() -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PumpPair.stop_wet","title":"stop_wet","text":"<pre><code>stop_wet() -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.drivers.PumpPair.stop","title":"stop","text":"<pre><code>stop() -&gt; SupplyEfforts\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual","title":"humidity.pumps.dual","text":""},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps","title":"DualPumps","text":""},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.pumps","title":"pumps  <code>instance-attribute</code>","text":"<pre><code>pumps: DualPumpDriver = pumps\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.max_flows","title":"max_flows  <code>instance-attribute</code>","text":"<pre><code>max_flows: MaxFlows = max_flows\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.units","title":"units  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>units: str | None = units\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.dry_max_flow","title":"dry_max_flow  <code>property</code>","text":"<pre><code>dry_max_flow: Positive\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.wet_max_flow","title":"wet_max_flow  <code>property</code>","text":"<pre><code>wet_max_flow: Positive\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.guaranteed_max_flow","title":"guaranteed_max_flow  <code>property</code>","text":"<pre><code>guaranteed_max_flow: Positive\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.limits","title":"limits  <code>property</code>","text":"<pre><code>limits: PumpsLimits\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.dry_effort","title":"dry_effort  <code>property</code>","text":"<pre><code>dry_effort: Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.wet_effort","title":"wet_effort  <code>property</code>","text":"<pre><code>wet_effort: Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.efforts","title":"efforts  <code>property</code>","text":"<pre><code>efforts: SupplyEfforts\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.dry_flow","title":"dry_flow  <code>property</code>","text":"<pre><code>dry_flow: NonNegative\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.wet_flow","title":"wet_flow  <code>property</code>","text":"<pre><code>wet_flow: NonNegative\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.flows","title":"flows  <code>property</code>","text":"<pre><code>flows: SupplyFlows\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.dry_output","title":"dry_output  <code>property</code>","text":"<pre><code>dry_output: PumpState\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.wet_output","title":"wet_output  <code>property</code>","text":"<pre><code>wet_output: PumpState\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.output","title":"output  <code>property</code>","text":"<pre><code>output: PumpsState\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.blend","title":"blend  <code>property</code>","text":"<pre><code>blend: CurrentBlend\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.total_flow","title":"total_flow  <code>property</code>","text":"<pre><code>total_flow: NonNegative\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.dry_fraction","title":"dry_fraction  <code>property</code>","text":"<pre><code>dry_fraction: Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.wet_fraction","title":"wet_fraction  <code>property</code>","text":"<pre><code>wet_fraction: Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.blend_effort","title":"blend_effort  <code>property</code>","text":"<pre><code>blend_effort: Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.view","title":"view  <code>property</code>","text":"<pre><code>view: PumpsView\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.__init__","title":"__init__","text":"<pre><code>__init__(pumps: DualPumpDriver, max_flows: MaxFlows, units: str | None = None) -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.flow_str","title":"flow_str","text":"<pre><code>flow_str(flow: NonNegative) -&gt; str\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.flows_to_efforts","title":"flows_to_efforts","text":"<pre><code>flows_to_efforts(flows: SupplyFlows) -&gt; SupplyEfforts\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.validate_flows","title":"validate_flows","text":"<pre><code>validate_flows(flows: SupplyFlows, wet_fraction: Normalised | None = None) -&gt; SupplyEfforts\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.raise_flow_overdriven","title":"raise_flow_overdriven","text":"<pre><code>raise_flow_overdriven(flows: SupplyFlows, wet_fraction: Normalised | None = None) -&gt; NoReturn\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.set_blend","title":"set_blend","text":"<pre><code>set_blend(flow: BlendFlow, wet_fraction: Normalised) -&gt; PumpsState\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.efforts_to_outputs","title":"efforts_to_outputs","text":"<pre><code>efforts_to_outputs(efforts: SupplyEfforts) -&gt; PumpsState\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.set_flows","title":"set_flows","text":"<pre><code>set_flows(flows: SupplyFlows) -&gt; PumpsState\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.set_efforts","title":"set_efforts","text":"<pre><code>set_efforts(efforts: SupplyEfforts) -&gt; PumpsState\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.dual.DualPumps.stop","title":"stop","text":"<pre><code>stop() -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.errors","title":"humidity.pumps.errors","text":""},{"location":"reference/humidity/pumps/#humidity.pumps.errors.PumpError","title":"PumpError","text":"<p>               Bases: <code>FlyballError</code></p> <p>Base for everything humidity.pumps raises.</p>"},{"location":"reference/humidity/pumps/#humidity.pumps.errors.PumpErrorGroup","title":"PumpErrorGroup","text":"<p>               Bases: <code>ExceptionGroup</code>, <code>PumpError</code></p>"},{"location":"reference/humidity/pumps/#humidity.pumps.errors.FlowError","title":"FlowError","text":"<p>               Bases: <code>PumpError</code>, <code>UnachievableError</code></p> <p>A requested flow or fraction cannot be applied.</p>"},{"location":"reference/humidity/pumps/#humidity.pumps.errors.FlowsOverdrivenError","title":"FlowsOverdrivenError","text":"<p>               Bases: <code>FlowError</code></p>"},{"location":"reference/humidity/pumps/#humidity.pumps.errors.FlowsOverdrivenError.flows","title":"flows  <code>instance-attribute</code>","text":"<pre><code>flows: SupplyFlows = flows\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.errors.FlowsOverdrivenError.max_flow","title":"max_flow  <code>instance-attribute</code>","text":"<pre><code>max_flow: Positive = max_flow or max_flows.dry + max_flows.wet\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.errors.FlowsOverdrivenError.max_flows","title":"max_flows  <code>instance-attribute</code>","text":"<pre><code>max_flows: MaxFlows = max_flows\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.errors.FlowsOverdrivenError.wet_fraction","title":"wet_fraction  <code>instance-attribute</code>","text":"<pre><code>wet_fraction: Normalised | None = wet_fraction\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.errors.FlowsOverdrivenError.units","title":"units  <code>instance-attribute</code>","text":"<pre><code>units: str | None = units\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.errors.FlowsOverdrivenError.dry_overdriven","title":"dry_overdriven  <code>cached</code> <code>property</code>","text":"<pre><code>dry_overdriven: bool\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.errors.FlowsOverdrivenError.wet_overdriven","title":"wet_overdriven  <code>cached</code> <code>property</code>","text":"<pre><code>wet_overdriven: bool\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.errors.FlowsOverdrivenError.total_overdriven","title":"total_overdriven  <code>cached</code> <code>property</code>","text":"<pre><code>total_overdriven: bool\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.errors.FlowsOverdrivenError.flow_exceeds_max","title":"flow_exceeds_max","text":"<pre><code>flow_exceeds_max(name: str, flow: float, max_flow: float) -&gt; str\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.errors.FlowsOverdrivenError.__init__","title":"__init__","text":"<pre><code>__init__(flows: SupplyFlows, max_flows: MaxFlows, max_flow: Positive | None = None, wet_fraction: Normalised | None = None, units: str | None = None) -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.errors.PumpHardwareError","title":"PumpHardwareError","text":"<p>               Bases: <code>PumpError</code>, <code>HardwareError</code></p> <p>The underlying device failed.</p>"},{"location":"reference/humidity/pumps/#humidity.pumps.types","title":"humidity.pumps.types","text":""},{"location":"reference/humidity/pumps/#humidity.pumps.types.DefaultBlendFlow","title":"DefaultBlendFlow  <code>module-attribute</code>","text":"<pre><code>DefaultBlendFlow = OfGuaranteedMax()\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DefaultHumidities","title":"DefaultHumidities  <code>module-attribute</code>","text":"<pre><code>DefaultHumidities = SupplyHumidities(dry=0.0, wet=100.0)\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DefaultDeadbands","title":"DefaultDeadbands  <code>module-attribute</code>","text":"<pre><code>DefaultDeadbands = SupplyDeadbands(dry=0.0, wet=0.0)\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.MaxFlowsDefault","title":"MaxFlowsDefault  <code>module-attribute</code>","text":"<pre><code>MaxFlowsDefault = MaxFlows(dry=1.0, wet=1.0)\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.BlendFlow","title":"BlendFlow","text":"<pre><code>BlendFlow = Absolute | OfBlendMax | OfGuaranteedMax\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.PumpsMode","title":"PumpsMode","text":"<pre><code>PumpsMode = Blend | SupplyFlows | SupplyEfforts\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.OnOverdrive","title":"OnOverdrive","text":"<p>               Bases: <code>Labelled</code></p> <p>How to handle a requested flow change that exceeds the maximum.</p>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.OnOverdrive.RAISE","title":"RAISE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>RAISE = ('raise', 'Refuse the request')\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.OnOverdrive.CLAMP","title":"CLAMP  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>CLAMP = ('clamp', 'Clamp to the maximum')\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.Absolute","title":"Absolute  <code>dataclass</code>","text":""},{"location":"reference/humidity/pumps/#humidity.pumps.types.Absolute.flow","title":"flow  <code>instance-attribute</code>","text":"<pre><code>flow: Flow\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.Absolute.on_overdrive","title":"on_overdrive  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>on_overdrive: OnOverdrive = OnOverdrive.RAISE\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.Absolute.__init__","title":"__init__","text":"<pre><code>__init__(flow: Flow, on_overdrive: OnOverdrive = OnOverdrive.RAISE) -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.OfBlendMax","title":"OfBlendMax  <code>dataclass</code>","text":""},{"location":"reference/humidity/pumps/#humidity.pumps.types.OfBlendMax.blend_fraction","title":"blend_fraction  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>blend_fraction: Normalised = 1.0\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.OfBlendMax.__init__","title":"__init__","text":"<pre><code>__init__(blend_fraction: Normalised = 1.0) -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.OfGuaranteedMax","title":"OfGuaranteedMax  <code>dataclass</code>","text":""},{"location":"reference/humidity/pumps/#humidity.pumps.types.OfGuaranteedMax.guaranteed_max_fraction","title":"guaranteed_max_fraction  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>guaranteed_max_fraction: Normalised = 1.0\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.OfGuaranteedMax.__init__","title":"__init__","text":"<pre><code>__init__(guaranteed_max_fraction: Normalised = 1.0) -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.Blend","title":"Blend","text":"<p>               Bases: <code>NamedTuple</code></p>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.Blend.flow","title":"flow  <code>instance-attribute</code>","text":"<pre><code>flow: BlendFlow\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.Blend.wet_fraction","title":"wet_fraction  <code>instance-attribute</code>","text":"<pre><code>wet_fraction: Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet","title":"DryWet  <code>dataclass</code>","text":"<p>A value for each line. Arithmetic is elementwise, with another pair or a scalar.</p> <p>Operators return the left operand's class; convert with of when the result means something else (<code>SupplyEfforts.of(flows / max_flows)</code>).</p>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.dry","title":"dry  <code>instance-attribute</code>","text":"<pre><code>dry: float\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.wet","title":"wet  <code>instance-attribute</code>","text":"<pre><code>wet: float\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.__rmul__","title":"__rmul__  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>__rmul__ = __mul__\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.__radd__","title":"__radd__  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>__radd__ = __add__\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.total","title":"total  <code>property</code>","text":"<pre><code>total: float\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.difference","title":"difference  <code>property</code>","text":"<pre><code>difference: float\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.min","title":"min  <code>property</code>","text":"<pre><code>min: float\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.max","title":"max  <code>property</code>","text":"<pre><code>max: float\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.__init__","title":"__init__","text":"<pre><code>__init__(dry: float, wet: float) -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.of","title":"of  <code>classmethod</code>","text":"<pre><code>of(pair: DryWet) -&gt; Self\n</code></pre> <p>The same two values as this class.</p>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.__iter__","title":"__iter__","text":"<pre><code>__iter__() -&gt; Iterator[float]\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.__truediv__","title":"__truediv__","text":"<pre><code>__truediv__(other: DryWet | float) -&gt; Self\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.__rtruediv__","title":"__rtruediv__","text":"<pre><code>__rtruediv__(other: DryWet | float) -&gt; Self\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.__mul__","title":"__mul__","text":"<pre><code>__mul__(other: DryWet | float) -&gt; Self\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.__add__","title":"__add__","text":"<pre><code>__add__(other: DryWet | float) -&gt; Self\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.__sub__","title":"__sub__","text":"<pre><code>__sub__(other: DryWet | float) -&gt; Self\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.__rsub__","title":"__rsub__","text":"<pre><code>__rsub__(other: DryWet | float) -&gt; Self\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.__neg__","title":"__neg__","text":"<pre><code>__neg__() -&gt; Self\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.DryWet.max_with","title":"max_with","text":"<pre><code>max_with(*others: float) -&gt; float\n</code></pre> <p>The larger line, or a larger given value.</p>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyHumidities","title":"SupplyHumidities  <code>dataclass</code>","text":"<p>               Bases: <code>DryWet</code></p>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyHumidities.dry","title":"dry  <code>instance-attribute</code>","text":"<pre><code>dry: Humidity\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyHumidities.wet","title":"wet  <code>instance-attribute</code>","text":"<pre><code>wet: Humidity\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyHumidities.__init__","title":"__init__","text":"<pre><code>__init__(dry: Humidity, wet: Humidity) -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyFlows","title":"SupplyFlows  <code>dataclass</code>","text":"<p>               Bases: <code>DryWet</code></p>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyFlows.dry","title":"dry  <code>instance-attribute</code>","text":"<pre><code>dry: Flow\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyFlows.wet","title":"wet  <code>instance-attribute</code>","text":"<pre><code>wet: Flow\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyFlows.blend","title":"blend  <code>property</code>","text":"<pre><code>blend: CurrentBlend\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyFlows.dry_fraction","title":"dry_fraction  <code>property</code>","text":"<pre><code>dry_fraction: float\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyFlows.wet_fraction","title":"wet_fraction  <code>property</code>","text":"<pre><code>wet_fraction: float\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyFlows.__init__","title":"__init__","text":"<pre><code>__init__(dry: Flow, wet: Flow) -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyFlows.from_blend","title":"from_blend  <code>classmethod</code>","text":"<pre><code>from_blend(total: NonNegative, wet_fraction: Normalised) -&gt; SupplyFlows\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyFlows.from_wet_total","title":"from_wet_total  <code>classmethod</code>","text":"<pre><code>from_wet_total(wet: NonNegative, total: NonNegative) -&gt; SupplyFlows\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyFlows.from_dry_total","title":"from_dry_total  <code>classmethod</code>","text":"<pre><code>from_dry_total(dry: NonNegative, total: NonNegative) -&gt; SupplyFlows\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyFlows.is_valid","title":"is_valid","text":"<pre><code>is_valid(max_flows: MaxFlows) -&gt; bool\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyFlows.to_efforts","title":"to_efforts","text":"<pre><code>to_efforts(max_flows: MaxFlows) -&gt; SupplyEfforts\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyFlows.to_total_humidity","title":"to_total_humidity","text":"<pre><code>to_total_humidity(humidities: SupplyHumidities) -&gt; Percent | None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyFlows.derated","title":"derated","text":"<pre><code>derated(max_flows: MaxFlows) -&gt; SupplyFlows\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyEfforts","title":"SupplyEfforts  <code>dataclass</code>","text":"<p>               Bases: <code>DryWet</code></p>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyEfforts.dry","title":"dry  <code>instance-attribute</code>","text":"<pre><code>dry: Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyEfforts.wet","title":"wet  <code>instance-attribute</code>","text":"<pre><code>wet: Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyEfforts.overdriven","title":"overdriven  <code>property</code>","text":"<pre><code>overdriven: bool\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyEfforts.__init__","title":"__init__","text":"<pre><code>__init__(dry: Normalised, wet: Normalised) -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyEfforts.shares","title":"shares  <code>classmethod</code>","text":"<pre><code>shares(wet_fraction: Normalised) -&gt; SupplyEfforts\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyEfforts.to_flows","title":"to_flows","text":"<pre><code>to_flows(max_flows: MaxFlows) -&gt; SupplyFlows\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyEfforts.derated","title":"derated","text":"<pre><code>derated() -&gt; SupplyEfforts\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyDeadbands","title":"SupplyDeadbands  <code>dataclass</code>","text":"<p>               Bases: <code>DryWet</code></p>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyDeadbands.dry","title":"dry  <code>instance-attribute</code>","text":"<pre><code>dry: Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.SupplyDeadbands.wet","title":"wet  <code>instance-attribute</code>","text":"<pre><code>wet: Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.MaxFlows","title":"MaxFlows  <code>dataclass</code>","text":"<p>               Bases: <code>DryWet</code></p>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.MaxFlows.dry","title":"dry  <code>instance-attribute</code>","text":"<pre><code>dry: Positive\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.MaxFlows.wet","title":"wet  <code>instance-attribute</code>","text":"<pre><code>wet: Positive\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.MaxFlows.guaranteed","title":"guaranteed  <code>property</code>","text":"<pre><code>guaranteed: Positive\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.MaxFlows.__init__","title":"__init__","text":"<pre><code>__init__(dry: Positive, wet: Positive) -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.MaxFlows.flows_at_blend","title":"flows_at_blend","text":"<pre><code>flows_at_blend(wet_fraction: Normalised) -&gt; SupplyFlows\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.MaxFlows.flow_at_blend","title":"flow_at_blend","text":"<pre><code>flow_at_blend(wet_fraction: Normalised) -&gt; NonNegative\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.MaxFlows.to_efforts","title":"to_efforts","text":"<pre><code>to_efforts(flows: SupplyFlows) -&gt; SupplyEfforts\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.MaxFlows.to_flows","title":"to_flows","text":"<pre><code>to_flows(efforts: SupplyEfforts) -&gt; SupplyFlows\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.MaxFlows.efforts_at_blend","title":"efforts_at_blend","text":"<pre><code>efforts_at_blend(wet_fraction: Normalised) -&gt; SupplyEfforts\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.PumpState","title":"PumpState  <code>dataclass</code>","text":""},{"location":"reference/humidity/pumps/#humidity.pumps.types.PumpState.effort","title":"effort  <code>instance-attribute</code>","text":"<pre><code>effort: Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.PumpState.flow","title":"flow  <code>instance-attribute</code>","text":"<pre><code>flow: Flow\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.PumpState.__init__","title":"__init__","text":"<pre><code>__init__(effort: Normalised, flow: Flow) -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.PumpsState","title":"PumpsState  <code>dataclass</code>","text":""},{"location":"reference/humidity/pumps/#humidity.pumps.types.PumpsState.efforts","title":"efforts  <code>instance-attribute</code>","text":"<pre><code>efforts: SupplyEfforts\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.PumpsState.flows","title":"flows  <code>instance-attribute</code>","text":"<pre><code>flows: SupplyFlows\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.PumpsState.__init__","title":"__init__","text":"<pre><code>__init__(efforts: SupplyEfforts, flows: SupplyFlows) -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.CurrentBlend","title":"CurrentBlend  <code>dataclass</code>","text":""},{"location":"reference/humidity/pumps/#humidity.pumps.types.CurrentBlend.wet_fraction","title":"wet_fraction  <code>instance-attribute</code>","text":"<pre><code>wet_fraction: Normalised\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.CurrentBlend.flow","title":"flow  <code>instance-attribute</code>","text":"<pre><code>flow: Flow\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.CurrentBlend.__init__","title":"__init__","text":"<pre><code>__init__(wet_fraction: Normalised, flow: Flow) -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.PumpsLimits","title":"PumpsLimits  <code>dataclass</code>","text":"<p>What the built pumps can do. Config: fixed once built.</p>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.PumpsLimits.max_flows","title":"max_flows  <code>instance-attribute</code>","text":"<pre><code>max_flows: MaxFlows\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.PumpsLimits.guaranteed_max_flow","title":"guaranteed_max_flow  <code>instance-attribute</code>","text":"<pre><code>guaranteed_max_flow: Positive\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.PumpsLimits.units","title":"units  <code>instance-attribute</code>","text":"<pre><code>units: str | None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.PumpsLimits.__init__","title":"__init__","text":"<pre><code>__init__(max_flows: MaxFlows, guaranteed_max_flow: Positive, units: str | None) -&gt; None\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.PumpsView","title":"PumpsView  <code>dataclass</code>","text":"<p>Limits and state at one instant.</p>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.PumpsView.limits","title":"limits  <code>instance-attribute</code>","text":"<pre><code>limits: PumpsLimits\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.PumpsView.state","title":"state  <code>instance-attribute</code>","text":"<pre><code>state: PumpsState\n</code></pre>"},{"location":"reference/humidity/pumps/#humidity.pumps.types.PumpsView.__init__","title":"__init__","text":"<pre><code>__init__(limits: PumpsLimits, state: PumpsState) -&gt; None\n</code></pre>"},{"location":"rig/config/","title":"Configuration","text":"<p><code>rig.yaml</code> for this rig, field by field \u2014 and <code>sim.yaml</code>, the overlay that runs it with no hardware.</p>"},{"location":"rig/config/#rigyaml-the-real-rig","title":"<code>rig.yaml</code>: the real rig","text":"<pre><code>name: humidity\n\nlinks:\n  i2c1: { tag: i2c, bus: 1 }\n  pwm0: { tag: pwm, chip: 0 }\n\ndevices:\n  hum_sensors:                             # hum_sensors.chamber/dry/wet .humidity/.temperature [RP]\n    driver: sht4x_set\n    label: Humidity sensors\n    poll_s: 1\n    config:\n      link: i2c1\n      sensors:\n        chamber: { address: 0x44 }\n        dry: { address: 0x45 }\n        wet: { address: 0x46 }\n    signals:\n      chamber: { signals: { humidity: { warn: [20, 80] } } }\n      dry: { poll_s: 5 }     # the supply lines drift slowly; no need to poll them as often\n      wet: { poll_s: 5 }\n\n  blender:                                 # two pumps blended into one settable humidity\n    driver: dual_pump_blender\n    label: Pump blender\n    poll_s: 1\n    config:\n      link: pwm0\n      dry: { channel: 0, deadband: 0.05, max_flow: 2.0 }   # L/min\n      wet: { channel: 1, deadband: 0.05, max_flow: 2.0 }\n      blend_flow: 1.0\n    bound: { dry: hum_sensors.dry.humidity, wet: hum_sensors.wet.humidity }\n\ncontrollers:\n  blender.humidity:\n    signal: hum_sensors.chamber.humidity\n    law: { tag: PI, kp: 0.8, ki: 0.02, tt: 60 }\n    default: true\n</code></pre> <p>Two links, two devices, one controller. <code>links</code> are transports, not devices, and neither is this package's own any more \u2014 both <code>i2c</code> and <code>pwm</code> are <code>flyball-linux</code> links (<code>examples/humidity/pyproject.toml</code> depends on <code>flyball-linux[i2c]</code>; this rig has no hardware driver of its own left, only <code>dual_pump_blender</code>'s split-range arithmetic and <code>sim_humidity_chamber</code>'s plant). <code>i2c1</code> is the Pi's I\u00b2C bus 1 (<code>flyball_linux.links.i2c.I2cConfig</code>, <code>/dev/i2c-1</code> through <code>smbus2</code>); <code>pwm0</code> is PWM chip 0 (<code>flyball_linux.links.pwm.PwmConfig</code>, sysfs <code>/sys/class/pwm/pwmchip0</code>, no extra library). Field by field:</p> <ul> <li><code>hum_sensors.config.sensors</code> declares the device's tree \u2014 one   namespace per key, each an SHT4x at that I\u00b2C address. This is   <code>sht4x_set</code>'s own config (<code>flyball_linux.devices.chips.sht4x</code>), not an   envelope key, so the tree can't be changed by <code>signals:</code> overrides, only   by editing <code>sensors:</code> itself.</li> <li><code>hum_sensors.signals</code> is envelope overrides only: <code>chamber</code>'s   <code>humidity</code> gets a warn band, <code>dry</code> and <code>wet</code> are polled every 5 s instead   of inheriting the device's <code>poll_s: 1</code>.</li> <li><code>blender.config</code> is the two pump lines (PWM channel, deadband,   max flow) and the starting <code>blend_flow</code>; <code>link: pwm0</code> is driven directly   through <code>flyball_linux</code>'s <code>PwmLink</code> protocol (<code>configure</code>/<code>enable</code>) \u2014   the blender owns both channels itself rather than wrapping a   <code>pwm_channel</code> device each, since the split-range arithmetic is its own.   A <code>frequency_hz</code> field (default 20 000 Hz) is also available in   <code>config</code>, shared by both lines, if <code>rig.yaml</code> needs to override it.</li> <li><code>blender.bound</code> wires the blender to follow the two supply sensors'   humidity directly \u2014 see The blender device.</li> <li><code>controllers.blender.humidity</code> is named by its target (<code>blender</code>'s   <code>humidity</code> signal), regulates the chamber's published humidity through a   PI law, and is <code>default: true</code> \u2014 the controller a program step or a   <code>flyball</code> command uses when it names none.</li> </ul> <p>Run it: <code>flyball-daemon rig.yaml</code> \u2014 real hardware, loopback-only by default. This package has no daemon of its own; the generic <code>flyball-daemon</code> also picks up a <code>tunings/</code> (and a <code>programs/</code>) directory beside the rig file automatically \u2014 see Configuration: tunings below.</p>"},{"location":"rig/config/#simyaml-the-same-addresses-no-hardware","title":"<code>sim.yaml</code>: the same addresses, no hardware","text":"<pre><code>name: humidity-sim\nclock: { speed: 1 }\n\nlinks:\n  i2c1: null\n  pwm0: null\n  chamber:\n    tag: sim_humidity_chamber\n    volume_l: 3.0                  # litres\n    dry_flow_l_per_min: 2.0        # match blender.dry.max_flow below\n    wet_flow_l_per_min: 2.0        # match blender.wet.max_flow below\n    flow_l_per_min: 1.0            # nominal total, feedforward only\n    dry_rh: 10.0                   # %RH, before drift\n    wet_rh: 90.0                   # %RH, before drift\n    ambient_rh: 45.0               # %RH the chamber leaks towards\n    exchange_per_min: 0.01         # fraction of volume/min exchanged with the room\n    initial_rh: 40.0               # %RH\n    temperature_c: 21.0            # \u00b0C, every temperature's steady baseline\n    sensor_tau_s: 3.0              # s, the chamber sensor's own lag\n    noise_rh: 0.3                  # %RH, Gaussian, chamber reading only\n    supply_noise_rh: 0.15          # %RH, Gaussian, dry/wet readings each sample\n    supply_drift_rh: 2.0           # \u00b1 %RH, slow sinusoidal supply wander (wet: 88-92)\n    supply_drift_period_s: 600.0   # s, period of the supply and temperature drift\n    temperature_noise_c: 0.05      # \u00b0C, Gaussian, every temperature reading\n    temperature_drift_c: 0.3       # \u00b1 \u00b0C, slow sinusoidal drift around temperature_c\n    flow_warming_c_per_lpm: 0.1    # \u00b0C per L/min: chamber warms a little under flow\n    seed: 7\n\ndevices:\n  hum_sensors:\n    driver: sim_daq\n    label: Humidity sensors (simulated)\n    poll_s: 1\n    config:\n      link: chamber\n      ports:\n        chamber.humidity: { port: chamber_humidity, quantity: humidity, unit: \"%RH\" }\n        chamber.temperature: { port: chamber_temperature, quantity: temperature, unit: \"\u00b0C\" }\n        dry.humidity: { port: dry_humidity, quantity: humidity, unit: \"%RH\" }\n        dry.temperature: { port: dry_temperature, quantity: temperature, unit: \"\u00b0C\" }\n        wet.humidity: { port: wet_humidity, quantity: humidity, unit: \"%RH\" }\n        wet.temperature: { port: wet_temperature, quantity: temperature, unit: \"\u00b0C\" }\n\n  blender:\n    driver: dual_pump_blender               # the *real* driver: the chamber stands in for pwm0\n    label: Pump blender (simulated)\n    poll_s: 1\n    config:\n      link: chamber\n      dry: { channel: 0, deadband: 0.05, max_flow: 2.0 }   # L/min\n      wet: { channel: 1, deadband: 0.05, max_flow: 2.0 }   # L/min\n      blend_flow: 1.0\n    bound: { dry: hum_sensors.dry.humidity, wet: hum_sensors.wet.humidity }\n\n# blender.humidity -&gt; hum_sensors.chamber.humidity: the same addresses as\n# rig.yaml, so its controllers entry needs no override here.\n</code></pre> <p>An overlay is a second file passed alongside the first \u2014 <code>flyball-daemon rig.yaml sim.yaml</code> \u2014 merged later-over-earlier: <code>null</code> deletes a key (here, <code>i2c1</code> and <code>pwm0</code>, so no real link is built), a new link (<code>chamber</code>, a <code>sim_humidity_chamber</code> plant \u2014 see <code>HumidityChamber</code> below) is added, and <code>hum_sensors</code> swaps its real driver for the generic <code>sim_daq</code>. <code>blender</code> keeps its real driver, <code>dual_pump_blender</code>, unmodified: <code>HumidityChamber</code> doubles as a <code>flyball_linux.links.pwm.PwmLink</code> (<code>configure</code>/<code>enable</code>), so the blender drives it exactly as it would drive <code>pwm0</code> -- channel 0 the dry line, channel 1 the wet line, matching <code>rig.yaml</code>'s <code>dry.channel</code>/<code>wet.channel</code>. <code>dry.max_flow</code>/<code>wet.max_flow</code> above are set equal to the chamber's own <code>dry_flow_l_per_min</code>/<code>wet_flow_l_per_min</code>, so what the blender believes it delivers is what the chamber actually receives.</p> <p>Every <code>hum_sensors</code> address <code>rig.yaml</code> declares is mirrored here, same unit and access \u2014 <code>sim_daq</code>'s <code>ports:</code> keys may contain a dot (<code>chamber.humidity: {...}</code>), which puts that port in a namespace exactly as <code>sht4x_set</code> does. Because <code>blender</code> is the genuine <code>DualPumpBlender</code> class in both files, every one of its signals, units, access, limits and commands match exactly \u2014 <code>dry_flow</code>/<code>wet_flow</code>/<code>dry_effort</code>/<code>wet_effort</code>/ <code>blend_flow</code>/<code>expected_humidity</code> are the blender's own bookkeeping (from its configured <code>max_flow</code>s and the supply humidity it observes through <code>bound</code>), not read back from the chamber, precisely as on the real rig. A program, dashboard or session built against <code>rig.yaml</code> runs unchanged against the overlay.</p>"},{"location":"rig/config/#the-plant-sim_humidity_chamber","title":"The plant: <code>sim_humidity_chamber</code>","text":"<p><code>humidity.sim.HumidityChamber</code> is two things at once:</p> <ul> <li>a <code>MultiPlant</code> \u2014 <code>sim_daq</code> (<code>hum_sensors</code>) reads its six named outputs,   one per leaf <code>hum_sensors</code> declares (<code>chamber_humidity</code>,   <code>chamber_temperature</code>, <code>dry_humidity</code>, <code>dry_temperature</code>,   <code>wet_humidity</code>, <code>wet_temperature</code>);</li> <li>a <code>PwmLink</code> (<code>configure</code>/<code>enable</code>) \u2014 the real <code>dual_pump_blender</code> driver   drives it exactly as it would a hardware PWM chip.</li> </ul> <p>The chamber mixes the dry and wet lines' actual delivered flow (each line's channel duty times its own <code>..._flow_l_per_min</code>) into a chamber of <code>volume_l</code>, settling on the mixing arithmetic (<code>wet_fraction</code> 0 rests at <code>dry_rh</code>, 1 at <code>wet_rh</code>, in between along that span), diluted further towards <code>ambient_rh</code> at <code>exchange_per_min</code>; the chamber's own sensor lags the true value by <code>sensor_tau_s</code>, plus Gaussian noise (<code>noise_rh</code>, seeded by <code>seed</code>). The dry and wet supplies drift slowly \u2014 a sinusoid of \u00b1<code>supply_drift_rh</code> over <code>supply_drift_period_s</code>, decorrelated by phase so they don't move in lockstep \u2014 and carry their own reading noise (<code>supply_noise_rh</code>); this drift feeds the real physics (the blender observes it through <code>bound</code>, same as a real sensor's drift would), not just the display. Every temperature drifts the same slow way around <code>temperature_c</code> with its own noise (<code>temperature_noise_c</code>), the chamber's also warming a little under total flow (<code>flow_warming_c_per_lpm</code>).</p>"},{"location":"rig/config/#tunings","title":"Tunings","text":"<p><code>examples/humidity/tunings/*.yaml</code> \u2014 each file a control law config, named by its filename stem \u2014 are loaded onto <code>rig.tunings</code> when <code>flyball-daemon</code> starts (<code>--tunings DIR</code> to point elsewhere; default <code>tunings/</code> beside the first rig file). This rig ships two:</p> <pre><code># tunings/gentle.yaml -- the demo's starting law\n# Proportional only, on top of the blender's feedforward: settles a little\n# short of the setpoint and never oscillates.\ntag: P\nkp: 0.5\n</code></pre> <pre><code># tunings/brisk.yaml\n# PID for the simulated chamber: 1 s samples on a ~6 s lag at full flow, so\n# the derivative gain is kept small -- at 8 it drove the pumps bang-bang on\n# sensor noise. Integral time 10 s; tracking time 5 s for anti-windup.\ntag: PID\nkp: 0.8\nki: 0.08\nkd: 1.0\ntt: 5\n</code></pre> <p>A <code>regulate</code> program step's <code>tuning</code> names one by its stem \u2014 <code>{setpoint: 45, tuning: brisk}</code> swaps <code>blender.humidity</code>'s law bumplessly before aiming \u2014 see Humidity programs. Neither file changes <code>rig.yaml</code>'s own <code>controllers.blender.humidity.law</code> (the PI gains the controller starts with); they're alternatives a program or an operator picks at runtime.</p>"},{"location":"rig/config/#-set-and-multiple-overlays","title":"<code>--set</code> and multiple overlays","text":"<p><code>--set devices.blender.config.blend_flow=1.5</code> overrides one value from the command line, applied after every file. A third file (say, a noisier plant) would overlay both <code>rig.yaml</code> and <code>sim.yaml</code> the same way \u2014 later file wins, in the order given.</p>"},{"location":"rig/dual-pumps/","title":"The blender device: dual pumps","text":"<p>How a demand becomes two pump flows: blend policy, supply humidities, limits.</p> <p><code>DualPumpBlender</code> (<code>examples/humidity/src/humidity/blender.py</code>, driver tag <code>dual_pump_blender</code>) is a composite device: a controller drives its <code>humidity</code> demand, and <code>commit</code> does the split-range arithmetic once per delivery, however many of a new target, a changed supply reading and a new blend flow arrived together \u2014 one pump write per delivery, not one per input. People run its commands, which drive the lines at once.</p>"},{"location":"rig/dual-pumps/#the-signals-and-what-drives-them","title":"The signals, and what drives them","text":"signal role what it is <code>humidity</code> demand the split-range target; a controller usually owns this <code>flows.dry</code>, <code>flows.wet</code> demand each line's flow, L/min: the readback, set by <code>set_flows</code> <code>efforts.dry</code>, <code>efforts.wet</code> demand each line's effort, 0\u20131 of full: the readback, set by <code>set_efforts</code> <code>expected_humidity</code> output what the current pump outputs should actually deliver <code>mode</code> output <code>blend</code> (a humidity demand) or <code>manual</code> (the last command by hand) <code>blend.flow</code> setting the flow a <code>humidity</code> demand mixes to; <code>set_blend</code> <code>blend.wet_fraction</code> demand the share drawn from the wet line: the readback while blending; <code>set_fraction</code> sets it directly <code>max_flows.dry</code>, <code>max_flows.wet</code> config each line's maximum, the limit of its flow demand <p>The mode decides what a delivery does. A <code>humidity</code> demand puts the blender in <code>blend</code>, where a moved supply reading re-blends; <code>set_flows</code>, <code>set_efforts</code>, <code>set_fraction</code> and <code>stop</code> drive the lines at once and put it in <code>manual</code>, where a supply reading changes nothing, so a manual flow is not undone by the next reading. The <code>dry</code>/<code>wet</code> sections tag each line's signals across <code>flows</code>, <code>efforts</code> and <code>max_flows</code>, so a UI can show the tree by line as well as by kind. The blender is never polled: it pushes its readbacks from <code>commit</code> and from its commands.</p>"},{"location":"rig/dual-pumps/#the-split-range-arithmetic","title":"The split-range arithmetic","text":"<p><code>calculate_wet_fraction(humidities, target)</code> turns a target <code>%RH</code> into a wet fraction 0\u20131, given the supply humidities:</p> <pre><code>def calculate_wet_fraction(humidities: SupplyHumidities, target: float) -&gt; Normalised | Limit:\n    if humidities.wet &lt;= humidities.dry:\n        raise SupplyHumiditiesError(humidities)\n    if target &lt; humidities.dry:\n        return Limit.LOW\n    if target &gt; humidities.wet:\n        return Limit.HIGH\n    return (target - humidities.dry) / humidities.difference\n</code></pre> <p>A target outside <code>[dry, wet]</code> doesn't raise \u2014 it rails to the nearer end (<code>flyball.core.signal.Limit</code>, the same enum the rig's clamp reports), and the delivery's write state for <code>humidity</code> carries it as <code>at_limit: \"low\"</code> or <code>\"high\"</code>. Only a wet-humidity-not-greater-than-dry configuration raises (<code>SupplyHumiditiesError</code>): with no span there is nothing to blend.</p> <p>The fraction, <code>blend</code> (the setting) and <code>pumps.set_blend(...)</code> then give the actual dry/wet flows, which each line's <code>PwmPump.set_effort</code> turns into a PWM duty ratio (<code>calculate_duty_ratio</code>, honouring that line's <code>deadband</code>) and writes straight to <code>flyball-linux</code>'s <code>PwmLink</code> protocol \u2014 <code>configure(channel, period_ns, duty_ns)</code> then <code>enable(channel, ...)</code>. The blender owns both channels directly rather than wrapping a <code>pwm_channel</code> device each: the split-range maths is its own, so one PWM chip link (<code>config.link</code>) is enough.</p>"},{"location":"rig/dual-pumps/#following-the-supply-lines","title":"Following the supply lines","text":"<pre><code>bound: { dry: hum_sensors.dry.humidity, wet: hum_sensors.wet.humidity }\n</code></pre> <p>The blender declares two inputs, <code>dry</code> and <code>wet</code> (<code>supply.input(...)</code> in the class body); the rig file binds them to the sensors. Whenever a bound signal publishes, the rig commits the blender, and <code>commit</code> reads the supply's newest values from the router (<code>self.dry_supply.value</code>) \u2014 there is no callback and no copy on the device. While blending, that re-blends; in any other mode it does nothing. A rig with no sensor bound uses <code>config.supply</code> (the <code>supply_defaults</code> config signals) instead. <code>expected_humidity</code> is pushed after every pump write from the pumps' actual output and the current supply humidities.</p>"},{"location":"rig/dual-pumps/#limits","title":"Limits","text":"<ul> <li><code>humidity</code> is clamped to <code>[0, 100]</code> by the signal's own <code>limits</code>, then   railed against the two supplies as above \u2014 so a demand can be within   <code>[0, 100]</code> and still rail if it's outside what the current dry/wet   supply can reach.</li> <li><code>flows.dry</code>/<code>flows.wet</code> are each limited to their pump's <code>max_flow</code>   (<code>rig.yaml</code>: 2.0 L/min per line) \u2014 the limit is the <code>max_flows.dry</code>   config signal, so the command form shows it and the rig clamps to it.</li> <li><code>efforts.dry</code>/<code>efforts.wet</code> are limited to <code>[0, 1]</code>.</li> <li><code>set_humidity(humidity, blend_flow)</code> blends to a target by hand, at a   blend flow, in one go \u2014 what a controller does through the <code>humidity</code>   demand; <code>set_flows</code> and <code>set_efforts</code> take one value per line. An   argument left out keeps its current value. They are refused while a controller drives   <code>humidity</code> (put it in manual, or detach it); <code>stop</code> is exempt and stops   both pumps at once, whatever is driving them.</li> </ul>"},{"location":"rig/dual-pumps/#config","title":"Config","text":"<pre><code>blender:\n  driver: dual_pump_blender\n  label: Pump blender\n  config:\n    link: pwm0\n    dry: { channel: 0, deadband: 0.05, max_flow: 2.0 }   # L/min\n    wet: { channel: 1, deadband: 0.05, max_flow: 2.0 }\n    blend_flow: 1.0\n  bound: { dry: hum_sensors.dry.humidity, wet: hum_sensors.wet.humidity }\n</code></pre> <p><code>link</code> names a PWM chip link (<code>pwm0: { tag: pwm, chip: 0 }</code>, from <code>flyball-linux</code> \u2014 sysfs <code>/sys/class/pwm/pwmchip0</code>, no extra library); <code>dry</code>/<code>wet</code> are each a channel number, a deadband (0\u20131, below which the pump doesn't turn) and a max flow. <code>config</code> also takes a <code>frequency_hz</code> (default 20 000 Hz), the PWM carrier both lines share. A rig with no sensor bound to <code>dry</code>/<code>wet</code> may give a starting <code>supply: { dry: \u2026, wet: \u2026 }</code> in <code>config</code> instead of <code>bound</code> \u2014 see <code>DualPumpBlenderConfig.supply</code> in <code>blender.py</code>.</p>"},{"location":"rig/sht4x/","title":"The sensor device: SHT4x","text":"<p>How the sensor is read, at what rate, in what units, and what it reports when it fails.</p> <p>This rig has no sensor driver of its own: <code>sht4x</code>/<code>sht4x_set</code> live in <code>flyball-linux</code> (<code>linux/src/flyball_linux/devices/chips/sht4x.py</code>), a dependency of this package (<code>examples/humidity/pyproject.toml</code>: <code>flyball-linux[i2c]</code>). Both are built on <code>Sht4xSensor</code> \u2014 one chip on an <code>I2cLink</code> (<code>flyball_linux.links.i2c</code>: register reads/writes at an address, plus raw <code>write</code>/<code>read</code> for a chip with no register map, like this one):</p> driver tag class tree <code>sht4x</code> <code>Sht4x</code> one chip on the device root: <code>humidity</code>, <code>temperature [RP]</code> <code>sht4x_set</code> <code>Sht4xSet</code> several chips, one atomic namespace each <p>This rig uses <code>sht4x_set</code> for <code>hum_sensors</code> \u2014 see Devices and signals for the three namespaces it declares.</p>"},{"location":"rig/sht4x/#one-i2c-transaction-command-to-decode","title":"One I2C transaction, command to decode","text":"<p><code>Sht4xSensor.read</code> sends the measure command, waits the conversion time, and reads back six bytes, in one call:</p> <pre><code>COMMANDS: dict[str, tuple[int, float]] = {\n    \"high\": (0xFD, 0.0083),\n    \"medium\": (0xF6, 0.0045),\n    \"low\": (0xE0, 0.0016),\n}\n\ndef read(self) -&gt; tuple[float, float]:\n    command, wait_s = COMMANDS[self.precision]\n    self.link.write(self.address, [command])\n    if self.sleep:\n        time.sleep(wait_s)\n    return decode(self.link.read(self.address, 6))\n</code></pre> <p><code>precision</code> (<code>\"high\"</code>/<code>\"medium\"</code>/<code>\"low\"</code>, default <code>\"high\"</code>) picks the command byte and the datasheet's own maximum conversion time for it; this rig doesn't override it, so every sensor reads at high precision, no on-die heater. <code>decode</code> checks two CRC-8 bytes (polynomial <code>0x31</code>, initial <code>0xFF</code>) and returns <code>(temperature, humidity)</code>:</p> <pre><code>def decode(frame: bytes) -&gt; tuple[float, float]:\n    if len(frame) != 6:\n        raise HardwareError(f\"SHT4x reply is {len(frame)} bytes, not 6\")\n    for word, crc in ((frame[0:2], frame[2]), (frame[3:5], frame[5])):\n        if crc8(word) != crc:\n            raise HardwareError(f\"SHT4x CRC mismatch in {frame.hex()}\")\n    raw_t = int.from_bytes(frame[0:2], \"big\")\n    raw_h = int.from_bytes(frame[3:5], \"big\")\n    temperature = -45.0 + 175.0 * raw_t / 65535.0\n    humidity = min(100.0, max(0.0, -6.0 + 125.0 * raw_h / 65535.0))\n    return temperature, humidity\n</code></pre> <p>A short reply or a CRC mismatch raises <code>HardwareError</code> directly \u2014 no sensor-specific error type \u2014 so the polling loop takes the device offline rather than delivering a bad sample (see Failure modes). Humidity is clamped to <code>[0, 100]</code> in the decode; temperature is not \u2014 a sensor can read below 0 \u00b0C or above the datasheet's usual span without being clipped.</p>"},{"location":"rig/sht4x/#sht4xset-several-sensors-one-namespace-each","title":"<code>Sht4xSet</code>: several sensors, one namespace each","text":"<p>Each sensor is its own <code>NodeSpec(atomic=True, children=(humidity, temperature))</code>, so <code>hum_sensors.dry</code> is read as one <code>Sample</code> \u2014 one I2C transaction \u2014 never mixed with <code>chamber</code>'s or <code>wet</code>'s. <code>read(time_ns, node)</code>:</p> <ul> <li>given no node (the periodic poll), reads only the namespaces due \u2014   at least <code>0.9 \u00d7 poll_s</code> since the last read, on each namespace's own   period: <code>chamber</code> roughly every second, <code>dry</code>/<code>wet</code> every five, per   <code>rig.yaml</code>'s overrides;</li> <li>given a node directly (<code>rig.read(hum_sensors.dry, fresh=True)</code>), reads it   regardless of when it was last due.</li> </ul> <p>At least one sensor is required \u2014 an <code>sht4x_set</code> with an empty <code>sensors:</code> map is a config error, not an empty device.</p>"},{"location":"rig/sht4x/#config","title":"Config","text":"<pre><code>hum_sensors:\n  driver: sht4x_set\n  label: Humidity sensors\n  poll_s: 1\n  config:\n    link: i2c1\n    sensors:\n      chamber: { address: 0x44 }\n      dry: { address: 0x45 }\n      wet: { address: 0x46 }\n  signals:\n    chamber: { signals: { humidity: { warn: [20, 80] } } }\n    dry: { poll_s: 5 }\n    wet: { poll_s: 5 }\n</code></pre> <p><code>link</code> names an I2C bus link (<code>i2c1: { tag: i2c, bus: 1 }</code>, <code>/dev/i2c-1</code> through <code>smbus2</code> \u2014 <code>flyball_linux.links.i2c.I2cConfig</code>); each sensor in <code>config.sensors</code> is just its address (<code>config.precision</code> would set <code>\"medium\"</code>/<code>\"low\"</code> for all of them, if this rig ever needed to trade accuracy for speed \u2014 it doesn't). <code>signals:</code> in the envelope carries overrides only \u2014 <code>chamber.humidity</code>'s warn band, and the two supply lines' slower poll \u2014 never a new tree; the tree itself (which namespaces exist) is declared by <code>config.sensors</code>, not by <code>signals:</code>.</p>"},{"location":"rig/sources/","title":"Devices and signals","text":"<p>Which devices this rig declares, which quantities each signal carries, and which signal the controller drives.</p> <p>The rig has two devices \u2014 everything named in <code>rig.yaml</code> is one of these two, or the controller that binds them:</p> device driver what it is <code>hum_sensors</code> <code>sht4x_set</code> three SHT4x sensors, each its own namespace <code>blender</code> <code>dual_pump_blender</code> two PWM pumps, blended to a target humidity"},{"location":"rig/sources/#hum_sensors","title":"<code>hum_sensors</code>","text":"<p>One <code>sht4x_set</code> device, one namespace per sensor: <code>chamber</code> (the process value), <code>dry</code> and <code>wet</code> (the two supply lines). Each namespace is read in its own I\u00b2C transaction, and each declares the same two signals:</p> signal access quantity unit <code>humidity</code> <code>[RP]</code> humidity %RH <code>temperature</code> <code>[RP]</code> temperature \u00b0C <p>So the full address list is <code>hum_sensors.chamber.humidity</code>, <code>hum_sensors.chamber.temperature</code>, <code>hum_sensors.dry.humidity</code>, \u2026, <code>hum_sensors.wet.temperature</code> \u2014 six signals, three atomic namespaces. The rig file polls <code>chamber</code> every second and the two supply lines every five (<code>rig.yaml</code>'s <code>signals:</code> overrides, since the lines drift slowly and don't need the chamber's rate).</p>"},{"location":"rig/sources/#blender","title":"<code>blender</code>","text":"<p>One <code>dual_pump_blender</code> device, no namespaces \u2014 every signal is on its root. <code>rig.yaml</code>'s own comment gives the tree the driver declares:</p> <pre><code>    # signals the driver declares:\n    #   humidity                [W]    %RH, limits [0, 100]   the split-range target\n    #   dry_flow, wet_flow      [RPW]  L/min, together        manual flows; also the readback\n    #   dry_effort, wet_effort  [RPW]  0-1 of full, together  manual efforts; also the readback\n    #   blend_flow               [RW]  L/min                  a setting: read on demand, not streamed\n    #   expected_humidity        [RP]  %RH                    what the lines actually deliver\n</code></pre> <p><code>humidity</code> is the only signal a controller may drive \u2014 it's the split-range target, and <code>blender.humidity</code> is exactly that controller's name. The rest are for driving or reading the pumps directly: <code>dry_flow</code>/<code>wet_flow</code> and <code>dry_effort</code>/<code>wet_effort</code> are <code>together</code> pairs (a demand naming one alone is refused \u2014 \"set with wet_flow\"), <code>blend_flow</code> is a setting read on demand, and <code>expected_humidity</code> is what <code>commit</code> computes the blend should be delivering, published alongside the readbacks.</p> <p><code>blender.bound: { dry: hum_sensors.dry.humidity, wet: hum_sensors.wet.humidity }</code> means the blender follows the two supply sensors: whenever either publishes, <code>blender.observe</code> records the new supply humidity, ready for the next <code>commit</code> \u2014 no bus poll of its own for that half of the picture.</p>"},{"location":"rig/sources/#the-controller","title":"The controller","text":"<p>One controller, <code>blender.humidity</code>, named by the signal it drives:</p> <pre><code>controllers:\n  blender.humidity:\n    signal: hum_sensors.chamber.humidity\n    law: { tag: PI, kp: 0.8, ki: 0.02, tt: 60 }\n    default: true\n</code></pre> <p>It binds the chamber's published humidity (the source, <code>[P]</code>) to the blender's <code>humidity</code> target (<code>[W]</code>) through a PI law, and is the rig's default \u2014 the one a program step or a <code>flyball</code> command uses when it names no controller at all.</p>"}]}