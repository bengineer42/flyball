# The furnace simulation as one daemon: `uv run flyball-daemon ../examples/site/furnace.yaml`
# from controller. The rig is examples/simulated's; this file only says how to serve it.
extends: [../simulated/furnace.yaml]
daemon:
  port: 8002
  root_path: /furnace
  allow_shutdown: true
  store_dir: stores
